<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Git on 天才まくまくノート</title><link>https://maku77.github.io/git/</link><description>Recent content in Git on 天才まくまくノート</description><generator>Hugo</generator><language>ja-jp</language><lastBuildDate>Sat, 21 Mar 2026 00:00:00 +0900</lastBuildDate><atom:link href="https://maku77.github.io/git/index.xml" rel="self" type="application/rss+xml"/><item><title>GitHub で複数アカウントの SSH キーを使い分ける (~/.ssh/config)</title><link>https://maku77.github.io/p/vn9aqfb/</link><pubDate>Thu, 11 Apr 2024 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/vn9aqfb/</guid><description>&lt;p>1 つのクライアント PC で、複数の GitHub アカウントを使い分けたいことがあります。
典型的なのは以下のような 2 つのアカウントの切り替えです。&lt;/p>
&lt;ul>
&lt;li>プライベートの GitHub アカウント&lt;/li>
&lt;li>仕事用の GitHub アカウント&lt;/li>
&lt;/ul>
&lt;p>GitHub の仕様上、複数の GitHub アカウントに同じ SSH キー（公開鍵）を登録することはできないようになっているため、GitHub アカウントごとに必ず異なる SSH キーペアを用意する必要があります。
つまり、クライアント側で接続時に使用する SSH キー（秘密鍵）を切り替えることが、すなわち GitHub アカウントの切り替えを意味します。&lt;/p>
&lt;p>SSH クライアント（&lt;code>ssh&lt;/code> コマンド）はデフォルトでは &lt;code>~/.ssh/id_rsa&lt;/code> などのパスに配置された SSH 秘密鍵を参照するようになっているため、なんらかの方法で SSH 秘密鍵を切り替えて GitHub に接続する必要があります。
ここでは、&lt;strong>&lt;code>~/.ssh/config&lt;/code>&lt;/strong> を使って、使用する SSH キー（秘密鍵）を切り替える方法を説明します。&lt;/p>
&lt;h2 id="必要があればssh-キーペアの作成と-github-への登録">（必要があれば）SSH キーペアの作成と GitHub への登録&lt;/h2>
&lt;p>すでに GitHub アカウントへの SSH キーの登録が済んでいる場合はこの説明は飛ばしてください。&lt;/p>
&lt;h3 id="ssh-キーペアの作成">SSH キーペアの作成&lt;/h3>
&lt;ul>
&lt;li>参考: &lt;a href="https://maku.blog/p/ftducs9/">SSH キーの管理: SSH キーを作成する (&lt;code>ssh-keygen&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>プライベートのアカウントと、仕事用のアカウントの SSH キーペアを作成します。
キータイプとしては、高速かつ強度のある Ed25519 鍵を使うことにしましょう。
それぞれのキーペアは、&lt;code>~/.ssh/github-maku&lt;/code>、&lt;code>~/.ssh/github-work&lt;/code> というディレクトリに保存することにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> mkdir ~/.ssh/github-maku
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> mkdir ~/.ssh/github-work
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> ssh-keygen -t ed25519 -f ~/.ssh/github-maku/id_ed25519
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> ssh-keygen -t ed25519 -f ~/.ssh/github-work/id_ed25519
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>
&lt;div class="xAccordion">
 &lt;input id="id-849731652" type="checkbox">
 &lt;label class="xAccordion_title" for="id-849731652">コマンドプロンプトの場合&lt;/label>
 &lt;div class="xAccordion_body">
 

&lt;figure class="xCodeBlock">

&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl">cd %USERPROFILE%
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">mkdir .ssh\github-maku
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">mkdir .ssh\github-work
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">ssh-keygen -t ed25519 -f .ssh\github-maku\id_ed25519
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">ssh-keygen -t ed25519 -f .ssh\github-work\id_ed25519&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


 &lt;/div>
&lt;/div>


&lt;p>これで次のようなファイルが作成されます。&lt;/p></description></item><item><title>Git リポジトリ内のコードを grep 検索する (git grep)</title><link>https://maku77.github.io/p/2c29gvn/</link><pubDate>Wed, 15 Nov 2023 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/2c29gvn/</guid><description>&lt;h2 id="git-grep-とは">git grep とは&lt;/h2>
&lt;p>Git に組み込まれている &lt;strong>&lt;code>git grep&lt;/code>&lt;/strong> コマンドを使うと、Git リポジトリ内のコードを対象にした &lt;code>grep&lt;/code> 検索を行うことができます。
Linux の &lt;code>grep&lt;/code> コマンドを使うのと比べて、次のような利点があります。&lt;/p>
&lt;ul>
&lt;li>OS に依存しない &lt;code>grep&lt;/code> コマンドとして使用できる&lt;/li>
&lt;li>&lt;code>.gitignore&lt;/code> で指定されているファイルを無視して検索してくれる&lt;/li>
&lt;li>ある時点のコードを対象に検索できる（コミットハッシュの指定）&lt;/li>
&lt;li>ある文字列を含む関数を検索できる（&lt;code>-p&lt;/code> オプション）&lt;/li>
&lt;/ul>
&lt;h2 id="git-grep-の基本">git grep の基本&lt;/h2>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">カレントディレクトリ以下を検索する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git grep &lt;span class="s2">&amp;#34;検索文字列&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>カレントディレクトリ以下のファイルを対象として &lt;code>git grep&lt;/code> するには、上記のように単純に検索文字列を指定するだけで OK です。
もちろん、Git 管理されているディレクトリ以下で実行する必要があります。&lt;/p>
&lt;p>検索対象のディレクトリやファイルを指定するには、次のように &lt;strong>&lt;code>--&lt;/code>&lt;/strong> で区切ってからディレクトリ名やファイル名のパターンを指定します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">src ディレクトリ以下を検索する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git grep &lt;span class="s2">&amp;#34;検索文字列&amp;#34;&lt;/span> -- src
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">src ディレクトリ以下の .py ファイルを検索する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git grep &lt;span class="s2">&amp;#34;検索文字列&amp;#34;&lt;/span> -- &lt;span class="s2">&amp;#34;src/**.py&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>ワイルドカードを指定してファイル名のパターンを指定するときは、&lt;code>&amp;quot;src/**.py&amp;quot;&lt;/code> のようにダブルクォートで囲む必要があることに注意してください。
これは、&lt;code>git grep&lt;/code> にパラメーターが渡される前にシェルがファイルグロブとして展開してしまうのを防ぐためです。&lt;/p>
&lt;p>特定のディレクトリやファイルを検索対象外にしたいときは、&lt;strong>&lt;code>:^&lt;/code>&lt;/strong> というプレフィックスを付けます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">.github ディレクトリや package.json を検索対象外にする&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git grep react -- :^.github &lt;span class="s2">&amp;#34;:^package*.json&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>拡張正規表現を使って検索したいときは、&lt;strong>&lt;code>-E&lt;/code>&lt;/strong> オプションを指定します。&lt;/p></description></item><item><title>GitHub Actions のワークフローをローカルで実行する (act)</title><link>https://maku77.github.io/p/iudtbr8/</link><pubDate>Fri, 09 Dec 2022 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/iudtbr8/</guid><description>&lt;h2 id="act-とは">act とは？&lt;/h2>
&lt;p>&lt;strong>act コマンド&lt;/strong> は、Casey Lee 氏 (&lt;a href="https://github.com/nektos/">@nektos&lt;/a>) が作成した、GitHub Actions ワークフローをローカル実行するためのコマンドです。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://github.com/nektos/act">nektos/act: Run your GitHub Actions locally 🚀&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>通常、GitHub Actions のワークフローを実行するには、ワークフローファイル (&lt;code>.github/workflows/*.yml&lt;/code>) を作成して、GitHub へコミット＆プッシュする必要がありますが、&lt;code>act&lt;/code> コマンドを使うと、ローカルのワークフローファイルをそのまま実行できます。
ローカルでの実行を主目的とした、タスクランナーとしての利用も可能です。&lt;/p>
&lt;p>&lt;code>act&lt;/code> コマンドは、&lt;a href="https://github.blog/2020-07-03-github-action-hero-casey-lee/">GitHub Blog でも紹介されています&lt;/a>。&lt;/p>
&lt;h2 id="docker-と-act-のインストール">Docker と act のインストール&lt;/h2>
&lt;p>&lt;code>act&lt;/code> はワークフローの実行環境として Docker コンテナを利用するので、Docker 環境はあらかじめインストールしておいてください。
Docker Desktop をインストールすれば、簡単に Docker 環境が整います。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://www.docker.com/products/docker-desktop/">Docker Desktop&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>&lt;code>act&lt;/code> コマンドは Go 言語で実装されており、各 OS 用の実行ファイルが提供されています。
下記の公式ページの説明に従って、OS ごとのパッケージ管理コマンドでインストールしてしまうのが簡単です。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://github.com/nektos/act">nektos/act: Run your GitHub Actions locally 🚀&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>例えば、macOS であれば &lt;code>brew install act&lt;/code>、Windows であれば &lt;code>choco install act-cli&lt;/code> などでインストールできます。
インストールが終わって、次のように実行できるようになっていれば準備完了です。&lt;/p></description></item><item><title>GitHub Actions で GitHub wiki ページを自動更新する</title><link>https://maku77.github.io/p/f2eggno/</link><pubDate>Mon, 28 Nov 2022 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/f2eggno/</guid><description>&lt;h2 id="何をするか">何をするか？&lt;/h2>
&lt;p>GitHub で管理しているリポジトリに対してコミット＆プッシュが行われたときに、自動的に GitHub wiki 側のリポジトリの内容（Markdown ファイル）を更新するようにしてみます。
例えば、プロダクトのソースコードからドキュメントを自動生成して、開発者がいつでも GitHub wiki ページで参照できるようにしておくと便利です。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="" width="579" height="123" src="../p/f2eggno/img-002.png" alt="/p/f2eggno/img-002.png" />
 &lt;figcaption>図: GitHub wiki ページの自動生成&lt;/figcaption>
&lt;/figure>

&lt;p>wiki ページの更新には GitHub Actions のワークフローを使い、次のように自動実行されるよう設定します。&lt;/p>
&lt;ol>
&lt;li>メインリポジトリを対象としたプッシュで GitHub Actions ワークフローを起動&lt;/li>
&lt;li>メインリポジトリと wiki リポジトリのソースコードを取得&lt;/li>
&lt;li>何らかの外部ツールを実行して Markdown ファイルを生成&lt;/li>
&lt;li>Markdown ファイルを wiki リポジトリにコミット＆プッシュ&lt;/li>
&lt;/ol>
&lt;p>ワークフローの中で GitHub wiki のリポジトリをチェックアウトしているので、あらかじめ対象リポジトリの wiki を作成しておいてください（トップページだけで OK）。&lt;/p>
&lt;h2 id="ワークフローファイルの作成">ワークフローファイルの作成&lt;/h2>
&lt;p>GitHub wiki を自動更新するための、Actions のワークフローファイルを作成します。
&lt;code>.yml&lt;/code> 拡張子のファイルを &lt;strong>&lt;code>.github/workflows&lt;/code>&lt;/strong> ディレクトリ以下に配置してコミットすれば、GitHub サービス側でワークフローとして認識してくれます。
ここでは、&lt;strong>&lt;code>update-wiki.yml&lt;/code>&lt;/strong> というファイル名にしてみました。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">.github/workflows/update-wiki.yml&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-yaml" data-lang="yaml">&lt;span class="line">&lt;span class="cl">&lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Update wiki pages&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">on&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">push&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">branches&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">main, master ]&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">workflow_dispatch&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">jobs&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">update-wiki&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">runs-on&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">ubuntu-latest&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">timeout-minutes&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="m">3&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">steps&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>- &lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Check out main repo&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">uses&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">actions/checkout@v3&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>- &lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Check out wiki repo&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">uses&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">actions/checkout@v3&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">with&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">repository&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;${{ github.repository }}.wiki&amp;#34;&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">path&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">.wiki&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>- &lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Update wiki pages&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">run&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">echo -e &amp;#34;# Hello\n\n$(date)&amp;#34; &amp;gt; .wiki/hello.md&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>- &lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Stage and count changes&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">working-directory&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">.wiki&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">id&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">staging&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">run&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="p">|&lt;/span>&lt;span class="sd">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> git add .
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> echo &amp;#34;NUM_OF_STAGED=$(git diff --staged --name-only | wc -l)&amp;#34; &amp;gt;&amp;gt; $GITHUB_OUTPUT&lt;/span>&lt;span class="w"> 
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>- &lt;span class="nt">name&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">Commit wiki pages&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">working-directory&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">.wiki&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">if&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">steps.staging.outputs.NUM_OF_STAGED &amp;gt; 0&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w"> &lt;/span>&lt;span class="nt">run&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="p">|&lt;/span>&lt;span class="sd">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> git config user.email &amp;#34;41898282+github-actions[bot]@users.noreply.github.com&amp;#34;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> git config user.name &amp;#34;github-actions[bot]&amp;#34;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> git commit -m &amp;#34;${GITHUB_WORKFLOW}&amp;#34;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="sd"> git push&lt;/span>&lt;span class="w"> &lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>定義しているジョブは、&lt;code>update-wiki&lt;/code> というジョブ 1 つだけです。
以下、このジョブの各ステップを順番に見ていきます。&lt;/p></description></item><item><title>Git サブモジュールで別リポジトリの内容を組み込む (git submodule)</title><link>https://maku77.github.io/p/dsctaq7/</link><pubDate>Sun, 13 Nov 2022 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/dsctaq7/</guid><description>&lt;h2 id="git-サブモジュールとは">Git サブモジュールとは&lt;/h2>
&lt;p>Git サブモジュールは、既存の別リポジトリの内容を、サブディレクトリの形で参照できるようにする仕組みです。
例えば、次のようなディレクトリ構成のプロジェクトがあったとします。&lt;/p>
&lt;pre tabindex="0">&lt;code>my-project/
 +-- src/
 +-- my-libs/ ★別リポジトリをサブモジュールとして組み込む
&lt;/code>&lt;/pre>&lt;p>ここでは、別リポジトリで管理している共有ライブラリを &lt;code>my-libs&lt;/code> サブディレクトリの形で参照できるようにしています。
NPM や Maven などのパッケージレジストリから共有ライブラリを取り込む方法もありますが、Git サブモジュールの仕組みを使うと、&lt;strong>メインプロジェクトでの開発と共有ライブラリの開発を並行して進められる&lt;/strong> ようになります。&lt;/p>
&lt;p>Git サブモジュールで特徴的なのは、メインプロジェクトからはサブモジュールの内容を &lt;strong>コミットハッシュのみで追跡する&lt;/strong> ということです。
この振る舞いを理解してしまえば、Git サブモジュールを使いこなすのは難しくありません。
サブモジュール側の変更履歴は、あくまでサブモジュール側の Git リポジトリで管理されます。
つまり、サブモジュール側のリポジトリで大量のコミットが行われていたとしても、メインプロジェクト側のリポジトリサイズが増加していくということはありません。
メインプロジェクト側では、どの時点でのスナップショット（のコミットハッシュ）を参照するかを指定するだけです。
サブモジュールとして取り込む Git リポジトリの URL は柔軟に切り替えることができます。&lt;/p>
&lt;h3 id="git-サブモジュールの利用例">Git サブモジュールの利用例&lt;/h3>
&lt;ul>
&lt;li>共有ライブラリ用のリポジトリがあるけれど、NPM や Maven などのパッケージリポジトリにはリリースしていないとき、サブモジュールとして共有ライブラリを取り込む。メインプロジェクト側の開発中に、並行して共有ライブラリのコードを修正したい場合も同様。&lt;/li>
&lt;li>頻繁に更新されるファイルがあるけれど、メインプロジェクト側のコミット履歴には残したくないとき、別リポジトリでそのファイルを管理し、サブモジュールとして取り込む。&lt;/li>
&lt;/ul>
&lt;h2 id="別リポジトリをサブモジュールとして追加する-git-submodule-add">別リポジトリをサブモジュールとして追加する (git submodule add)&lt;/h2>
&lt;p>既存の別リポジトリの内容（前述の例では共有ライブラリ）を、カレントプロジェクトにサブモジュールとして組み込みたいときは、&lt;strong>&lt;code>git submodule add&lt;/code>&lt;/strong> コマンドを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git submodule add &amp;lt;別リポジトリのURL&amp;gt; &lt;span class="o">[&lt;/span>ローカルディレクトリ&lt;span class="o">]&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>例えば次のように実行すると、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git submodule add https://github.com/maku77/my-libs
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ローカルに &lt;code>my-libs&lt;/code> というディレクトリが作成されて、サブモジュールとして参照できるようになります。
別のディレクトリ名で取り込みたい場合は、末尾にディレクトリ名を追加で指定します。&lt;/p>
&lt;p>初めてサブモジュールが追加されると、&lt;strong>&lt;code>.gitmodules&lt;/code>&lt;/strong> というメタ情報ファイルが作成されます。
ここには、サブモジュールごとのリポジトリ URL とローカルディレクトリのパスが記録されています。
このファイルをコミットすることで、他の開発者がサブモジュールとして管理されているファイルを取得できるようになります。&lt;/p></description></item><item><title>Git ユーザーをディレクトリごとに自動で切り替える (.gitconfig, includeIf)</title><link>https://maku77.github.io/p/hxyiu7g/</link><pubDate>Mon, 12 Sep 2022 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/hxyiu7g/</guid><description>&lt;h2 id="何をするか">何をするか？&lt;/h2>
&lt;p>会社用と個人用の GitHub ユーザーを使い分けている場合、リポジトリへのコミット時に正しいユーザーでコミットログを残すように注意しなければいけません。
多くのリポジトリを扱っている人は、次のような感じで GitHub organization ごとにディレクトリを分けて管理するなどの工夫をしていると思います（リポジトリ名だけだと重複するので）。&lt;/p>
&lt;pre tabindex="0">&lt;code>~/gitwork/
 +-- company/ ... 会社用のリポジトリ（会社用のユーザー Rei Ayanami を使いたい）
 | +-- repo1/
 | +-- repo2/
 | +-- repo3/
 +-- maku77/ ... 個人用のリポジトリ（個人用のユーザー maku77 を使いたい）
 +-- repo4/
 +-- repo5/
 +-- repo6/
&lt;/code>&lt;/pre>&lt;p>&lt;code>company&lt;/code> は自分の会社で使っている GitHub organization 名、&lt;code>maku77&lt;/code> は自分の GitHub アカウント名だと考えてください。
ここでは、上記のようにディレクトリを階層化してリポジトリを管理しているときに、ディレクトリ単位で自動的に Git クライアントの設定を切り替える方法を示します。
具体的には、&lt;code>company&lt;/code> ディレクトリ以下のリポジトリで作業しているときは、会社用の Git ユーザー名とメールアドレスを使い、&lt;code>maku77&lt;/code> ディレクトリ以下のリポジトリで作業しているときは、個人用のユーザー名とメールアドレスを使うようにします。&lt;/p>
&lt;p>リポジトリごとに local 設定 (&lt;code>.git/config&lt;/code>) をするのもよいのですが、扱うリポジトリが増えてくるといちいち設定するのが大変なので、親ディレクトリの &lt;code>company&lt;/code>、&lt;code>maku77&lt;/code> 単位でまるっと設定を入れ替えます。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/af7q7n3/">Git 設定のスコープ (local/global/system) を理解する&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="設定ファイルの自動切換えincludeif-ディレクティブ">設定ファイルの自動切換え（includeIf ディレクティブ）&lt;/h2>
&lt;p>Git クライアントの設定ファイル（&lt;code>~/.gitconfig&lt;/code> など）には、もともと別の設定ファイルをインクルードする機能 (&lt;code>include&lt;/code> ディレクティブ）があるのですが、&lt;a href="https://git-scm.com/docs/git-config#_conditional_includes">Conditional includes（条件付きインクルード）&lt;/a> の仕組みを使うと、指定した条件に一致したときのみ設定ファイルをインクルードすることができます。
この仕組みを利用して、次のような条件付きインクルードを行えば、Git のユーザー設定をディレクトリごとに自動で切り替えることができます。&lt;/p></description></item><item><title>Gitメモ: Git やり直し: main ブランチに入れてしまったコミットを別のブランチに移す</title><link>https://maku77.github.io/p/8uatxxj/</link><pubDate>Tue, 01 Feb 2022 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/8uatxxj/</guid><description>&lt;h2 id="何をするか">何をするか？&lt;/h2>
&lt;p>あるコードをトピックブランチにコミットしないといけないのに、間違えて &lt;code>main&lt;/code>（旧名 &lt;code>master&lt;/code>）ブランチに入れてしまった場合の修正方法です。
ここでは、&lt;code>main&lt;/code> ブランチに入れてしまった直近 2 つのコミットを、&lt;code>new-feature&lt;/code> というブランチに移動してみます。&lt;/p>
&lt;h2 id="手順">手順&lt;/h2>
&lt;h3 id="1-main-ブランチの現状を確認する">(1) main ブランチの現状を確認する&lt;/h3>
&lt;p>まず、main ブランチのコミットログを確認しておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch main
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --oneline
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">3d40884 (HEAD -&amp;gt; main) Commit 4
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">daba4f2 Commit 3
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">256b4f0 Commit 2
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">696a835 (origin/main, origin/HEAD) Commit 1
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>一番上の &lt;code>Commit 4&lt;/code> が最新のコミットで、一番下の &lt;code>Commit 1&lt;/code> が GitHub などから pull してきたコミットであることがわかります。
&lt;code>Commit 2&lt;/code> から &lt;code>Commit 4&lt;/code> までは、ローカルだけでコミットされた（まだ push していない）ものなので、今回のような方法でコミット履歴を修正する余地があります。&lt;/p>
&lt;h3 id="2-new-feature-ブランチを用意する">(2) new-feature ブランチを用意する&lt;/h3>
&lt;p>ここでは、&lt;code>main&lt;/code> に入れてしまった &lt;strong>&lt;code>Commit 3&lt;/code> と &lt;code>Commit 4&lt;/code> を別のブランチ (&lt;code>new-feature&lt;/code>) に移動させたい&lt;/strong> とします。
まず、現在の &lt;code>main&lt;/code> ブランチと同じ情報を持つ &lt;code>new-feature&lt;/code> ブランチを作成します。&lt;/p></description></item><item><title>Gitメモ: Git で変更内容にある文字列が含まれているコミットを検索する (git log -G/-S)</title><link>https://maku77.github.io/p/hf7bnyi/</link><pubDate>Tue, 22 Dec 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/hf7bnyi/</guid><description>&lt;p>ソースコードのある行の変更がなぜ行われたかを調べるには &lt;code>git blame&lt;/code> コマンドが便利で、指定したファイルに関して行単位の変更者（とコミットメッセージ）を確認することができます。
ただし、&lt;code>git blame&lt;/code> では直近の変更しか調べられないため、もっと前のコミット履歴まで遡って変更者を確認することはできません。
例えば、リファクタで関数を別クラスに移動させた場合は、&lt;code>git blame&lt;/code> ではリファクタを行った人の名前が見えてしまいます。&lt;/p>
&lt;p>そんなときは、&lt;strong>&lt;code>git log&lt;/code>&lt;/strong> コマンドの &lt;strong>&lt;code>-G&lt;/code>&lt;/strong> オプションや &lt;strong>&lt;code>-S&lt;/code>&lt;/strong> オプションを使用すると、すべてのコミットのソースコードの変更内容を検索することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -G&lt;span class="s2">&amp;#34;正規表現&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -S&lt;span class="s2">&amp;#34;文字列&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -S&lt;span class="s2">&amp;#34;正規表現&amp;#34;&lt;/span> --pickaxe-regex
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="git-log--g変更内容の検索">git log -G（変更内容の検索）&lt;/h2>
&lt;p>例えば、ソースコードの変更内容（追加行 or 削除行）に、&lt;code>xxxYyyZzz&lt;/code> が含まれているコミットを検索したいのであれば、&lt;code>git log&lt;/code> の &lt;strong>&lt;code>-G&lt;/code>&lt;/strong> オプションを使って次のようにします。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: 変更内容に xxxYyyZzz が含まれているコミットを探す&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -G&lt;span class="s2">&amp;#34;xxxYyyZzz&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>実際の変更行まで表示したいのであれば、&lt;code>git log&lt;/code> の &lt;strong>&lt;code>-p&lt;/code>&lt;/strong> オプションも指定します。
じっくり見たいのであれば、ファイルにリダイレクトしてしまうのもよいです。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -G&lt;span class="s2">&amp;#34;xxxYyyZzz&amp;#34;&lt;/span> -p &amp;gt; log.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>-G&lt;/code> オプションで指定する文字列は、正規表現として扱われます。
例えば、次のようにすると &lt;code>image012&lt;/code> などの変更内容にヒットします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -G&lt;span class="s2">&amp;#34;image[0-9]+&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="git-log--spickaxe-検索">git log -S（pickaxe 検索）&lt;/h2>
&lt;p>&lt;strong>&lt;code>-S&lt;/code>&lt;/strong> オプションは、&lt;code>-G&lt;/code> オプションとはちょっと異なり、指定した単語で検索したときの &lt;strong>追加行と削除行の数が異なる&lt;/strong> コミットのみ検索します。
つまり、&lt;code>-S&lt;/code> オプションの方を使えば、ある名前の関数が初めて追加されたコミットだけを効率的に探すことができます（リファクタで関数を移動しただけの場合は、追加行と削除行の数が等しくなるのでヒットしなくなる）。&lt;/p></description></item><item><title>GitHub のオートリンク機能で外部サイトへ ID ベースでリンクする (autolink)</title><link>https://maku77.github.io/p/ias8kt3/</link><pubDate>Thu, 03 Dec 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ias8kt3/</guid><description>&lt;h2 id="オートリンク機能とは">オートリンク機能とは&lt;/h2>
&lt;p>GitHub の Issue（イシュー）や PR（プルリクエスト）のページの概要欄、コメント欄では、次のように Issue 番号を書いておくだけで該当ページに自動リンクすることができます。&lt;/p>
&lt;pre tabindex="0">&lt;code>#1234
&lt;/code>&lt;/pre>&lt;ul>
&lt;li>参考: &lt;a href="https://docs.github.com/en/free-pro-team@latest/github/writing-on-github/autolinked-references-and-urls">Autolinked references and URLs - GitHub Docs&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>この機能はあくまで GitHub サイト内のリンクを簡単に記述するためのものですが、&lt;strong>カスタムの Autolink references&lt;/strong> を設定しておくことで、任意の外部サイトに ID ベースで自動リンクできるようになります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="https://docs.github.com/en/free-pro-team@latest/github/administering-a-repository/configuring-autolinks-to-reference-external-resources">Configuring autolinks to reference external resources - GitHub Docs&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>例えば、仕様書サイトのあるページ（ID=123 とします）にリンクしたい場合に、次のように簡単にリンクできるようになります。&lt;/p>
&lt;pre tabindex="0">&lt;code>SPEC-123
&lt;/code>&lt;/pre>&lt;p>URL そのものを記述する必要がなくなるので、仕様書サイトのサーバー移転などでドメイン名が変わったとしても、Issue のコメントに記述したリンクが無効になってしまうことがありません。
ちなみにブログサイトなどの各ページの URL には、ID ベースのパーマリンクアドレスを割り当てる運用にしておくと、こういった機能と連携しやすくなります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/u9r9p7n/">サイト構造を変えてもページの URL が変わらないようにする - まくまくHugoノート&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="カスタムオートリンクの設定">カスタム・オートリンクの設定&lt;/h2>
&lt;p>（2022年7月時点で、このカスタム・オートリンクは、GitHub Pro、GitHub Team などの &lt;strong>有料プランのみで設定可能&lt;/strong> です）&lt;/p>
&lt;p>外部サイトへのオートリンク機能を有効にするには、次のように設定します。
ここでは、外部サイトの URL は &lt;code>https://spec.example.com/p/123/&lt;/code> といったパーマリンクで提供されるものとします（123 といった ID はサーバー移転などしても変わらないことが保証されていると過程）。&lt;/p>
&lt;ol>
&lt;li>対象のリポジトリの &lt;strong>&lt;code>Settings&lt;/code>&lt;/strong>（設定）を選択&lt;/li>
&lt;li>サイドバーから &lt;strong>&lt;code>Autolink references&lt;/code>&lt;/strong> を選択&lt;/li>
&lt;li>次のような感じで Autolink 方法を設定
&lt;ul>
&lt;li>Reference prefix: &lt;strong>&lt;code>SPEC-&lt;/code>&lt;/strong>&lt;/li>
&lt;li>Target URL: &lt;strong>&lt;code>https://spec.example.com/p/&amp;lt;num&amp;gt;/&lt;/code>&lt;/strong>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;p>すると、Issue や PR のコメントとして &lt;code>SPEC-123&lt;/code> のように記述するだけで、自動的に &lt;code>https://spec.example.com/p/123/&lt;/code> という URL へリンクされるようになります。&lt;/p></description></item><item><title>GitHub で Pull Request のコードレビューワーを自動でアサインする</title><link>https://maku77.github.io/p/oifj5jn/</link><pubDate>Tue, 20 Oct 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/oifj5jn/</guid><description>&lt;p>2019年の末に、GitHub にコードレビューワーの自動アサイン機能が&lt;a href="https://github.blog/changelog/2019-11-12-code-review-assignment-beta/">追加&lt;/a>されました。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://docs.github.com/en/free-pro-team@latest/github/setting-up-and-managing-organizations-and-teams/managing-code-review-assignment-for-your-team">Managing code review assignment for your team - GitHub Docs&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>この機能を有効にすると、Pull Request のレビューワーとしてチームをアサインしたときに、そのチーム内の数名を自動アサインしてくれるようになります。
この機能は、チーム単位の設定になるので、あらかじめリポジトリに何らかのチームを参加させておく必要があります。
チーム構成に迷ったら、プロジェクトと同じ粒度で作ってしまうのが手っ取り早いです（例えば、&lt;code>MyApp&lt;/code> プロジェクトのチームとして、&lt;code>team-MyApp&lt;/code> を作成します）。&lt;/p>
&lt;p>自動アサインの設定画面は、チームのページから、&lt;strong>Settings&lt;/strong> → &lt;strong>Code review assignment&lt;/strong> と辿ると開くことができます。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="border: solid #ccc 2px;" width="700" height="350" src="../p/oifj5jn/img-001_hu7087904287513090476.png" alt="/p/oifj5jn/img-001.png" />
 &lt;figcaption>図: GitHub Auto Assign&lt;/figcaption>
&lt;/figure>

&lt;p>上記の例では次のような設定を行っています。&lt;/p>
&lt;ul>
&lt;li>&lt;b>自動アサインする人数:&lt;/b> 2人&lt;/li>
&lt;li>&lt;b>選択アルゴリズム:&lt;/b> ロードバランス&lt;/li>
&lt;li>&lt;b>アサイン対象外とする人:&lt;/b> （省略）&lt;/li>
&lt;/ul>
&lt;p>自動アサインのアルゴリズムとしては「ラウンドロビン」と「ロードバランス」を選択できます。
「ラウンドロビン」を選択すると、単純に順番に割り当てていきます。
「ロードバランス」を選択すると、最近レビュー活動を行っていない人から優先的に割り当てていきます。&lt;/p>
&lt;p>自動アサインといっても、PR 作成時に完全自動でアサインされるわけではありません（チームを手動でアサインする必要がある）。
状況に応じて、この自動アサイン機能と、ユーザー単位の手動アサインを使い分けると便利です。&lt;/p>
&lt;p>ちなみに、この自動アサインの設定は、Organization の Owner メンバーだけでなく、各チームで &lt;strong>team maintainer&lt;/strong> として登録されているユーザーが自由に行うことができます。
各チームのレビュー事情はチーム内の人が一番よく分かっているので、team maintainer をうまく設定してあげるとよいです。&lt;/p></description></item><item><title>GitHub で管理している社内リポジトリをトピックで分類する (topics)</title><link>https://maku77.github.io/p/3j6qatd/</link><pubDate>Wed, 14 Oct 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/3j6qatd/</guid><description>&lt;h2 id="github-リポジトリのトピック機能">GitHub リポジトリのトピック機能&lt;/h2>
&lt;p>社内で GitHub を使っている場合、組織内 (GitHub organization) にたくさんのリポジトリがごちゃ混ぜになっているかもしれません。
例えば、製品アプリ用のリポジトリ、社内ツール用のリポジトリ、ドキュメント管理用のリポジトリなどなど。&lt;/p>
&lt;p>このような場合は、&lt;strong>リポジトリのトピック機能&lt;/strong> を使用すると、リポジトリをグループ化して管理することができます。
例えば、一般ユーザーにリリースする予定の製品アプリには &lt;strong>&lt;code>product&lt;/code>&lt;/strong> というトピックを付けるというルールで運用すると、製品アプリの一覧を簡単に作成することができます。
GitHub API を使ってリポジトリの一覧情報を取得すれば、高度な自動化処理を実現することができます。&lt;/p>
&lt;h2 id="github-リポジトリにトピックを割り当てる">GitHub リポジトリにトピックを割り当てる&lt;/h2>
&lt;p>リポジトリへのトピック割り当てはとても簡単です。
リポジトリのトップページを開き、右上の &lt;strong>歯車アイコンをクリック&lt;/strong> します。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="border: solid #ccc 2px;" width="800" height="309" src="../p/3j6qatd/img-001_hu8121801663604449553.png" alt="/p/3j6qatd/img-001.png" />
&lt;/figure>

&lt;p>すると、リポジトリの情報を入力するダイアログが開くので、&lt;strong>Topics&lt;/strong> の項目に任意のトピック名を入力します。
ここでは、製品アプリであることを示す &lt;code>product&lt;/code> というトピックを付けています。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="border: solid #ccc 2px;" width="800" height="589" src="../p/3j6qatd/img-002_hu9667792538850133952.png" alt="/p/3j6qatd/img-002.png" />
&lt;/figure>

&lt;p>あとは、左上の検索窓などに &lt;strong>&lt;code>topic:product&lt;/code>&lt;/strong> と入力すれば、組織内の製品アプリ用のリポジトリの一覧を表示することができます。&lt;/p>
&lt;p>

&lt;figure class="xImage">
 &lt;img style="border: solid #ccc 2px;" width="800" height="171" src="../p/3j6qatd/img-003_hu11602044055995248926.png" alt="/p/3j6qatd/img-003.png" />
&lt;/figure>



&lt;figure class="xImage">
 &lt;img style="border: solid #ccc 2px;" width="800" height="456" src="../p/3j6qatd/img-004_hu95923375222093018.png" alt="/p/3j6qatd/img-004.png" />
&lt;/figure>
&lt;/p>
&lt;p>このときブラウザに表示されている URL を共有すれば、マネージャー陣にアプリリストを提出するということも簡単にできます。
製品別のトピックを付けて管理するのもいいですね！&lt;/p>
&lt;h2 id="トピックの-and-検索と-or-検索">トピックの AND 検索と OR 検索&lt;/h2>
&lt;p>複数のトピックで GitHub リポジトリを検索するときに、次のように指定すれば AND 検索や OR 検索を行えるようです（2023-03 確認）。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">AND 検索の例: AAA トピックと BBB トピックが付いているリポジトリを検索&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl">topic:AAA topic:BBB&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">OR 検索の例: AAA トピックまたは BBB トピックが付いているリポジトリを検索&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl">topic:&amp;#34;AAA BBB&amp;#34;&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: git diff で package-lock.json の差分が表示されるのを防ぐ (:(exclude))</title><link>https://maku77.github.io/p/4zoiaou/</link><pubDate>Tue, 07 Jul 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/4zoiaou/</guid><description>&lt;p>Node.js でアプリを開発していると、npm が生成する依存情報ファイルである &lt;a href="https://docs.npmjs.com/configuring-npm/package-lock-json.html">package-lock.json も一緒にコミットする&lt;/a> ことになると思います。
&lt;code>npm install&lt;/code> で新しくモジュールをインストールすると、&lt;code>package-lock.json&lt;/code> に大量の差分が発生してしまい、自分が修正したソースコードの差分を &lt;code>git diff&lt;/code> で確認するのが難しくなってしまいます。&lt;/p>
&lt;p>そのような場合は、次のように &lt;code>git diff&lt;/code> を実行すれば、&lt;code>package-lock.json&lt;/code> 以外の差分を確認することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff -- . &lt;span class="s2">&amp;#34;:(exclude)package-lock.json&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ステージングされたものの差分を見るには次のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --staged -- . &lt;span class="s2">&amp;#34;:(exclude)package-lock.json&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これらのコマンドを頻繁に使用するようであれば、次のようにエイリアスを定義しておくのがよいでしょう（&lt;code>ndiff&lt;/code> は node diff の略のつもり）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.ndiff &lt;span class="s2">&amp;#34;diff -- . :(exclude)package-lock.json&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.ndiff-staged &lt;span class="s2">&amp;#34;diff --staged -- . :(exclude)package-lock.json&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これで、次のように &lt;code>package-lock.json&lt;/code> 以外の差分を簡単に確認できるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git ndiff
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git ndiff-staged
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>GitHub の REST API を Python から使用する</title><link>https://maku77.github.io/p/8axzppz/</link><pubDate>Sat, 30 May 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/8axzppz/</guid><description>&lt;h2 id="何を作るか">何を作るか？&lt;/h2>
&lt;p>Python で GitHub API を利用するアプリケーションを作るときは、GitHub API を実行して情報取得する部分を、1 つのモジュールとして切り出しておくと全体のコードがすっきりします。
ここでは、実装例として &lt;code>GitHubApi&lt;/code> というクラスを作り、次のようなことを行えるようにします。&lt;/p>
&lt;ul>
&lt;li>&lt;code>get_members()&lt;/code> や &lt;code>get_issues()&lt;/code> といった直感的な API で GitHub の情報を取得できるようにする&lt;/li>
&lt;li>オプションでプロキシや Personal Access Token を指定できるようにする&lt;/li>
&lt;li>ページネーションによる連続アクセスで、多くの情報を一度に取得できるようにする&lt;/li>
&lt;/ul>
&lt;p>GitHub の REST API に関しては、下記の記事を参照してください。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="../p/mprs3v6/">GitHub の REST API を使用する&lt;/a>&lt;/li>
&lt;li>&lt;a href="../p/uvoibwi/">GitHub の REST API で Issue 情報を取得する方法いろいろ&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="githubapi-クラスを実装する">GitHubApi クラスを実装する&lt;/h2>
&lt;p>ここで紹介する &lt;code>GitHubApi&lt;/code> クラスを使うと、次のようなシンプルなコードで GitHub API を呼び出して、その結果を Python のオブジェクトとして取得することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-python" data-lang="python">&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># ユーザー情報を取得する&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="n">user&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">api&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">get_user&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s1">&amp;#39;maku77&amp;#39;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">print&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">user&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="s1">&amp;#39;login&amp;#39;&lt;/span>&lt;span class="p">])&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>下記は、&lt;code>GitHubApi&lt;/code> クラスの実装例です。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">github.py&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-python" data-lang="python">&lt;span class="line">&lt;span class="cl">&lt;span class="kn">import&lt;/span> &lt;span class="nn">json&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kn">import&lt;/span> &lt;span class="nn">re&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kn">import&lt;/span> &lt;span class="nn">sys&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kn">from&lt;/span> &lt;span class="nn">urllib.parse&lt;/span> &lt;span class="kn">import&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">urlencode&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kn">import&lt;/span> &lt;span class="nn">urllib.request&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">class&lt;/span> &lt;span class="nc">GitHubApi&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="s1">&amp;#39;https://api.github.com&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">UNVERIFY_SSL&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="kc">True&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="fm">__init__&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">option&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="p">{}):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">option&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">UNVERIFY_SSL&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__unverify_ssl&lt;/span>&lt;span class="p">()&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">__unverify_ssl&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="kn">import&lt;/span> &lt;span class="nn">ssl&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">ssl&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">_create_default_https_context&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">ssl&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">_create_unverified_context&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="kc">False&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">req&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__create_request&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">try&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">with&lt;/span> &lt;span class="n">urllib&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">request&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">urlopen&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">req&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">as&lt;/span> &lt;span class="n">res&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">json_text&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">res&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">read&lt;/span>&lt;span class="p">()&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">decode&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s1">&amp;#39;utf-8&amp;#39;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">json_obj&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">json&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">loads&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">json_text&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># ページネーションによる繰り返し取得&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">next_link&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_next_link&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">res&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">info&lt;/span>&lt;span class="p">())&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="n">next_link&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">json_obj&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">extend&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">next_link&lt;/span>&lt;span class="p">))&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="n">json_obj&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">except&lt;/span> &lt;span class="n">urllib&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">error&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">URLError&lt;/span> &lt;span class="k">as&lt;/span> &lt;span class="n">err&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nb">print&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s1">&amp;#39;Could not access: &lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="n">req&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">full_url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">file&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="n">sys&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">stderr&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nb">print&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">err&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">file&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="n">sys&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">stderr&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">sys&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">exit&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># ページネーションによる連続取得が必要な場合は、&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 次のアドレスを返す。必要ない場合は None を返す。&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">__get_next_link&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">response_headers&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">link&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">response_headers&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="s1">&amp;#39;Link&amp;#39;&lt;/span>&lt;span class="p">]&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="ow">not&lt;/span> &lt;span class="n">link&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="kc">None&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">match&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">re&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">search&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="sa">r&lt;/span>&lt;span class="s1">&amp;#39;&amp;lt;(\S+)&amp;gt;; rel=&amp;#34;next&amp;#34;&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">link&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="k">match&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="k">match&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">group&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="kc">None&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">__create_request&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">url&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">req&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="n">urllib&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">request&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">Request&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="s1">&amp;#39;token&amp;#39;&lt;/span> &lt;span class="ow">in&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">req&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">add_header&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s1">&amp;#39;Authorization&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s1">&amp;#39;token &lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="s1">&amp;#39;token&amp;#39;&lt;/span>&lt;span class="p">])&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="s1">&amp;#39;proxy&amp;#39;&lt;/span> &lt;span class="ow">in&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">req&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">set_proxy&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="s1">&amp;#39;proxy&amp;#39;&lt;/span>&lt;span class="p">],&lt;/span> &lt;span class="s1">&amp;#39;http&amp;#39;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">req&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">set_proxy&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">option&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="s1">&amp;#39;proxy&amp;#39;&lt;/span>&lt;span class="p">],&lt;/span> &lt;span class="s1">&amp;#39;https&amp;#39;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="n">req&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 指定したユーザーの情報を取得します&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">get_user&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">username&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="s1">&amp;#39;/users/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">username&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 指定した組織のメンバーリストを取得します&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">get_members&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">org&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="s1">&amp;#39;/orgs/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">/members?per_page=100&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">org&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="kc">True&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 指定した組織のリポジトリ一覧を取得します&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">get_org_repos&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">org&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="s1">&amp;#39;/orgs/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">/repos?per_page=100&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">org&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="kc">True&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 指定したユーザーのリポジトリ一覧を取得します&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">get_user_repos&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">user&lt;/span>&lt;span class="p">):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="s1">&amp;#39;/users/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">/repos?per_page=100&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">user&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="kc">True&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># 指定したリポジトリの Issue 一覧を取得します&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">def&lt;/span> &lt;span class="nf">get_issues&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="bp">self&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">owner&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">repo&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">params&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="p">{}):&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">BASE_URL&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="s1">&amp;#39;/repos/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">/&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">/issues?per_page=100&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">owner&lt;/span>&lt;span class="p">),&lt;/span> &lt;span class="n">quote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">repo&lt;/span>&lt;span class="p">))&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="n">params&lt;/span>&lt;span class="p">:&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="n">url&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="s1">&amp;#39;&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">&amp;amp;&lt;/span>&lt;span class="si">%s&lt;/span>&lt;span class="s1">&amp;#39;&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">urlencode&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">params&lt;/span>&lt;span class="p">))&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="bp">self&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">__get_json&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">url&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="n">paginate&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="kc">True&lt;/span>&lt;span class="p">)&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>最後の方にある &lt;code>get_xxx&lt;/code> 系のメソッドが、GitHub API を呼び出すための public メソッドです。
同じようにメソッドを追加していけば、いろいろな GitHub API に対応することができます。
どのような API があるかは、&lt;a href="https://developer.github.com/v3/">GitHub REST API v3 のサイト&lt;/a> を参照してください。&lt;/p></description></item><item><title>GitHub の REST API で Issue 情報を取得する方法いろいろ</title><link>https://maku77.github.io/p/uvoibwi/</link><pubDate>Mon, 25 May 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/uvoibwi/</guid><description>&lt;h2 id="あるリポジトリの-issues-の一覧を取得する">あるリポジトリの Issues の一覧を取得する&lt;/h2>
&lt;p>GitHub の &lt;strong>&lt;code>/repos/&amp;lt;ユーザー名&amp;gt;/&amp;lt;リポジトリ名&amp;gt;/issues&lt;/code>&lt;/strong> という REST API エンドポイントにアクセスすると、指定したリポジトリ内の Issue の一覧を取得することができます。
特定の Organization が提供しているリポジトリを指定するときは、&lt;code>ユーザー名&lt;/code> の部分を &lt;code>Organization名&lt;/code> に置き換えてください。&lt;/p>
&lt;h3 id="public-リポジトリの場合">Public リポジトリの場合&lt;/h3>
&lt;p>Public リポジトリの情報は、誰でも簡単に取得することができます。
下記は、&lt;code>curl&lt;/code> コマンドを使って REST API を呼び出すサンプルです。
ユーザー名とリポジトリ名の部分は適切な値に変更してください。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">Public なリポジトリの場合&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nv">USER_NAME&lt;/span>&lt;span class="o">=&lt;/span>myname
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nv">REPO_NAME&lt;/span>&lt;span class="o">=&lt;/span>myrepo
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> curl https://api.github.com/repos/&lt;span class="nv">$USER_NAME&lt;/span>/&lt;span class="nv">$REPO_NAME&lt;/span>/issues
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>HTTP レスポンスのヘッダ情報も確認したい場合は、 &lt;strong>&lt;code>-i&lt;/code>&lt;/strong> オプションを追加してください。
上記のような GET リクエストにより、Issue のリスト（PullRequest も含む）が JSON 形式で返されます。
単純な GET リクエストなので、ブラウザで直接 URL を入力することでも情報を取得することができます。&lt;/p>
&lt;p>下記は、1 つの Issue だけ含むリポジトリから情報を取得したときのサンプルです。
この Issue には、&lt;code>ラベル1&lt;/code> と &lt;code>ラベル2&lt;/code> という 2 つのラベルが設定されています。&lt;/p>

&lt;div class="xAccordion">
 &lt;input id="id-173926845" type="checkbox">
 &lt;label class="xAccordion_title" for="id-173926845">リポジトリの Issue リストの例 (JSON)&lt;/label>
 &lt;div class="xAccordion_body">
 

&lt;figure class="xCodeBlock">

&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-json" data-lang="json">&lt;span class="line">&lt;span class="cl">&lt;span class="p">[&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/issues/1&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;repository_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;labels_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/issues/1/labels{/name}&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;comments_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/issues/1/comments&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;events_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/issues/1/events&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;html_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://github.com/maku77/sample/issues/1&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">624149172&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;node_id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;MDU6SXNzdWU2MjQxNDkxNzI=&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;number&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;title&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;Issueのタイトル1&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;user&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;login&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;maku77&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">5519503&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;node_id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;MDQ6VXNlcjU1MTk1MDM=&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;avatar_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://avatars2.githubusercontent.com/u/5519503?v=4&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;gravatar_id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;html_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://github.com/maku77&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;followers_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/followers&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;following_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/following{/other_user}&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;gists_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/gists{/gist_id}&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;starred_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/starred{/owner}{/repo}&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;subscriptions_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/subscriptions&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;organizations_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/orgs&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;repos_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/repos&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;events_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/events{/privacy}&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;received_events_url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/users/maku77/received_events&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;type&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;User&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;site_admin&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">false&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">},&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;labels&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">[&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">2086106907&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;node_id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;MDU6TGFiZWwyMDg2MTA2OTA3&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/labels/%E3%83%A9%E3%83%99%E3%83%AB1&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;name&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;ラベル1&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;color&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;23c6bc&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;default&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">false&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;description&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">},&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">2086107046&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;node_id&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;MDU6TGFiZWwyMDg2MTA3MDQ2&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;url&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;https://api.github.com/repos/maku77/sample/labels/%E3%83%A9%E3%83%99%E3%83%AB2&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;name&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;ラベル2&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;color&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;f9bbdb&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;default&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">false&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;description&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">],&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;state&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;open&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;locked&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">false&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;assignee&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">null&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;assignees&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">[&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">],&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;milestone&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">null&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;comments&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">0&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;created_at&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;2020-05-25T08:51:56Z&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;updated_at&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;2020-05-25T08:51:56Z&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;closed_at&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">null&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;author_association&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;OWNER&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;body&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;Issueのコメント1&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">]&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


 &lt;/div>
&lt;/div>


&lt;p>Issue が 1 つも存在しない場合は、空の配列を示す JSON 文字列 (&lt;code>[]&lt;/code>) が返されます。
リポジトリ名などの指定が間違っている場合は、次のような JSON 文字列が返されます（レスポンスのステータスコードは &lt;code>HTTP/1.1 404 Not Found&lt;/code> です）。&lt;/p></description></item><item><title>Git コマンドで使用するプロキシを設定する (http.proxy)</title><link>https://maku77.github.io/p/uyzcgd8/</link><pubDate>Wed, 04 Mar 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/uyzcgd8/</guid><description>&lt;h2 id="git-のプロキシを設定する">Git のプロキシを設定する&lt;/h2>
&lt;p>会社のオフィスなどのプロキシ環境内から &lt;code>git&lt;/code> コマンドを使用する場合は、次のようにプロキシサーバーのアドレス（とポート番号）を設定できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">git コマンド用のプロキシ設定&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global http.proxy http://proxy.example.com:8080
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>ちゃんと設定されているかは、&lt;code>git config --list&lt;/code> コマンド、あるいは設定ファイルを直接見て確認できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">プロキシ設定の確認 (1)&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global --list
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">http.proxy=http://proxy.examle.com:8080
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">プロキシ設定の確認 (2)&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> cat ~/.gitconfig
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">[http]
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	proxy = http://proxy.examle.com:8080
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="git-のプロキシ設定を削除する">Git のプロキシ設定を削除する&lt;/h2>
&lt;p>プロキシ設定を削除するには、&lt;code>git config --unset&lt;/code> コマンドを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global --unset http.proxy
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;ul>
&lt;li>参考: &lt;a href="../p/4pad93a/">git config で設定を削除する (&lt;code>config --unset&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="git-のプロキシ設定を一時的に無効にする">Git のプロキシ設定を一時的に無効にする&lt;/h2>
&lt;p>リモートワークなどで、一時的にプロキシ設定を無視して &lt;code>git&lt;/code> コマンドを使用したい場合は、&lt;strong>&lt;code>-c&lt;/code>&lt;/strong> オプションで &lt;code>http.proxy&lt;/code> 設定を次のように空っぽに指定してやります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git -c http.proxy&lt;span class="o">=&lt;/span> pull
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>http.proxy=&lt;/code> の後ろには空であることを示すスペースがあることに注意してください（もっと明示的に &lt;code>http.proxy=&amp;quot;&amp;quot;&lt;/code> としてもOK）。
また、&lt;code>-c&lt;/code> オプションは、&lt;code>git&lt;/code> コマンドのグローバルなオプションなので、&lt;code>git&lt;/code> の直後くらいに入力しないといけないことに注意してください。&lt;/p></description></item><item><title>Git の設定値がどのファイルで設定されているか調べる (config --show-origin)</title><link>https://maku77.github.io/p/msds6iv/</link><pubDate>Wed, 04 Mar 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/msds6iv/</guid><description>&lt;p>Git 設定のスコープには、&lt;strong>&lt;code>local&lt;/code>&lt;/strong>/&lt;strong>&lt;code>global&lt;/code>&lt;/strong>/&lt;strong>&lt;code>system&lt;/code>&lt;/strong> などがあり、ある設定値がどのスコープ（設定ファイル）で行われているか混乱することがあります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/af7q7n3/">Git 設定のスコープ (&lt;code>local&lt;/code>/&lt;code>global&lt;/code>/&lt;code>system&lt;/code>) を理解する&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>そのような場合は、&lt;strong>&lt;code>git config --list --show-origin&lt;/code>&lt;/strong> コマンドを使うと、それぞれの設定値がどのファイルで設定されいるものなのか調べることができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --list --show-origin
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:/Users/maku/.gitconfig user.name=maku77
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:/Users/maku/.gitconfig user.email=xxx@gmail.com
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:/Users/maku/.gitconfig core.editor=vim
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:/Users/maku/.gitconfig push.default=simple
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.repositoryformatversion=0
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.filemode=true
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.bare=false
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.logallrefupdates=true
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.ignorecase=true
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config core.precomposeunicode=true
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config remote.origin.url=https://github.com/maku77/maku77.github.io.git
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config remote.origin.fetch=+refs/heads/*:refs/remotes/origin/*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config branch.master.remote=origin
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:.git/config branch.master.merge=refs/heads/master
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>この例の場合、&lt;code>/Users/maku/.gitconfig&lt;/code> で始まるものが global スコープ、&lt;code>.git/config&lt;/code> で始まるものが local スコープで設定されていることが分かります。
ここでは、&lt;code>--list&lt;/code> オプションですべての設定値を列挙していますが、次のように個別の設定値に関して調べることもできます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --show-origin user.name
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">file:/Users/maku/.gitconfig user.name=maku77
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: 別の Git リポジトリの内容を強引にマージする</title><link>https://maku77.github.io/p/odgmdp2/</link><pubDate>Tue, 18 Feb 2020 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/odgmdp2/</guid><description>&lt;h2 id="何をしたいか">何をしたいか？&lt;/h2>
&lt;p>本質的に内容の異なるリポジトリ &lt;code>App1&lt;/code> と &lt;code>App2&lt;/code> があったとして、&lt;code>App1&lt;/code> に &lt;code>App2&lt;/code> 上のファイル群を強引にマージしてしまう方法です。
ここでは、&lt;code>App2&lt;/code> の &lt;code>master&lt;/code> ブランチの内容を、&lt;code>App1&lt;/code> の &lt;code>master&lt;/code> ブランチ上に取り込んでみます。&lt;/p>
&lt;p>ユースケースとしては、&lt;code>App2&lt;/code> に新しいアプリや機能が実装されていて、それらを &lt;code>App1&lt;/code> にも徐々に採用していきたいので、とりあえずソースコードだけ &lt;code>App1&lt;/code> 側に取り込んでしまおう、というケースが考えられます。&lt;/p>
&lt;p>残念ながらコミットログはうまくマージできないっぽいです（一括でコードをコミットしたというログになってしまう）。
これだと、ファイル群を丸ごとコピペするのとあまり変わらないような気もしますが、ファイルのコピペだと、同じ名前のファイルがあった場合に、上書き保存することになってしまいます。
下記の手順のようにちゃんと &lt;code>git merge&lt;/code> を使うことで、同一ファイル内のコンフリクトを解消しながらマージ作業を進めることができます。&lt;/p>
&lt;h2 id="2-つの-git-リポジトリをマージする">2 つの Git リポジトリをマージする&lt;/h2>
&lt;h3 id="step-1-作業用のリポジトリをクローンする">Step 1: 作業用のリポジトリをクローンする&lt;/h3>
&lt;p>まず、マージ作業を行う側（マージ先）の &lt;code>App1&lt;/code> リポジトリをクローンして、&lt;code>master&lt;/code> ブランチをチェックアウトしておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone https://github.com/yourname/App1.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> App1
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout master （クローン直後は必要なし）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h3 id="step-2-取り込むリポジトリを参照できるようにする">Step 2: 取り込むリポジトリを参照できるようにする&lt;/h3>
&lt;p>&lt;code>App1&lt;/code> リポジトリから、&lt;strong>リモートリポジトリ&lt;/strong>として &lt;code>App2&lt;/code> を参照できるようにします。
次のように、&lt;strong>&lt;code>git remote add&lt;/code>&lt;/strong> コマンドを使って、リモートリポジトリ名（ここでは &lt;code>App2&lt;/code>）を作成しておくと、その後は長い URL を入力しなくても、&lt;code>App2&lt;/code> という名前でリモートリポジトリを参照できるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> App1
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git remote add App2 https://github.com/yourname/repo2.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>リモートリポジトリが追加されているか確認しておきましょう。&lt;/p></description></item><item><title>GitHub の REST API を使用する</title><link>https://maku77.github.io/p/mprs3v6/</link><pubDate>Wed, 30 Jan 2019 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/mprs3v6/</guid><description>&lt;h2 id="github-の-rest-api-でできること">GitHub の REST API でできること&lt;/h2>
&lt;p>GitHub は Web API (REST API) を提供しており、コマンドラインや任意のアプリケーションから利用することで、GitHub の操作を自動化することができます。
Linux の &lt;code>curl&lt;/code> コマンドや、スクリプト（Python や Ruby など）から簡単に利用できるので、Organization やリポジトリを管理する立場にある人はぜひ使ってみてください。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://developer.github.com/v3/">GitHub - REST API v3&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>GitHub の REST API を使うと、例えば、下記のような処理を自動化できます。&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://developer.github.com/v3/users/">Users&lt;/a>
&lt;ul>
&lt;li>指定したユーザの詳細情報を取得する&lt;/li>
&lt;li>二要素認証が設定できていないユーザのリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="https://developer.github.com/v3/repos/">Repositories&lt;/a>
&lt;ul>
&lt;li>指定したオーナー（ユーザー or 組織）のリポジトリのリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="https://developer.github.com/v3/orgs/members/">Organizations/Members&lt;/a>
&lt;ul>
&lt;li>指定した組織のユーザーリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="https://developer.github.com/v3/pulls/">Pull Requests&lt;/a>
&lt;ul>
&lt;li>Pull Request のリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="https://developer.github.com/v3/issues/">Issues&lt;/a>
&lt;ul>
&lt;li>指定したリポジトリの Issue のリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="https://developer.github.com/v3/projects/">Projects&lt;/a>
&lt;ul>
&lt;li>指定したリポジトリのプロジェクト（かんばん）のリストを取得する&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;p>ただし、アプリケーションに GitHub との連携機能を本格的に組み込む場合は、REST API ではなく GraphQL API を使うをことをお勧めします。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="https://maku.blog/p/ubkahpw/">GitHub GraphQL API のクエリ例｜まくろぐ&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="api-トークンを発行する">API トークンを発行する&lt;/h2>
&lt;p>REST API を呼び出すには、&lt;strong>API トークン (HTTPS Access Token)&lt;/strong> が必要です。
プライベートリポジトリに関する情報を取得するには、そのリポジトリへのアクセス権限を持つユーザが作成した API トークンを使用して REST API を呼び出す必要があります。&lt;/p></description></item><item><title>Gitメモ: あるバージョン間の変更行数（追加＆削除）を集計する (git diff --stat, git log --numstat)</title><link>https://maku77.github.io/p/3cjkt3v/</link><pubDate>Tue, 04 Dec 2018 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/3cjkt3v/</guid><description>&lt;p>下記の説明では、バージョン 1、バージョン 2 のリリースコミットとして、タグ &lt;code>v1&lt;/code>、&lt;code>v2&lt;/code> が設定されているとします。
バージョン間の変更行数を調べるには、主に &lt;strong>&lt;code>git diff --stat&lt;/code>&lt;/strong> を使った方法と、&lt;strong>&lt;code>git log --numstat&lt;/code>&lt;/strong> を使った方法があります。&lt;/p>
&lt;ul>
&lt;li>&lt;b>&lt;code>git diff --stat v1 v2&lt;/code>&lt;/b>&lt;br>v1 と v2 のスナップショット間で差分を取って変更行数を集計したいとき&lt;/li>
&lt;li>&lt;b>&lt;code>git log --numstat v1..v2&lt;/code>&lt;/b>&lt;br>v1 から v2 までの各コミットの修正行数を集計したいとき&lt;/li>
&lt;/ul>
&lt;h2 id="1-git-diff---stat-を使う方法">(1) git diff --stat を使う方法&lt;/h2>
&lt;p>リビジョン間（&lt;code>v1&lt;/code> と &lt;code>v2&lt;/code>）の変更行数を調べるには、下記のように &lt;code>git diff&lt;/code> に &lt;strong>&lt;code>--stat&lt;/code>&lt;/strong> オプションを付けて実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --stat v1 v2
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> .../path/to/filename/Sample1.java | 5 +
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> .../path/to/filename/Sample2.java | 5 +
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> .../path/to/filename/Sample3.java | 4 +-
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> .../path/to/filename/Sample4.java | 26 -
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> ...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 22 files changed, 327 insertions(+), 655 deletions(-)
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ファイル単位の増減に加え、出力の最終行に、追加された行数 (insertions) と、削除された行数 (deletions) が表示されます。&lt;/p></description></item><item><title>Gitメモ: ユーザーごとのコミット統計を出力する (git shortlog)</title><link>https://maku77.github.io/p/ocva5fh/</link><pubDate>Tue, 04 Dec 2018 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ocva5fh/</guid><description>&lt;h2 id="git-shortlog-の使い方">git shortlog の使い方&lt;/h2>
&lt;p>&lt;strong>&lt;code>git shortlog&lt;/code>&lt;/strong> コマンドを使用すると、ユーザごとのコミットを一覧で表示することができます。&lt;/p>
&lt;p>何もオプションを付けないで実行すると、ユーザ名のアルファベット順に、コミット数とそれぞれのコミットのタイトルが列挙されます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git shortlog
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Rei Ayanami (3):
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Fix xxx yyy zzz
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Encapsulate FileCache in ProgramFacade
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Remove unnecessary comments in utility classes
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Shinji Ikari (4):
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Remove currentTimeMillis() calls from FocusPosition
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Package information for com.example.foo
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Apply LRU caching for event information
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> Emphasize FPS display
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>デフォルトではアルファベット順にユーザが表示されますが、&lt;strong>&lt;code>-n&lt;/code>&lt;/strong> オプションを付けるとコミット数の多いユーザ順に表示されます。
さらに、&lt;strong>&lt;code>-s&lt;/code>&lt;/strong> オプションを付けることで、コミット数だけが表示されるようになります。
&lt;strong>&lt;code>-e&lt;/code>&lt;/strong> オプションは、名前に加えてメールアドレスも表示します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git shortlog -nse
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 6183 Doppo Orochi &amp;lt;doppo-orochi@example.com&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 1323 Kaoru Hanayama &amp;lt;kaoru-hanayama@example.com&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 1201 Kaiou Retsu &amp;lt;kaiou-retsu@example.com&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 1043 Katsumi Orochi &amp;lt;katsumi-orochi@example.com&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 832 Izou Motobe &amp;lt;izou-motobe@example.com&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これを見れば、誰がたくさんコミットしているかを調べることができます。&lt;/p></description></item><item><title>GitHub Pages で独自の 404 ページを用意する</title><link>https://maku77.github.io/p/bf2nban/</link><pubDate>Wed, 12 Sep 2018 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/bf2nban/</guid><description>&lt;p>GitHub Pages で存在しないページにアクセスすると、デフォルトでは GitHub が用意している 404 ページが表示されます。
Web サイトのルートパスに &lt;code>404.html&lt;/code> ファイルを配置すると、独自の 404 ページを表示することができます。
ここでは、Jekyll のページ生成の仕組みを使って、独自の 404 ページを作成する方法を説明します。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="" width="600" height="367" src="../p/bf2nban/img-001_hu6614139173509778516.png" alt="/p/bf2nban/img-001.png" />
 &lt;figcaption>図: GitHub Pages で独自の 404 ページを表示した例&lt;/figcaption>
&lt;/figure>

&lt;h2 id="コンテンツファイル-404md-の作成">コンテンツファイル (&lt;code>404.md&lt;/code>) の作成&lt;/h2>
&lt;p>まず、プロジェクトのルートディレクトリに &lt;strong>&lt;code>404.md&lt;/code>&lt;/strong> を作成し、&lt;code>404.html&lt;/code> というページが出力されるようにします。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">404.md&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-yaml" data-lang="yaml">&lt;span class="line">&lt;span class="cl">&lt;span class="nn">---&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">title&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;404&amp;#34;&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">layout&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;404&amp;#34;&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">permalink&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;404.html&amp;#34;&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nn">---&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>表示内容に関しては、404 ページ専用のレイアウト (&lt;strong>&lt;code>_layouts/404.html&lt;/code>&lt;/strong>) の方で定義することにします。
よって、上記の Markdown ファイルでは、フロントマターで出力設定だけを記述しておきます。&lt;/p>
&lt;h2 id="レイアウトファイル-_layouts404html-の作成">レイアウトファイル (&lt;code>_layouts/404.html&lt;/code>) の作成&lt;/h2>
&lt;p>404 ページの出力内容は特有のレイアウトになることが多いので、専用のレイアウトファイルで作成するのがオススメです。
ここでは、&lt;strong>&lt;code>_layouts/404.html&lt;/code>&lt;/strong> というパスにファイルを作成します（&lt;code>404.md&lt;/code> のフロントマターで指定したレイアウト名に合わせてください）。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">_layouts/404.html の記述例&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-html" data-lang="html">&lt;span class="line">&lt;span class="cl">&lt;span class="cp">&amp;lt;!DOCTYPE html&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">html&lt;/span> &lt;span class="na">lang&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;ja&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">head&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">meta&lt;/span> &lt;span class="na">charset&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;UTF-8&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">meta&lt;/span> &lt;span class="na">property&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;og:title&amp;#34;&lt;/span> &lt;span class="na">content&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;{{ page.title }} | {{ site.name }}&amp;#34;&lt;/span> &lt;span class="p">/&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">title&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>{{ page.title }} | {{ site.name }}&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">title&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">link&lt;/span> &lt;span class="na">rel&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;icon&amp;#34;&lt;/span> &lt;span class="na">sizes&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;16x16 32x32 48x48 64x64&amp;#34;&lt;/span> &lt;span class="na">href&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;/assets/img/favicon/favicon.ico&amp;#34;&lt;/span> &lt;span class="p">/&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">link&lt;/span> &lt;span class="na">rel&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;apple-touch-icon-precomposed&amp;#34;&lt;/span> &lt;span class="na">href&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;/assets/img/favicon/favicon-152.png&amp;#34;&lt;/span> &lt;span class="p">/&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">style&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">body&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">background&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mh">#eee&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">margin&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="kt">em&lt;/span> &lt;span class="kc">auto&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">text-align&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">center&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">.&lt;/span>&lt;span class="nc">content&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">width&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">500&lt;/span>&lt;span class="kt">px&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">margin&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">0&lt;/span> &lt;span class="kc">auto&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">h1&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">font-size&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">3&lt;/span>&lt;span class="kt">rem&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">margin&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="kt">em&lt;/span> &lt;span class="mi">0&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">a&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">color&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mh">#36f&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">font-weight&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">bolder&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">text-decoration&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">none&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">a&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="nd">hover&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">color&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">white&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">background&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mh">#36f&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">img&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">width&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="mi">100&lt;/span>&lt;span class="kt">px&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">vertical-align&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="kc">bottom&lt;/span>&lt;span class="p">;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">style&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> {% if jekyll.environment != &amp;#34;development&amp;#34; %}
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> {% include analytics.html %}
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> {% endif %}
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">head&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">body&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">h1&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>{{ page.title }}&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">h1&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">div&lt;/span> &lt;span class="na">class&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;content&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>ページが見つかりませんでした。&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&amp;lt;&lt;/span>&lt;span class="nt">a&lt;/span> &lt;span class="na">href&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;{{ site.url }}&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>{{ site.name }}のトップページ&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">a&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>へ移動するか、&lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">br&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> 下記から検索してみてください。&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>{% include search-bar.html %}&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&amp;lt;&lt;/span>&lt;span class="nt">a&lt;/span> &lt;span class="na">href&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;{{ site.url }}&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&amp;lt;&lt;/span>&lt;span class="nt">img&lt;/span> &lt;span class="na">src&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;{{ site.logo }}&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&amp;lt;/&lt;/span>&lt;span class="nt">a&lt;/span>&lt;span class="p">&amp;gt;&amp;lt;/&lt;/span>&lt;span class="nt">p&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">div&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">body&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">html&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の例では、Google Analytics 用のコード (&lt;code>_includes/analytics.html&lt;/code>) も挿入するようにしています。
こうしておけば、404 ページへのアクセスも解析できるようになるので便利です。
Google Analytics を使用していないのであれば外してください。
また、404 ページからトップページへ飛ばすだけでは不親切なので、Google カスタム検索のためのフォーム (&lt;code>_includes/search-bar.html&lt;/code>) も配置するようにしています。
このあたりは適宜調整してください。&lt;/p></description></item><item><title>Gitメモ: Git やり直し: 最近のコミットを歴史から抹消する (git rebase -i)</title><link>https://maku77.github.io/p/hmcakev/</link><pubDate>Fri, 31 Aug 2018 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/hmcakev/</guid><description>&lt;p>最近実行したコミットを、完全にコミットログ上から削除してしまいたい場合は、&lt;code>git rebase&lt;/code> を下記のような感じで実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git rebase -i HEAD~5 &lt;span class="c1"># 最近の５つのコミットから選んで削除したいとき&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git rebase -i 40facd &lt;span class="c1"># 指定したコミットから最新までの中から選んで削除したいとき&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記のように実行すると、下記のようにコミットのリストがエディタ上に表示されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>pick 26b81e6 How to show dependencies of subprojects
pick 86f2132 Correct ffmpeg options
pick 892197c Vim articles
pick 492c84c How to use Sass in Hugo
pick fa18ad8 Correct Markdown format

# Rebase d67f4e7..fa18ad8 onto d67f4e7 (5 command(s))
#
# Commands:
# p, pick = use commit
# r, reword = use commit, but edit the commit message
# e, edit = use commit, but stop for amending
# s, squash = use commit, but meld into previous commit
# f, fixup = like &amp;#34;squash&amp;#34;, but discard this commit&amp;#39;s log message
# x, exec = run command (the rest of the line) using shell
# d, drop = remove commit
#
# These lines can be re-ordered; they are executed from top to bottom.
#
# If you remove a line here THAT COMMIT WILL BE LOST.
#
# However, if you remove everything, the rebase will be aborted.
#
# Note that empty commits are commented out
&lt;/code>&lt;/pre>&lt;p>歴史から削除したいコミットの行頭に書かれている、&lt;code>pick&lt;/code> という部分を、&lt;code>drop&lt;/code> に書き換えて保存すれば、それらのコミットがなかったことになります。&lt;/p></description></item><item><title>git プロトコルではなく強制的に https プロトコルで git clone するようにする (url.xxx)</title><link>https://maku77.github.io/p/k84zg8x/</link><pubDate>Tue, 22 Aug 2017 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/k84zg8x/</guid><description>&lt;p>プロキシ環境化で作業している場合は、git プロトコルによるフェッチを実行したときも、強制的に https プロトコルを使用してフェッチしたいことがあります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone git://github.com/xxx/yyy.git &lt;span class="c1"># のようなコマンドの実行を、&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone https://github.com/xxx/yyy.git &lt;span class="c1"># というコマンドで実行したことにしたい&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>このようなときは、下記の設定を行っておくと、自動的に https プロトコル経由でのフェッチに置き換えてくれるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global url.https://github.com/.insteadOf git://github.com/
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>自分でフェッチ先の URL を &lt;code>https://...&lt;/code> と指定できるようなケースではこのような設定は必要ないのですが、何らかのツールが内部で git プロトコルを使用する前提になっているときにはこの設定を活用できます。
例えば、Ruby の Bundler などでモジュールをインストールするときには、内部で勝手に git プロトコル経由でフェッチすることがあり、プロキシ環境下からアクセスできないことがあります。
このような場合は、上記のような設定で強制的に https 経由のアクセスに置き換えることで https プロキシ経由でアクセスできるようになります。&lt;/p></description></item><item><title>git diff や git status での日本語の文字化けを防ぐ (core.page, core.quotepath)</title><link>https://maku77.github.io/p/cj2uie9/</link><pubDate>Sat, 19 Aug 2017 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/cj2uie9/</guid><description>&lt;h2 id="git-diff-での文字化け">git diff での文字化け&lt;/h2>
&lt;p>&lt;code>git diff&lt;/code> の出力に日本語が含まれているときに、下記のように２桁の数値ばかりの表示に文字化けしてしまうことがあります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">diff --git a/README.txt b/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">index 0005c4e..5d6ea9c 100644
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">--- a/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+++ b/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">@@ -1 +1 @@
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">-&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;82&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;84&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;86&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;88&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;8A&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;8B&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;8D&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;8F&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;91&amp;gt;&amp;lt;E3&amp;gt;&amp;lt;81&amp;gt;&amp;lt;93&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>このような場合は、ページャの設定 (&lt;strong>&lt;code>core.pager&lt;/code>&lt;/strong>) で下記のように &lt;strong>&lt;code>LESSCHARSET&lt;/code>&lt;/strong> を設定するようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.pager &lt;span class="s2">&amp;#34;LESSCHARSET=utf-8 less&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>あるいは、システムの環境変数で Git のページャ設定を行っておくこともできます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">~/.bashrc&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="nb">export&lt;/span> &lt;span class="nv">GIT_PAGER&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;LESSCHARSET=utf-8 less&amp;#34;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>これで正しく日本語の差分が表示されるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">diff --git a/README.txt b/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">index 0005c4e..5d6ea9c 100644
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">--- a/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+++ b/README.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">@@ -1 +1 @@
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">-あいうえお
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+かきくけこ
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Windows の場合は、環境変数 &lt;strong>&lt;code>LANG&lt;/code>&lt;/strong> を設定しておくとよいようです。&lt;/p>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; set LANG=ja_JP.UTF-8
&lt;/code>&lt;/pre>&lt;h2 id="git-status-での文字化け">git status での文字化け&lt;/h2>
&lt;p>&lt;code>git status&lt;/code> の表示で、日本語のファイル名が下記のように文字化けしてしまうことがあります。&lt;/p></description></item><item><title>Git で他の人が作成したブランチ上で作業する</title><link>https://maku77.github.io/p/ewvaoe3/</link><pubDate>Tue, 05 Apr 2016 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ewvaoe3/</guid><description>&lt;p>GitHub などの共有リポジトリを使用して複数のメンバーで開発を進めている場合、他のメンバーが作成したブランチ（&lt;code>git push&lt;/code> されたブランチ）をチェックアウトして共同作業したいことがあります。
そのような場合は、下記のような手順でローカルブランチを作成して編集作業を進めます。&lt;/p>
&lt;ol>
&lt;li>共有リポジトリに push されているブランチをフェッチする (&lt;code>git fetch&lt;/code>)&lt;/li>
&lt;li>作業用のローカルブランチを作成する (&lt;code>git branch&lt;/code>)&lt;/li>
&lt;li>ローカルブランチをチェックアウトして編集作業を行う (&lt;code>git checkout&lt;/code>, &lt;code>git commit&lt;/code>)&lt;/li>
&lt;li>変更内容を共有リポジトリに push する (&lt;code>git push&lt;/code>)&lt;/li>
&lt;/ol>
&lt;h2 id="共有リポジトリに-push-されているブランチをフェッチする">共有リポジトリに push されているブランチをフェッチする&lt;/h2>
&lt;p>まずは、他の人が共有リポジトリ上に作成したブランチをローカルに持ってくる必要があります（&lt;code>--prune&lt;/code> オプションを付けると、共有リポジトリ側で削除されているブランチをローカルからも削除してくれます）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git fetch --prune
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>リモートトラッキングブランチの一覧を表示し、対象のブランチをうまく fetch してこれたかを確認しておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -r
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/HEAD -&amp;gt; origin/main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/change-date-format
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/fix-lint-warnings
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/main
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>リモートトラッキングブランチ名は、&lt;code>&amp;lt;リポジトリ&amp;gt;/&amp;lt;ブランチ&amp;gt;&lt;/code> という構成になっていますが、clone 元のリポジトリ名は &lt;code>origin&lt;/code> というエイリアス名で指定できるようになっています。&lt;/p>
&lt;h2 id="作業用のローカルブランチを作成する">作業用のローカルブランチを作成する&lt;/h2>
&lt;p>リモートトラッキングブランチは、あくまでリモートリポジトリ側の内容を追跡するためのものなので、ここに直接修正を加えていくことはできません。
そこで、リモートトラッキングブランチをベースにして、ローカルブランチを作成する必要があります。
次の例では、リモートトラッキングブランチ &lt;code>origin/change-date-format&lt;/code> と同名の &lt;code>change-date-format&lt;/code> というローカルブランチを作成しています。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch change-date-format origin/change-date-format
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Branch &amp;#39;change-date-format&amp;#39; set up to track remote branch &amp;#39;change-date-format&amp;#39; from &amp;#39;origin&amp;#39;.
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>次のようにして、正しくローカルブランチが作成されていることを確認しておきます。&lt;/p></description></item><item><title>GitHub の Markdown でメールアドレスへのリンクを記述する</title><link>https://maku77.github.io/p/xikzhtr/</link><pubDate>Thu, 04 Feb 2016 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xikzhtr/</guid><description>&lt;p>Markdown ファイルの中で、E メールアドレスのリンクを記述するには下記のようにします。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">Markdown ファイル抜粋&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-md" data-lang="md">&lt;span class="line">&lt;span class="cl">[&lt;span class="nt">表示名&lt;/span>](&lt;span class="na">&amp;lt;mailto:xxx@example.com&amp;gt;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記のように &lt;code>&amp;lt;&lt;/code> と &lt;code>&amp;gt;&lt;/code> で囲んだメールアドレスは、出力される HTML の中では、下記のように実体参照による表現に変換されるため、スパムメールの対策にもなります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">変換後の HTML&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-html" data-lang="html">&lt;span class="line">&lt;span class="cl">&lt;span class="p">&amp;lt;&lt;/span>&lt;span class="nt">a&lt;/span> &lt;span class="na">href&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s">&amp;#34;&amp;amp;#109;&amp;amp;#097;&amp;amp;#105;&amp;amp;#108;&amp;amp;#116;&amp;amp;#111;:&amp;amp;#120;&amp;amp;#120;&amp;amp;#120;&amp;amp;#064;&amp;amp;#101;&amp;amp;#120;&amp;amp;#097;&amp;amp;#109;&amp;amp;#112;&amp;amp;#108;&amp;amp;#101;&amp;amp;#046;&amp;amp;#099;&amp;amp;#111;&amp;amp;#109;&amp;#34;&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>表示名&lt;span class="p">&amp;lt;/&lt;/span>&lt;span class="nt">a&lt;/span>&lt;span class="p">&amp;gt;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>実際の表示結果は次のような感じになります。&lt;/p>
&lt;p>&lt;a href="mailto:xxx@example.com">表示名&lt;/a>&lt;/p></description></item><item><title>Gitメモ: git diff で単語単位で差分を表示する (git diff --word-diff)</title><link>https://maku77.github.io/p/xav46kn/</link><pubDate>Wed, 16 Dec 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xav46kn/</guid><description>&lt;p>&lt;code>git diff&lt;/code> コマンドは、デフォルトでは行単位の差分を表示しますが、&lt;strong>&lt;code>--word-diff&lt;/code>&lt;/strong> オプションを指定することで、単語単位で差分を表示することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --word-diff
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">[- 削除した部分 -]{+ 追加した部分 +}
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>デフォルトの出力フォーマットでは上記のように、削除した部分と追加した部分を特殊な記号で挟んで表示します。&lt;/p>
&lt;p>&lt;code>--word-diff&lt;/code> オプションのパラメータに &lt;code>color&lt;/code> と指定すれば、色の違いだけで追加部分と削除部分を表示してくれるようになります。
端末上で diff の出力に色を付けることができる場合は、こちらの方が見やすいと思います。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --word-diff&lt;span class="o">=&lt;/span>color
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: git diff で改行コードの違いを無視する (git diff -w)</title><link>https://maku77.github.io/p/zm7ezhx/</link><pubDate>Wed, 16 Dec 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/zm7ezhx/</guid><description>&lt;p>&lt;code>git diff&lt;/code> コマンドを実行するときに、改行コードが違うだけの行を検出しないようにするには、&lt;code>-w&lt;/code> オプションを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff -w
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: リポジトリ内のディレクトリを別リポジトリに切り出す</title><link>https://maku77.github.io/p/vaqzwih/</link><pubDate>Thu, 22 Oct 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/vaqzwih/</guid><description>&lt;h2 id="やりたいこと">やりたいこと&lt;/h2>
&lt;p>リポジトリの一部のディレクトリを、コミット履歴ごと別のリポジトリに切り出したいケースがあります。
例えば、以下のようなリポジトリ構成で、&lt;code>aaa&lt;/code> ディレクトリを独立したリポジトリに分離する場合です。&lt;/p>
&lt;pre tabindex="0">&lt;code>old リポジトリ（移行元）
├── aaa/ ← これを別リポジトリとして切り出したい
├── bbb/
└── README.md
&lt;/code>&lt;/pre>&lt;p>切り出し後は、&lt;code>aaa&lt;/code> ディレクトリの中身がルートになる新しいリポジトリ &lt;code>new&lt;/code> ができます。
&lt;code>aaa&lt;/code> 内のファイルに関するコミット履歴もそのまま引き継がれます。&lt;/p>
&lt;h2 id="手順">手順&lt;/h2>
&lt;h3 id="前提条件">前提条件&lt;/h3>
&lt;ul>
&lt;li>GitHub 上に新しい空のリポジトリ &lt;code>new&lt;/code> を作成済み&lt;/li>
&lt;li>古いリポジトリのバックアップ (&lt;code>git clone&lt;/code>) を取得済み&lt;/li>
&lt;/ul>
&lt;p>この手順では &lt;code>git filter-branch&lt;/code> を使って古いリポジトリの履歴を書き換えるため、&lt;strong>作業前に必ず古いリポジトリのバックアップを取ってください&lt;/strong>。&lt;/p>
&lt;h3 id="ステップ-1-古いリポジトリをミラークローンする">ステップ 1: 古いリポジトリをミラークローンする&lt;/h3>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone --mirror https://github.com/maku77/old.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> old.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>--mirror&lt;/code> を付けることで、すべてのブランチ・タグを含む完全なコピーを取得します（ただし、作業ディレクトリの存在しないベアリポジトリとなります）。&lt;/p>
&lt;h3 id="ステップ-2-特定ディレクトリをルートに書き換える">ステップ 2: 特定ディレクトリをルートに書き換える&lt;/h3>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git filter-branch -f --subdirectory-filter aaa -- --all
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>--subdirectory-filter aaa&lt;/code> は、&lt;code>aaa&lt;/code> ディレクトリ内のファイルだけを残し、それをリポジトリのルートに移動させるオプションです。
&lt;code>aaa&lt;/code> 以外のファイルに関するコミット履歴は自動的に削除されます。
末尾の &lt;code>-- --all&lt;/code> は、すべてのブランチに対してこの操作を適用することを意味します。&lt;/p>
&lt;h3 id="ステップ-3-新しいリポジトリとして-push-する">ステップ 3: 新しいリポジトリとして push する&lt;/h3>
&lt;p>リモートの向き先を新しいリポジトリに変更して push します。&lt;/p></description></item><item><title>Git で管理するシェルスクリプトに実行権限（パーミッション）を付ける（chmod +x 相当） (git update-index)</title><link>https://maku77.github.io/p/xwxdv6y/</link><pubDate>Wed, 21 Oct 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xwxdv6y/</guid><description>&lt;p>Git はファイルの所有者情報なのどメタ情報を管理しませんが、&lt;strong>ファイルの実行可能ビットだけは記録する&lt;/strong> ようになっています（Linux の &lt;code>chmod +x&lt;/code> で付加するビット）。&lt;/p>
&lt;p>例えば、Windows 上で Linux のシェルスクリプトを作成していて、Git リポジトリにコミットするときは、次のように &lt;strong>&lt;code>git update-index&lt;/code>&lt;/strong> コマンドで実行可能ビットを付加してからコミットしなければいけません。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">シェルスクリプトに実行可能ビットを付ける&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git update-index --add --chmod&lt;span class="o">=&lt;/span>+x &amp;lt;filename&amp;gt;
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>Linux 上で作業する場合は、&lt;code>chmod +x&lt;/code> で付加した情報が反映されるので、通常通り &lt;code>git add&lt;/code> でファイルを追加するだけで OK です。&lt;/p></description></item><item><title>GitHub の新しい Organization パーミッションモデル（2015年10月リリース）について理解する</title><link>https://maku77.github.io/p/494ernf/</link><pubDate>Thu, 01 Oct 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/494ernf/</guid><description>&lt;p>GitHub の Organization アカウントの新しいパーミッションモデルが公開されました。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="https://github.blog/2015-09-30-new-organization-permissions-now-available/">New organization permissions now available - The GitHub Blog&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="チームメンテナの設定が可能">チームメンテナの設定が可能&lt;/h2>
&lt;p>チーム内のメンバーに対して、&lt;strong>team maintainer&lt;/strong> という役割を設定できるようになりました（念願！）。
これまでは、各チームに対するメンバー追加を行うときは、Organization の Owners メンバーが追加作業を行う必要がありましたが、この機能によって、&lt;strong>チーム内のメンバー管理は、そのチームの代表者&lt;/strong> に任せることができるようになります。&lt;/p>
&lt;p>チーム内のメンバーを team maintainer に割り当てるには次のようにします。&lt;/p>
&lt;ol>
&lt;li>Organization のトップページから Teams を選択&lt;/li>
&lt;li>対象のチームを選択&lt;/li>
&lt;li>チームメンテナに設定したいメンバーを選択し、"Change role&amp;hellip;" → "Maintainer" を選択&lt;/li>
&lt;/ol>
&lt;p>チームメンテナとなったメンバーは、自分の管理するチームに対して、自由にメンバーを参加させたり外したりできます。
チーム名の変更や、チーム内メンバーのチームメンテナへの昇格も自由に行えるようになるため、ある程度責任のあるリーダクラスの人をチームメンテナとして設定すべきです。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="https://help.github.com/articles/giving-team-maintainer-permissions-to-an-organization-member-early-access-program/">Giving "team maintainer" permissions to an organization member&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="organization-内のリポジトリに対するデフォルトパーミッションを設定可能">Organization 内のリポジトリに対するデフォルトパーミッションを設定可能&lt;/h2>
&lt;p>デフォルトパーミッションとして Read/Write 権限を付加しておくことで、Organization 内のメンバー全員に対して、すべてのプロジェクト（リポジトリ）の参照権限、コミット権限を付けることができます。&lt;/p>
&lt;p>例えば、直接コミットできるメンバーはプロジェクトごとに限定したいけれど、コードの参照や PullRequest くらいは全メンバーに許可したい、ということが簡単に実現できます（これまでは、プロジェクトに対する参照権限を与えるために、対象のユーザを Read 権限のあるチームに追加させ、そのチームをプロジェクトに登録する、という分かりにくい作業が必要でした）。&lt;/p>
&lt;p>メンバー全員に全リポジトリの参照権限を付けるには以下のようにします。&lt;/p>
&lt;ol>
&lt;li>Organization のトップページから Settings を選択&lt;/li>
&lt;li>Member privileges を選択&lt;/li>
&lt;li>Base permissions の項目を No permission から Read に変更&lt;/li>
&lt;/ol>
&lt;p>全メンバーが全プロジェクトの開発に関わっているような Organization では、デフォルトのパーミッションとして Write を設定しておけば、アクセス権限管理のためにチームを作成する必要がなくなります。&lt;/p></description></item><item><title>Gitメモ: Git やり直し: コミット履歴を書き換えてディレクトリ階層を変更する (git filter-branch)</title><link>https://maku77.github.io/p/iut5h68/</link><pubDate>Mon, 29 Jun 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/iut5h68/</guid><description>&lt;p>例えば、Git で下記のようなディレクトリのプロジェクトを管理しているとします。&lt;/p>
&lt;pre tabindex="0">&lt;code>+ root
 + proj
 +-- file1
 +-- file2
 +-- file3
&lt;/code>&lt;/pre>&lt;p>この階層構造をひとつずつ上げて、下記のようなディレクトリ構成にしたいとします。&lt;/p>
&lt;pre tabindex="0">&lt;code>+ proj
 +-- file1
 +-- file2
 +-- file3
&lt;/code>&lt;/pre>&lt;p>&lt;code>git filter-branch&lt;/code> コマンドを使用すると、すべてのコミット履歴を書き換えて、もともとプロジェクト全体がこのような構成になっていたとみなすことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git filter-branch -f --subdirectory-filter root -- --all
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git filter-branch&lt;/code> はコミット履歴を書き換えるので、実行には結構時間がかかります。
このコマンドは非常に強力な反面、ディレクトリ構成の互換性が断たれてしまうことに注意してください。
リポジトリ内のブランチの整合性は保たれますが、この変更を既存のリモートリポジトリに push したりすると、内部の整合性が崩れてしまいます。
そのため、&lt;code>git filter-branch&lt;/code> は、&lt;a href="../p/bfxgbf4/">リポジトリの移行&lt;/a>時などに限定して実行すべきです。
現在のリポジトリの内容を書き換えてしまうため、試すときは、テスト用に git clone したプロジェクト内で実行するようにしましょう。&lt;/p>
&lt;h4 id="例-srcgit-の階層構造を変えて-dstgit-として-push">例: src.git の階層構造を変えて dst.git として push&lt;/h4>
&lt;p>下記は、GitHub 上で空のリポジトリ dst.git を作成済みだと仮定しています。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">#&lt;/span>&lt;span class="c1">## 元になるリポジトリを clone して階層構造を変更する&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone --mirror https://github.com/user/src.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> src.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git filter-branch -f --subdirectory-filter root -- --all
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">#&lt;/span>&lt;span class="c1">## 新しいリポジトリとして push&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git remote remove origin
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git remote add origin https://github.com/user/dst.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --all （全ブランチを push）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --tags （全タグを push）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h4 id="参考-git-filter-branch-コマンドのヘルプ">参考: git filter-branch コマンドのヘルプ&lt;/h4>
&lt;ul>
&lt;li>&lt;a href="https://git-scm.com/docs/git-filter-branch">https://git-scm.com/docs/git-filter-branch&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: タグの名前を変更する (git tag new old)</title><link>https://maku77.github.io/p/h4dnw6e/</link><pubDate>Mon, 29 Jun 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/h4dnw6e/</guid><description>&lt;h2 id="ローカルリポジトリでタグの名前を変更する">ローカルリポジトリでタグの名前を変更する&lt;/h2>
&lt;p>Git には直接タグをリネームするコマンドはないため、「新しい名前のタグを作成 → 古いタグを削除」という手順で実現します。&lt;/p>
&lt;p>&lt;code>git tag &amp;lt;新しいタグ名&amp;gt; &amp;lt;古いタグ名&amp;gt;&lt;/code> とすると、古いタグが指しているコミットと同じコミットを指す新しいタグが作成されます。
その後、古いタグを削除すれば、実質的にリネームしたことになります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: タグ old を new にリネームする&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag new old &lt;span class="c1"># old と同じコミットを指す新しいタグ new を作成&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag -d old &lt;span class="c1"># 古いタグ old を削除&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="中央リポジトリに-push-済みのタグをリネームする">中央リポジトリに push 済みのタグをリネームする&lt;/h2>
&lt;p>GitHub などの中央リポジトリ（リモート）に push 済みのタグをリネームする場合は、ローカルだけでなくリモート側のタグも更新する必要があります。
以下の 4 ステップで行います。&lt;/p>
&lt;ol>
&lt;li>ローカルで新しいタグを作成する（古いタグと同じコミットを指すようにする）&lt;/li>
&lt;li>中央リポジトリに新しいタグを push する&lt;/li>
&lt;li>ローカルの古いタグを削除する&lt;/li>
&lt;li>中央リポジトリの古いタグを削除する&lt;/li>
&lt;/ol>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: タグ old を new にリネームする&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag new old &lt;span class="c1"># 1. 新しいタグを作成&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin new &lt;span class="c1"># 2. 新しいタグをリモートに push&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag -d old &lt;span class="c1"># 3. ローカルの古いタグを削除&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin :old &lt;span class="c1"># 4. リモートの古いタグを削除&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>最後の &lt;code>git push origin :old&lt;/code> は、リモートの &lt;code>old&lt;/code> タグを削除するコマンドです。
&lt;code>:old&lt;/code> は &lt;code>:refs/tags/old&lt;/code> の省略形で、「空の内容をリモートの &lt;code>old&lt;/code> に push する＝削除する」という意味になります。
次のように &lt;code>--delete&lt;/code> オプションを使って書くこともできます。&lt;/p></description></item><item><title>Gitメモ: GitHub で git コマンドが Authentication failed になる場合（HTTPS 認証とアクセストークン）</title><link>https://maku77.github.io/p/c7ddt8i/</link><pubDate>Wed, 24 Jun 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/c7ddt8i/</guid><description>&lt;h2 id="症状">症状&lt;/h2>
&lt;p>GitHub に対して &lt;code>git clone&lt;/code> や &lt;code>git push&lt;/code> を HTTPS 経由で実行すると、以下のように認証エラーが発生することがあります。&lt;/p>
&lt;pre tabindex="0">&lt;code>$ git clone https://github.com/xxx/yyy.git
Cloning into &amp;#39;yyy&amp;#39;...
Username for &amp;#39;https://github.com&amp;#39;: maku77
Password for &amp;#39;https://maku77@github.com&amp;#39;: *****
fatal: Authentication failed
&lt;/code>&lt;/pre>&lt;p>これは、GitHub が 2021 年 8 月にパスワードによる HTTPS の Git 認証を廃止したことが原因です。
二要素認証の有無にかかわらず、HTTPS でアクセスする場合は &lt;strong>パーソナルアクセストークン (PAT)&lt;/strong> を使う必要があります。&lt;/p>
&lt;h2 id="対処法-1-パーソナルアクセストークンを使う-https">対処法 1: パーソナルアクセストークンを使う (HTTPS)&lt;/h2>
&lt;p>GitHub の &lt;a href="https://github.com/settings/tokens">Personal access tokens ページ&lt;/a> からアクセストークンを生成し、パスワードの代わりに入力します。&lt;/p>
&lt;ol>
&lt;li>GitHub の &lt;strong>Settings → Developer settings → Personal access tokens → Tokens (classic)&lt;/strong> を開く&lt;/li>
&lt;li>&lt;strong>Generate new token&lt;/strong> をクリック&lt;/li>
&lt;li>必要なスコープ（最低限 &lt;code>repo&lt;/code>）を選択してトークンを生成&lt;/li>
&lt;li>生成されたトークンをコピーしておく（画面を離れると二度と表示されません）&lt;/li>
&lt;/ol>
&lt;p>&lt;code>git clone&lt;/code> や &lt;code>git push&lt;/code> でパスワードを求められたら、このトークンを入力します。&lt;/p></description></item><item><title>Gitメモ: Git やり直し: 直前のコミットのコメントを修正する (git commit --amend)</title><link>https://maku77.github.io/p/wyq4tea/</link><pubDate>Sun, 17 May 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/wyq4tea/</guid><description>&lt;p>直前の &lt;code>git commit&lt;/code> において、コメントを typo してしまった場合などは、下記のようにして手っ取り早く修正することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit --amend
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>とするとエディタ上でコミット内容が表示されるので、コメント部分を修正してエディタを閉じれば OK です。
コメントが一行だけの場合は、下記のようにコマンドラインから直接コメント入力することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit --amend -m &lt;span class="s1">&amp;#39;Introduce a zip code validator&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>GitHub の Organization を構成したときのチームの権限について理解する</title><link>https://maku77.github.io/p/x7hr9z7/</link><pubDate>Thu, 07 May 2015 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/x7hr9z7/</guid><description>&lt;h2 id="github-の-organization-の-4-種類のチーム">GitHub の Organization の 4 種類のチーム&lt;/h2>
&lt;p>GitHub で Organization を作成すると、その下に作成したリポジトリは、Organization 内に作成したチーム単位でアクセス権限を設定することになります。
&lt;a href="https://help.github.com/articles/permission-levels-for-an-organization-repository/">GitHub のサイト&lt;/a> にざっと説明がありますが、分かりにくいのでここで補足します。&lt;/p>
&lt;p>チーム区分としては、&lt;/p>
&lt;ul>
&lt;li>Owners&lt;/li>
&lt;li>Admin&lt;/li>
&lt;li>Write&lt;/li>
&lt;li>Read&lt;/li>
&lt;/ul>
&lt;p>に分かれていて、それぞれ異なる権限を持ちます。&lt;/p>
&lt;h2 id="owners-チームとその権限">Owners チームとその権限&lt;/h2>
&lt;p>Owners はその名の通り organization 自体の管理者チームであり、デフォルトで存在しているものです。
残りの 3 つ（Admin、Write、Read）とは性質が異なり、削除したり、Owners という名前を変更することはできません。
organization を作成した人が、Owners の最初のメンバになります。&lt;/p>
&lt;ul>
&lt;li>Owners 権限
&lt;ul>
&lt;li>デフォルトで存在するチームで、その organization に関してすべての権限を持つ&lt;/li>
&lt;li>Owners に属するメンバは、支払い情報を管理し、クレジットカードなどの登録を行う&lt;/li>
&lt;li>Owners には必ず 1 人以上が所属している必要がある&lt;/li>
&lt;li>organization へのメンバ追加が可能&lt;/li>
&lt;li>リポジトリを作成可能&lt;/li>
&lt;li>チームを作成可能&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;p>残りの Admin、Write、Read というのは、後付で作成したチームに対して割り当てる権限を意味しています。
Owners チームは organization 内でひとつしか存在しませんが、Admin、Write、Read 権限を持つチームは複数作ることが可能です。
チームを作成できるのは Owners のメンバだけです。&lt;/p>
&lt;p>各リポジトリへのアクセス権限は、このチーム単位で参加者 (contributors) を割り当てることで行います。
例えば、my-project というリポジトリの contributors として、team1 (Write 権限)、team2 (Read 権限) を参加させた場合、team1 に参加しているメンバだけが、my-project へのコミット権限を持つことになります。
リポジトリに対してチームを割り当てることができるのは Owners のメンバだけです。&lt;/p></description></item><item><title>Git でブランチやタグの作者を調べる (git for-each-ref)</title><link>https://maku77.github.io/p/o2m2ft7/</link><pubDate>Thu, 18 Sep 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/o2m2ft7/</guid><description>&lt;p>トピックブランチを使った開発手法を適用していると、トピックブランチの削除忘れなどにより、ブランチが散乱してくることがあります。
このような場合は、誰が作成したブランチが放置されているのか調べる必要があります。
下記は、手っ取り早くブランチの作者を調べる方法です。&lt;/p>
&lt;p>まずは、GitHub（中央リポジトリ）上で削除済みのブランチは、ローカルのリモートトラッキングブランチからも削除しておきます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">不要なリモートトラッキングブランチを削除&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git fetch --prune
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>あとは、次のようにすれば、ブランチやタグ (refs) の作者を調べることができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ブランチやタグの作者を調べる&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git &lt;span class="k">for&lt;/span>-each-ref --format&lt;span class="o">=&lt;/span>%&lt;span class="o">(&lt;/span>authorname&lt;span class="o">)&lt;/span>%09%09%&lt;span class="o">(&lt;/span>refname&lt;span class="o">)&lt;/span> &lt;span class="p">|&lt;/span> sort
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">maku refs/heads/main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">maku refs/heads/my-branch
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">john refs/remotes/origin/apply-eslint
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">ripper refs/remotes/origin/gh-pages
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">maku refs/remotes/origin/HEAD
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">maku refs/remotes/origin/main
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>msysGit で terminal is not fully functional と怒られる場合の対策</title><link>https://maku77.github.io/p/coekmmp/</link><pubDate>Tue, 19 Aug 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/coekmmp/</guid><description>&lt;p>Windows の Git クライアントとして &lt;strong>msysGit&lt;/strong> を使っていると、&lt;code>git log&lt;/code> や &lt;code>git diff&lt;/code> を実行したときに以下のような警告が出ることがあります。&lt;/p>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; git log
WARNING: terminal is not fully functional
&lt;/code>&lt;/pre>&lt;p>これを防ぐには、&lt;strong>&lt;code>TERM&lt;/code>&lt;/strong> 環境変数を設定します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-bat" data-lang="bat">&lt;span class="line">&lt;span class="cl">&lt;span class="k">set&lt;/span> &lt;span class="nv">TERM&lt;/span>&lt;span class="p">=&lt;/span>msys
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: 特定バージョンのファイルの内容を確認する (git show, git cat-file -p)</title><link>https://maku77.github.io/p/32b2ttc/</link><pubDate>Thu, 17 Jul 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/32b2ttc/</guid><description>&lt;p>過去のコミット時点でのファイルの内容を確認したいときは、&lt;strong>&lt;code>git show&lt;/code>&lt;/strong> や &lt;strong>&lt;code>git cat-file -p&lt;/code>&lt;/strong> コマンドを使います。
どちらもファイルの中身を表示できますが、用途や動作が少し異なります。&lt;/p>
&lt;h2 id="git-show-でファイルの内容を確認する">&lt;code>git show&lt;/code> でファイルの内容を確認する&lt;/h2>
&lt;p>&lt;strong>&lt;code>git show&lt;/code>&lt;/strong> は、日常的に使いやすい高レベルコマンドです。
&lt;code>&amp;lt;コミットID&amp;gt;:&amp;lt;ファイルパス&amp;gt;&lt;/code> の形式で、特定のコミット時点のファイル内容を表示できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git show 8de2fcd0:main.py
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>出力はページャ（&lt;code>less&lt;/code> など）を通して表示されるため、長いファイルでもスクロールして確認できます。
コミット ID の代わりにブランチ名やタグ名を指定することもできます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git show main:main.py &lt;span class="c1"># main ブランチの main.py を表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git show HEAD~3:config.yml &lt;span class="c1"># 3 つ前のコミットの config.yml を表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>また、&lt;code>git show&lt;/code> にコミット ID だけを指定すると、そのコミットの情報と差分 (diff) を表示します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git show 8de2fcd0 &lt;span class="c1"># コミットの詳細と変更内容を表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="git-cat-file--p-でファイルの内容を確認する">&lt;code>git cat-file -p&lt;/code> でファイルの内容を確認する&lt;/h2>
&lt;p>&lt;strong>&lt;code>git cat-file&lt;/code>&lt;/strong> は、Git の内部オブジェクトを直接操作するための低レベルコマンド（plumbing コマンド）です。
&lt;code>-p&lt;/code> オプションを付けると、オブジェクトの内容を整形して表示します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git cat-file -p 8de2fcd0:main.py
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git show&lt;/code> とは異なり、ページャを通さず標準出力にそのまま出力されるため、スクリプトやパイプラインでの利用に適しています。&lt;/p></description></item><item><title>GitHub でソフトウェア配布用にプロジェクトの Web サイトを作成する (GitHub Pages)</title><link>https://maku77.github.io/p/yujway4/</link><pubDate>Mon, 14 Jul 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/yujway4/</guid><description>&lt;p>GitHub Pages という機能を使用すると、GitHub 上で Web サイトをホスティングできます。GitHub を開発用にだけ使うのであれば、リポジトリだけ公開しておけばよいのですが、作成したソフトウェアを配布したり、マニュアルを公開するのであれば、GitHub Pages 上で通常の Web サイトとして見えるようにしておくのがよいでしょう。&lt;/p>
&lt;h2 id="プロジェクトの-web-サイトを作る">プロジェクトの Web サイトを作る&lt;/h2>
&lt;p>各プロジェクト用の GitHub Pages は非常に簡単に作成できます。
各リポジトリに &lt;strong>&lt;code>gh-pages&lt;/code>&lt;/strong> という名前のブランチを作り、そこに Web サイトのコンテンツ（&lt;code>index.html&lt;/code> など）をコミットすれば、そのプロジェクト用の Web サイトとして見えるようになります。&lt;/p>
&lt;pre tabindex="0">&lt;code>https://&amp;lt;username&amp;gt;.github.io/&amp;lt;repository&amp;gt;/
&lt;/code>&lt;/pre>&lt;p>Organization を作成している場合は、上記の &lt;code>&amp;lt;username&amp;gt;&lt;/code> のところを、&lt;code>&amp;lt;organization&amp;gt;&lt;/code> と読み替えてください。&lt;/p>
&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ GitHub Pages の非公開化&lt;/span>
 &lt;span class="xNote_body">&lt;p>プライベートリポジトリで GitHub Pages (gh-pages) を作ったとしても、Web サイトはインターネット上に公開されてしまうことに注意してください。
プライベートな情報提供サイトを作る場合は、リポジトリの &lt;code>README.md&lt;/code> や Wiki を活用するか、独自で Web サーバを立ち上げる必要があります。&lt;/p>
&lt;p>あるいは、GitHub Enterprise Cloud という高額なプランを導入すれば、非公開の GitHub Pages を作成することができます。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="https://docs.github.com/en/enterprise-cloud@latest/pages/getting-started-with-github-pages/changing-the-visibility-of-your-github-pages-site">Changing the visibility of your GitHub Pages site - GitHub Enterprise Cloud Docs&lt;/a>&lt;/li>
&lt;/ul>
&lt;/span>
&lt;/div>
&lt;h2 id="ユーザ用組織用の-web-サイトを作る">ユーザ用、組織用の Web サイトを作る&lt;/h2>
&lt;p>GitHub Pages では、リポジトリごとの Web サイトだけではなく、ユーザや組織用のトップサイトを作成することも可能です。
その場合は、&lt;code>&amp;lt;username&amp;gt;.github.io&lt;/code> という名前のリポジトリを作成し、その &lt;strong>&lt;code>main&lt;/code>&lt;/strong> ブランチにコンテンツを格納してください（この場合はリポジトリが Web サイト専用になるので、&lt;code>gh-pages&lt;/code> ブランチを作る必要がありません）。
下記のようなアドレスで参照できるようになります。&lt;/p></description></item><item><title>git config で設定すべき一般的な設定項目のまとめ</title><link>https://maku77.github.io/p/rcwaz54/</link><pubDate>Wed, 04 Jun 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/rcwaz54/</guid><description>&lt;p>Git を使い始める前の初期設定としては、多くの場合は、下記のような設定を行っておけばよいでしょう。
改行コードに関する設定部分は、自動変換するかしないかで、どの設定を行うかを判断してください。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-shell" data-lang="shell">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### コミット時のログ設定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global user.name &lt;span class="s2">&amp;#34;Taro Yamada&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global user.email &lt;span class="s2">&amp;#34;taro@example.com&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 便利設定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global color.ui auto &lt;span class="c1"># コマンド出力に色を付ける&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.editor nvim &lt;span class="c1"># git commit 時のエディタ&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 文字化け防止 (git diff)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.pager &lt;span class="s2">&amp;#34;LESSCHARSET=utf-8 less&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 文字化け防止 (git status)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.quotepath &lt;span class="nb">false&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 改行コードの自動変換なし&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.autocrlf &lt;span class="nb">false&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 改行コードの自動変換あり（Windows の場合）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.autocrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># ローカルの改行コード CR+LF&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.safecrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># 余計な改行コード変換を抑制&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.whitespace cr-at-eol &lt;span class="c1"># git diff 時の ^M を抑制&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">### 改行コードの自動変換あり（Mac/Linux の場合）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.autocrlf input &lt;span class="c1"># ローカルの改行コード LF&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git config --global core.safecrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># 余計な改行コード変換を抑制&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>参考&lt;/p></description></item><item><title>Windows の git diff で改行コードが ^M で表示される問題を解決する (core.whitespace)</title><link>https://maku77.github.io/p/uek4bb2/</link><pubDate>Wed, 04 Jun 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/uek4bb2/</guid><description>&lt;p>Windows のターミナル環境において、改行コードが &lt;code>CR&lt;/code>+&lt;code>LF&lt;/code> のファイルを編集している場合、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global color.ui auto
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --system color.diff auto
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>のように、&lt;code>git diff&lt;/code> の出力がカラフルに表示されるようになっていると、行末の &lt;strong>&lt;code>CR&lt;/code>&lt;/strong> がうまく処理できず、&lt;strong>&lt;code>^M&lt;/code>&lt;/strong> と表示されてしまうことがあります。
このような場合は、次のように &lt;strong>&lt;code>core.whitespace&lt;/code>&lt;/strong> 設定を行っておくと &lt;code>^M&lt;/code> の表示を抑制できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.whitespace cr-at-eol
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Git 設定のスコープ (local/global/system) を理解する</title><link>https://maku77.github.io/p/af7q7n3/</link><pubDate>Sun, 25 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/af7q7n3/</guid><description>&lt;h2 id="git-設定の-3-つのスコープ">Git 設定の 3 つのスコープ&lt;/h2>
&lt;p>&lt;code>git config&lt;/code> による設定のスコープは 3 種類あり、スコープが狭くなるほど参照時の優先度は高くなります。
下記はそれぞれのスコープでの設定方法を、優先度の高い順に示しています。
カッコの中のファイル名は、コマンドを実行したときの設定値の保存先です。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --local ... &lt;span class="c1"># 各リポジトリごとの設定 (&amp;lt;repo&amp;gt;/.git/config)（優先度:高）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global ... &lt;span class="c1"># 現在のユーザの共通設定 (~/.gitconfig)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --system ... &lt;span class="c1"># システム内の共通設定 (/etc/gitconfig など)（優先度:低）&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>例えば、global 設定で &lt;code>user.name&lt;/code> が &lt;code>Ichiro&lt;/code> になっていても、local 設定が &lt;code>Jiro&lt;/code> になっていれば、&lt;code>Jiro&lt;/code> の方が優先的に使用されます。
プロジェクト（リポジトリ）ごとに固有の設定をする場合は、local なスコープで設定を行うとよいでしょう。
この場合、プロジェクトの作業ツリーのルートにある &lt;strong>&lt;code>.git/config&lt;/code>&lt;/strong> ファイルに設定が保存されます。&lt;/p>
&lt;p>例えば、次のようにして local 設定の &lt;code>user.name&lt;/code> と &lt;code>user.email&lt;/code> を変更することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --local user.name maku77
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --local user.email maku77@example.com
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記コマンドを実行した後で、&lt;code>&amp;lt;リポジトリ&amp;gt;/.git/config&lt;/code> ファイルを開くと次のように設定情報が保存されていることが分かります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">&amp;lt;リポジトリ&amp;gt;/.git/config（抜粋）&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-ini" data-lang="ini">&lt;span class="line">&lt;span class="cl">&lt;span class="k">[user]&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">	&lt;span class="na">name&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="s">maku77
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s">	email = maku77@example.com&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;ul>
&lt;li>参考: &lt;a href="../p/msds6iv/">Git の設定値がどのファイルで設定されているか調べる (&lt;code>config --show-origin&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="それぞれの設定ファイルをコマンドで開く-git-config---edit">それぞれの設定ファイルをコマンドで開く (&lt;code>git config --edit&lt;/code>)&lt;/h2>
&lt;p>各スコープの設定ファイルは、&lt;strong>&lt;code>git config --edit&lt;/code>&lt;/strong> コマンドで開くことができます。&lt;/p></description></item><item><title>Gitメモ: GitLab からの通知メールを設定する</title><link>https://maku77.github.io/p/ycphr93/</link><pubDate>Tue, 20 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ycphr93/</guid><description>&lt;h2 id="設定ファイルのパス">設定ファイルのパス&lt;/h2>
&lt;p>GitLab サーバからの通知メールの設定は、以下のファイルで行います。&lt;/p>
&lt;ul>
&lt;li>パッケージでインストールした場合
&lt;ul>
&lt;li>&lt;code>/opt/gitlab/embedded/service/gitlab-rails/config/environments/production.rb&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>手動でインストールした場合
&lt;ul>
&lt;li>&lt;code>/home/git/gitlab/config/environments/production.rb&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2 id="sendmail-でメール送信する場合の設定方法">sendmail でメール送信する場合の設定方法&lt;/h2>
&lt;p>GitLab のデフォルト設定では、通知メールはローカルにインストールされた &lt;code>sendmail&lt;/code> で送信されるようになっています。
&lt;code>sendmail&lt;/code> がインストールされていない場合はインストールしておく必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo apt-get install sendmail
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>そして、&lt;code>production.rb&lt;/code> に下記のように記述して有効化します（GitLab 6.8.2 のデフォルト設定）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-ruby" data-lang="ruby">&lt;span class="line">&lt;span class="cl">&lt;span class="n">config&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">action_mailer&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">delivery_method&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="ss">:sendmail&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="n">config&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">action_mailer&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">sendmail_settings&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">location&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s1">&amp;#39;/usr/sbin/sendmail&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">arguments&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s1">&amp;#39;-i -t&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>設定を終えたら、GitLab のサービス群を再起動して完了です。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl restart
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="外部の-smtp-サーバでメール送信する場合の設定方法">外部の SMTP サーバでメール送信する場合の設定方法&lt;/h2>
&lt;p>ホスト PC 内の sendmail を使用するのではなく、外部の SMTP サーバを使用して GitLab の通知メールを送ることもできます。
ここでは、Gmail の SMTP サーバを使用する設定例を示します。
&lt;code>production.rb&lt;/code> に下記のように記述します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-ruby" data-lang="ruby">&lt;span class="line">&lt;span class="cl">&lt;span class="n">config&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">action_mailer&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">delivery_method&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="ss">:smtp&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="n">config&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">action_mailer&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">smtp_settings&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:address&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="s2">&amp;#34;smtp.gmail.com&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:port&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="mi">587&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:domain&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="s1">&amp;#39;gmail.com&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:user_name&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="s1">&amp;#39;sample@sample&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="c1"># 省略可能&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:password&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="s1">&amp;#39;password&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="c1"># 省略可能&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:authentication&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="ss">:plain&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="c1"># 省略可能&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="ss">:enable_starttls_auto&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="kp">true&lt;/span> &lt;span class="c1"># 省略可能&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="メールの送信者情報を変更する">メールの送信者情報を変更する&lt;/h2>
&lt;p>GitLab からの通知メールで使われる送信者アドレスは、以下の場所で設定されています。&lt;/p></description></item><item><title>Gitメモ: Git リポジトリを移行する (git clone --mirror, git push --mirror)</title><link>https://maku77.github.io/p/bfxgbf4/</link><pubDate>Mon, 19 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/bfxgbf4/</guid><description>&lt;p>Git リポジトリを別のサーバに移行するときは、&lt;strong>&lt;code>git clone --mirror&lt;/code>&lt;/strong> を使ってリポジトリの完全なコピーを作成し、新しいリモートに push します。&lt;/p>
&lt;h2 id="--mirror-オプションとは">--mirror オプションとは&lt;/h2>
&lt;p>通常の &lt;code>git clone&lt;/code> は、デフォルトブランチの作業ツリーを含むリポジトリのコピーを作成します。
一方、&lt;strong>&lt;code>git clone --mirror&lt;/code>&lt;/strong> を使うと、すべてのブランチ、タグ、リモート追跡情報などを含むベアリポジトリ（作業ツリーを持たないリポジトリ）として完全なコピーを作成します。&lt;/p>
&lt;p>同様に、&lt;strong>&lt;code>git push --mirror&lt;/code>&lt;/strong> は、ローカルのすべての参照（ブランチ、タグなど）をリモートに反映します。
リモートにだけ存在する参照は削除されるため、ローカルの状態がリモートに完全にコピーされます。&lt;/p>
&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ 注意&lt;/span>
 &lt;span class="xNote_body">&lt;code>git push --mirror&lt;/code> は、移行先リポジトリの既存の参照をすべて上書き・削除します。
移行先として必ず &lt;strong>空のリポジトリ&lt;/strong> を用意してください。&lt;/span>
&lt;/div>
&lt;h2 id="github-内のリポジトリを別のリポジトリへ移行する">GitHub 内のリポジトリを別のリポジトリへ移行する&lt;/h2>
&lt;p>GitHub 内（または異なるサーバ間）でリポジトリを移行する基本的な手順です。
移行先に空のリポジトリ &lt;code>dst.git&lt;/code> を作成済みだと仮定しています。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: src.git から dst.git への移行&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone --mirror https://github.com/user/src.git &lt;span class="c1"># ミラーとしてクローン&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> src.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --mirror https://github.com/user/dst.git &lt;span class="c1"># すべての参照を移行先に push&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>この方法は、GitHub 内での移行だけでなく、異なるサーバ間の移行（例えば GitLab → GitHub）にも使えます。
すべてのブランチ、タグ、コミット履歴がそのまま移行されます。&lt;/p>
&lt;h2 id="github-のリポジトリを社内サーバイントラの-git-サーバへ移行する">GitHub のリポジトリを社内サーバ（イントラ）の Git サーバへ移行する&lt;/h2>
&lt;p>社内サーバに移行する場合は、まず GitHub のリポジトリをミラークローンし、それを社内サーバに配置します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">1. GitHub からミラークローンして ZIP にバックアップ&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone --mirror git@github.com:maku/proj.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> zip -r proj.zip proj.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">2. 社内サーバ上で ZIP を展開して共有リポジトリとして配置&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> unzip proj.zip
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>&lt;code>git clone --mirror&lt;/code> で作成されたディレクトリはベアリポジトリなので、そのまま共有リポジトリとして使用できます。
あとは、各メンバーがこのリポジトリを &lt;code>clone&lt;/code> すれば開発を開始できます。&lt;/p></description></item><item><title>Gitメモ: GitLab リポジトリの Clone URL として表示されるホスト名を変更する</title><link>https://maku77.github.io/p/b9fmgxq/</link><pubDate>Mon, 19 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/b9fmgxq/</guid><description>&lt;p>GitLab の Clone URL などに表示されているアドレス内のホスト名は、設定ファイル &lt;code>gitlab.yml&lt;/code> に記述されたものが使用されます。
この設定ファイルは、パッケージを使って GitLab をインストールした場合は、以下の場所にあります。&lt;/p>
&lt;ul>
&lt;li>&lt;code>/opt/gitlab/embedded/service/gitlab-rails/config/gitlab.yml&lt;/code>&lt;/li>
&lt;/ul>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">gitlab.yml の抜粋&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-yaml" data-lang="yaml">&lt;span class="line">&lt;span class="cl">&lt;span class="nn">...&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">host&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="l">YourHostName &lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="c"># これを変更&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>変更後、以下のように GitLab 関連のサービス群を再起動すれば、新しいホスト名が反映されます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl restart
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: GitLab のデータをバックアップ／リストアする</title><link>https://maku77.github.io/p/df778jg/</link><pubDate>Sat, 17 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/df778jg/</guid><description>&lt;h2 id="gitlab-データのアーカイブ">GitLab データのアーカイブ&lt;/h2>
&lt;p>以下のようにすると、バックアップファイルが作成されます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-rake gitlab:backup:create
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">done
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ファイル名のプレフィックスとしてタイムスタンプが付くため、前回のバックアップファイルが上書きされることはありません。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo ls /var/opt/gitlab/backups
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">1402312561_gitlab_backup.tar
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="適切なメディアへバックアップ">適切なメディアへバックアップ&lt;/h2>
&lt;p>&lt;code>/var/opt/gitlab/backups&lt;/code> に作成されたアーカイブファイルは、適切な場所に退避しておきましょう。
下記は HOME ディレクトリにコピーする例ですが、できれば、サーバ PC が故障したときに備えて外部のメディアなどにバックアップしておくことをお勧めします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> mkdir -p ~/gitlab_backup
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo cp -u /var/opt/gitlab/backups/1402312561_gitlab_backup.tar ~/gitlab_backup/
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>以下のようなシェルスクリプトを作っておくのもよいでしょう。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">gitlab-backup.sh&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-shell" data-lang="shell">&lt;span class="line">&lt;span class="cl">&lt;span class="cp">#!/bin/bash
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="cp">&lt;/span>sudo gitlab-rake gitlab:backup:create
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">mkdir -p ~/gitlab_backup
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">sudo sh -c &lt;span class="s1">&amp;#39;cp -u /var/opt/gitlab/backups/*.tar ~/gitlab_backup/&amp;#39;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: GitLab が使用する Unicorn 用のポート番号を変更する</title><link>https://maku77.github.io/p/fhyfx7p/</link><pubDate>Fri, 16 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/fhyfx7p/</guid><description>&lt;p>GitLab 内部では、Rails サーバの Unicorn を使用しています。
このサーバはデフォルトで &lt;code>8080&lt;/code> ポートで待ち受けるため、別の用途で &lt;code>8080&lt;/code> ポートを使う Web サーバを立てていたりすると、GitLab にアクセスしたときに &lt;strong>HTTP 502 (Bad Gateway)&lt;/strong> のエラーが発生します。
Unicorn の使用するポート番号を変更するには、以下のファイルを修正します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">/var/opt/gitlab/gitlab-rails/etc/unicorn.rb&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-ruby" data-lang="ruby">&lt;span class="line">&lt;span class="cl">&lt;span class="n">listen&lt;/span> &lt;span class="s2">&amp;#34;127.0.0.1:8080&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="ss">:tcp_nopush&lt;/span> &lt;span class="o">=&amp;gt;&lt;/span> &lt;span class="kp">true&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の &lt;code>8080&lt;/code> というところを &lt;code>8090&lt;/code> などに変更します。&lt;/p>
&lt;p>さらに、各クライアントからの SSH 接続などを処理する &lt;code>gitlab-shell&lt;/code> が、上記のサーバが提供する Web API を使用していたりするので、&lt;code>gitlab-shell&lt;/code> の設定も書き換える必要があります。
これをやっておかないと、&lt;code>git clone&lt;/code> するときに、fatal error が発生して &lt;strong>Access denied&lt;/strong> と言われてしまいます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">/opt/gitlab/embedded/service/gitlab-shell/config.yml&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-yaml" data-lang="yaml">&lt;span class="line">&lt;span class="cl">&lt;span class="nt">gitlab_url&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;http://localhost:8080&amp;#34;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の &lt;code>8080&lt;/code> というところを &lt;code>8090&lt;/code> などに変更します。&lt;/p>
&lt;p>あとは、GitLab のサービス群を再起動すれば設定が反映されます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl restart
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: GitLab サーバのアクセスログを調べる</title><link>https://maku77.github.io/p/nzhf2bv/</link><pubDate>Fri, 16 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/nzhf2bv/</guid><description>&lt;h2 id="gitlab-サーバ関連のログファイルの場所">GitLab サーバ関連のログファイルの場所&lt;/h2>
&lt;p>GitLab サーバのログファイルは、下記のようなディレクトリに出力されるようになっています。&lt;/p>
&lt;pre tabindex="0">&lt;code>/var/log/gitlab/gitlab-rails/
/var/log/gitlab/gitlab-shell/
/var/log/gitlab/nginx/
/var/log/gitlab/postgresql/
/var/log/gitlab/redis/
/var/log/gitlab/sidekiq/
/var/log/gitlab/unicorn/
&lt;/code>&lt;/pre>&lt;p>これらのログの一部は、Admin 権限の付いている GitLab ユーザであれば、ブラウザ上で &lt;strong>Admin area&lt;/strong> の &lt;strong>Logs&lt;/strong> タブから確認することができます。&lt;/p>
&lt;h2 id="nginx-サーバへのアクセスログ">nginx サーバへのアクセスログ&lt;/h2>
&lt;p>GitLab を deb パッケージでインストールした場合は、GitLab ウェブサーバ (nginx) へのアクセスログは以下のファイルに記録されています。&lt;/p>
&lt;pre tabindex="0">&lt;code>/var/log/gitlab/nginx/gitlab_access.log
/var/log/gitlab/nginx/gitlab_error.log
&lt;/code>&lt;/pre>&lt;p>上記のパス設定は、以下のファイルで行われています。&lt;/p>
&lt;pre tabindex="0">&lt;code>/var/opt/gitlab/nginx/etc/gitlab-http.conf
&lt;/code>&lt;/pre>&lt;h2 id="実行された-git-コマンドのログ-gitlab-shelllog">実行された Git コマンドのログ (gitlab-shell.log)&lt;/h2>
&lt;p>下記にログファイルの位置が設定されています。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">/opt/gitlab/embedded/service/gitlab-shell/config.yml&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-yaml" data-lang="yaml">&lt;span class="line">&lt;span class="cl">&lt;span class="c"># Log file.&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="c"># Default is gitlab-shell.log in the root directory.&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="nt">log_file&lt;/span>&lt;span class="p">:&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="s2">&amp;#34;/var/log/gitlab/gitlab-shell/gitlab-shell.log&amp;#34;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: GitLab 用 nginx サーバの設定ファイルの場所</title><link>https://maku77.github.io/p/bqqhyr2/</link><pubDate>Fri, 16 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/bqqhyr2/</guid><description>&lt;p>パッケージを利用して GitLab をインストールした場合、GitLab 用 nginx の設定ファイルは以下の位置に配置されます。&lt;/p>
&lt;ul>
&lt;li>&lt;code>/var/opt/gitlab/nginx/etc/nginx.conf&lt;/code>&lt;/li>
&lt;li>&lt;code>/var/opt/gitlab/nginx/etc/gitlab-http.conf&lt;/code>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: GitLab 関連サービスを起動／停止する</title><link>https://maku77.github.io/p/9hybaqx/</link><pubDate>Fri, 16 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/9hybaqx/</guid><description>&lt;p>GitLab に関連するサービスは &lt;code>gitlab-ctl&lt;/code> コマンドを使用して起動／停止の制御を行うことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl start
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl stop
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl restart
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: 起動中の GitLab 関連サービスを調べる</title><link>https://maku77.github.io/p/5672eis/</link><pubDate>Thu, 15 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/5672eis/</guid><description>&lt;p>&lt;code>gitlab-ctl service-list&lt;/code> や &lt;code>gitlab-ctl status&lt;/code> コマンドを使用すると、起動している GitLab 関連のサービスの一覧や状態を確認できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">GitLab 関連サービスの一覧を表示&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl service-list
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">nginx*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">postgresql*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">redis*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">sidekiq*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">unicorn*
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">GitLab 関連サービスの動作状態を確認&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl status
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">run: nginx: (pid 11059) 597s; run: log: (pid 11058) 597s
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">run: postgresql: (pid 10971) 632s; run: log: (pid 10970) 632s
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">run: redis: (pid 10890) 638s; run: log: (pid 10889) 638s
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">run: sidekiq: (pid 11043) 599s; run: log: (pid 11042) 599s
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">run: unicorn: (pid 11031) 601s; run: log: (pid 11030) 601s
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>実際に &lt;code>ps&lt;/code> コマンドで確認してみると、GitLab に関連するプロセスがたくさん起動していることが分かります。&lt;/p></description></item><item><title>（旧）GitHub のプランと GitHub ライクなツールの比較など（GitHub と GitLab の比較）</title><link>https://maku77.github.io/p/x63is5h/</link><pubDate>Wed, 14 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/x63is5h/</guid><description>&lt;h2 id="github">GitHub&lt;/h2>
&lt;h3 id="public-repository-と-private-repository-プラン">Public repository と Private repository プラン&lt;/h3>
&lt;ul>
&lt;li>GitHub は公開リポジトリのみであれば無料で使用できるが、非公開にするには有料。&lt;/li>
&lt;li>10 repos で $25/month&lt;/li>
&lt;li>20 repos で $50/month&lt;/li>
&lt;/ul>
&lt;h3 id="github-enterprise社内サーバ用">GitHub Enterprise（社内サーバ用）&lt;/h3>
&lt;ul>
&lt;li>GitHub 社が提供している、GitHub の機能をまるごと含めた VM。&lt;/li>
&lt;li>サーバは別途用意する必要あり。&lt;/li>
&lt;li>社内で GitHub の機能を使ってコード管理したい場合などに有用。&lt;/li>
&lt;li>若干高い。20 person で $5,000/year&lt;/li>
&lt;/ul>
&lt;h2 id="gitlab">GitLab&lt;/h2>
&lt;h3 id="特徴">特徴&lt;/h3>
&lt;ul>
&lt;li>一番メジャーなオープンソースの GitHub クローン&lt;/li>
&lt;li>実装は Ruby on Rails. (+ PostgreSQL, Redis, Nginx, Unicorn, etc.)&lt;/li>
&lt;li>インストールは比較的容易。&lt;/li>
&lt;li>Pull Request もどき有り (Merge Request)。ブランチ間のマージも可能。&lt;/li>
&lt;li>LDAP 連携。ActiveDirectory 連携。&lt;/li>
&lt;/ul>
&lt;h3 id="github-より便利なところ">GitHub より便利なところ&lt;/h3>
&lt;ul>
&lt;li>ユーザに対して Master 権限と Developer 権限を分けて設定することで、master ブランチへマージできる人を制限できる。これにより、チーム内開発において、トピックブランチを使用した開発を強制できるようになる（GitHub フローなど）。&lt;/li>
&lt;li>MergeRequest のコメントで各ユーザが &lt;code>+1&lt;/code> と記述しておくと、MergeRequest のページにその合計数が表示される。これを利用して、「&lt;code>+3&lt;/code> 溜まったらトピックブランチをマージしてよい」といった運用を行うことができる。&lt;/li>
&lt;/ul>
&lt;h2 id="gitorious">Gitorious&lt;/h2>
&lt;h3 id="特徴-1">特徴&lt;/h3>
&lt;ul>
&lt;li>若干独自路線気味の GitHub クローン&lt;/li>
&lt;li>実装は Ruby on Rails.&lt;/li>
&lt;li>インストール、サーバ管理に難あり（モジュール依存が多い）。&lt;/li>
&lt;li>Pull Request もどき有り (Merge Request)。ブランチ間のマージ機能がないので、GitHub Flow のような開発方法は行えない（2014-05-14 現在）。&lt;/li>
&lt;li>権限＆Roll制御。ユーザごとに push 可能なリポジトリを制御可能。&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: git-daemon でリポジトリを読み取り専用で公開する</title><link>https://maku77.github.io/p/kax48rz/</link><pubDate>Wed, 14 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/kax48rz/</guid><description>&lt;h2 id="git-daemon-ですべてのリポジトリを公開する">git-daemon ですべてのリポジトリを公開する&lt;/h2>
&lt;p>&lt;strong>git-daemon&lt;/strong> を使用すると、特定の共有リポジトリを読み取り専用で簡単に公開できます。
読み取り専用というのは、&lt;code>git clone&lt;/code>、&lt;code>git fetch&lt;/code>、&lt;code>git pull&lt;/code> はできるけど、&lt;code>git push&lt;/code> はできないことを意味します。
例として、&lt;code>/var/git&lt;/code> 以下に次のようなリポジトリが作成済みであるとします。&lt;/p>
&lt;pre tabindex="0">&lt;code>/var/git/repo1.git
/var/git/repo2.git
&lt;/code>&lt;/pre>&lt;p>これらのリポジトリをすべて公開するには、以下のように git-daemon を起動します。
リポジトリの格納されているディレクトリのパスは、絶対パスで指定する必要があります。
当然ですが、git-daemon を起動するプロセスは、公開するリポジトリへのアクセス権限を持っている必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git daemon --export-all /var/git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これにより、git-daemon はポート番号 9418 (&lt;code>DEFAULT_GIT_PORT&lt;/code>) での待ち受けを開始し、各クライアントから &lt;code>git clone&lt;/code> できるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone git://192.168.0.1/var/git/repo1.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>（参考）git-daemon のヘルプは以下のように確認できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> man git-daemon
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git &lt;span class="nb">help&lt;/span> daemon
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="ベースパスを指定してリポジトリの-clone-url-を短くする">ベースパスを指定してリポジトリの clone URL を短くする&lt;/h2>
&lt;p>git-daemon を起動する際に、&lt;code>--base-path&lt;/code> オプションを指定しておくと、&lt;code>git clone&lt;/code> に使用する URL のパスから、指定した部分のプレフィックスを除くことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git daemon --export-all --base-path&lt;span class="o">=&lt;/span>/var/git /var/git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これによって、&lt;code>/var/git/repo1.git&lt;/code> にあるリポジトリに以下のようにアクセスできるようになります。
clone URL から、&lt;code>/var/git&lt;/code> というプレフィックスを省略することができています。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone git://192.168.0.1/repo1.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="指定したリポジトリのみを-git-daemon-で公開する">指定したリポジトリのみを git-daemon で公開する&lt;/h2>
&lt;p>&lt;code>git daemon&lt;/code> を起動するときに、&lt;code>--export-all&lt;/code> オプションを指定すると、指定したディレクトリ以下のリポジトリをすべて公開しますが、指定したリポジトリのみを git-daemon の公開対象にすることもできます。
公開したいリポジトリのルートに、&lt;code>git-daemon-export-ok&lt;/code> というファイルを作成することで、git-daemon による公開対象であることを示します。&lt;/p></description></item><item><title>Gitメモ: GitLab をインストールする</title><link>https://maku77.github.io/p/4mjwu34/</link><pubDate>Wed, 14 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/4mjwu34/</guid><description>&lt;p>ここでは、一番楽なパッケージ (GitLab 6.8.2) を使ったインストールを行います。
ダウンロードには時間がかかるけど、インストールは簡単に数分で終わります。&lt;/p>
&lt;ul>
&lt;li>確認済み Version
&lt;ul>
&lt;li>Ubuntu 13.10 + GitLab 6.8.2&lt;/li>
&lt;li>Ubuntu 12.04 + GitLab 6.8.2&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2 id="deb-パッケージによる-gitlab-のインストール">Deb パッケージによる GitLab のインストール&lt;/h2>
&lt;ul>
&lt;li>&lt;a href="https://www.gitlab.com/downloads/">https://www.gitlab.com/downloads/&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>から、Ubuntu 用の deb パッケージをダウンロードして、以下のようにインストールします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo dpkg -i gitlab_6.8.2-omnibus-1_amd64.deb
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>あるいは、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gdebi gitlab_6.8.2-omnibus-1_amd64.deb
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Thank you for installing GitLab!
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">You can configure GitLab for your system by running the following command:
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">sudo gitlab-ctl reconfigure
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>最後に設定しろと出るので設定を実行します（一分強で終わりました）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> sudo gitlab-ctl reconfigure
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Chef Client finished, 112/120 resources updated in 72.205821345 seconds
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">gitlab Reconfigured!
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Web ブラウザで &lt;code>http://localhost/&lt;/code> にアクセスすると、以下のようにログイン画面が表示されるはずです。
楽ちん！&lt;/p></description></item><item><title>Gitメモ: 共有リポジトリを作成する (git init --bare, git clone --bare)</title><link>https://maku77.github.io/p/yaocjjs/</link><pubDate>Tue, 13 May 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/yaocjjs/</guid><description>&lt;h2 id="ベアリポジトリを作成する">ベアリポジトリを作成する&lt;/h2>
&lt;p>中央サーバとして公開する Git リポジトリは、通常、&lt;strong>working ツリーを持たないベアリポジトリ (bare repository)&lt;/strong> として作成します。
通常の作業用リポジトリ（ノンベア）では、ルートディレクトリに &lt;code>.git&lt;/code> ディレクトリが作成され、その中に管理情報ファイルが作成されますが、ベアリポジトリでは、ルートディレクトリ直下に、それらのファイルが生成されます。
ここでは、&lt;code>myrepo.git&lt;/code> というディレクトリ名でリポジトリを作成します。
ベアリポジトリの名前には、慣例としてサフィックスに &lt;code>.git&lt;/code> を付けます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git init --bare myrepo.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これで、&lt;code>git clone&lt;/code> 可能な共有リポジトリができました。
以下のように clone することができます（空っぽなので warning が出ています）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone myrepo.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Cloning into &amp;#39;myrepo&amp;#39;...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">done.
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">warning: You appear to have cloned an empty repository.
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>最初のファイルをコミットして、共有リポジトリへ push してみます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">echo&lt;/span> hoge &amp;gt; hoge.txt
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add hoge.txt
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;First commit.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin master
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>初回の &lt;code>git push&lt;/code> では、push 先のリモートリポジトリと、push するブランチを指定する必要があります。&lt;/p></description></item><item><title>Windows でパーミッションの違いで git diff が表示されてしまうのを防ぐ (core.filemode)</title><link>https://maku77.github.io/p/ijepy8e/</link><pubDate>Tue, 22 Apr 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ijepy8e/</guid><description>&lt;p>Linux と Windows ではファイルのパーミッションの扱いが異なるので、Linux で作成したスクリプトファイル（パーミッション: &lt;code>0755&lt;/code>）を、Windows で &lt;code>git diff&lt;/code> すると、パーミッションが &lt;code>0644&lt;/code> に変わっているという結果が出てしまったりします。
これを防ぐには、Windows 側の &lt;code>git config&lt;/code> で、&lt;strong>&lt;code>core.filemode&lt;/code>&lt;/strong> を &lt;strong>&lt;code>false&lt;/code>&lt;/strong> に設定します。&lt;/p>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; git config --global core.filemode false
&lt;/code>&lt;/pre>&lt;p>うまくいかない場合は、ローカルスコープの設定（リポジトリごとの設定）で &lt;code>core.filemode&lt;/code> が &lt;code>true&lt;/code> になっている可能性があります。
以下のように一度設定をクリアしてから行うとうまくいくはずです。&lt;/p>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; git config --local --unset core.filemode
&lt;/code>&lt;/pre>
&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">参考: core.fileMode の説明&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl">core.fileMode
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">	If false, the executable bit differences between the index and the
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">	working copy are ignored; useful on broken filesystems like FAT.
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">	See git-update-index(1). True by default.&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Git でコミット、チェックアウト時に改行コードを自動変換する (core.autocrlf, core.safecrlf)</title><link>https://maku77.github.io/p/efmfnuy/</link><pubDate>Fri, 21 Mar 2014 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/efmfnuy/</guid><description>&lt;p>Git の設定 (&lt;code>git config&lt;/code>) で、&lt;strong>&lt;code>core.autocrlf&lt;/code>&lt;/strong> や &lt;strong>&lt;code>core.safecrlf&lt;/code>&lt;/strong> を設定しておくと、コミットやチェックアウト時に改行コードを自動変換することができます。
変換設定として、下記のような設定を行うことができます。&lt;/p>
&lt;ul>
&lt;li>&lt;code>core.autocrlf&lt;/code>
&lt;ul>
&lt;li>&lt;strong>&lt;code>false&lt;/code>&lt;/strong>: コミット時、チェックアウト時に改行コードの変換を行わない&lt;/li>
&lt;li>&lt;strong>&lt;code>true&lt;/code>&lt;/strong>: コミット時に CRLF→LF の変換を行い、チェックアウト時に LF→CRLF の返還を行う。&lt;/li>
&lt;li>&lt;strong>&lt;code>input&lt;/code>&lt;/strong>: コミット時に CRLF→LF の変換を行い、チェックアウト時には変換を行わない。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;code>core.safecrlf&lt;/code>
&lt;ul>
&lt;li>&lt;strong>&lt;code>true&lt;/code>&lt;/strong>: ファイル内に複数の改行コードが混じっている場合に自動変換を行わない。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;p>おすすめの設定は以下の通りです。
このように設定しておくと、Git サーバー側では &lt;code>LF&lt;/code> で統一して管理され、チェックアウト後は各 OS の標準的な改行コード（Windows は CRLF、macOS は LF）でファイルを扱えます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">Windows の場合&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.autocrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># コミット時: CRLF=&amp;gt;LF チェックアウト時: LF=&amp;gt;CRLF&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.safecrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># ファイル内に複数の改行コードが混じっている場合に自動変換をしない&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.whitespace cr-at-eol &lt;span class="c1"># git diff 時の ^M を抑制&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">macOS/Linux の場合&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.autocrlf input &lt;span class="c1"># コミット時: CRLF=&amp;gt;LF チェックアウト時: 変換なし&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.safecrlf &lt;span class="nb">true&lt;/span> &lt;span class="c1"># ファイル内に複数の改行コードが混じっている場合に自動変換をしない&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>改行コード関連の変換を一切行ってほしくない場合は、単純に &lt;code>core.autocrlf&lt;/code> を &lt;code>false&lt;/code> に設定します。
複雑な構成管理を行っている環境では、&lt;strong>結局この設定が一番安全だったりします&lt;/strong>。&lt;/p></description></item><item><title>Gitメモ: コミットに対してタグをつけて中央リポジトリにプッシュする (git tag)</title><link>https://maku77.github.io/p/y2cmv5d/</link><pubDate>Mon, 08 Aug 2011 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/y2cmv5d/</guid><description>&lt;h2 id="コミットにタグを付ける">コミットにタグを付ける&lt;/h2>
&lt;p>&lt;strong>&lt;code>git tag&lt;/code>&lt;/strong> コマンドで任意のコミットに対してタグを付けておくと、そのコミットに対して分かりやすい名前でアクセスできるようになります。
例えば、アプリケーションのリリース時には、&lt;code>v1.0.0&lt;/code> のような &lt;a href="https://semver.org/lang/ja/">セマンティックバージョニング&lt;/a> に基づいたタグを付けるのが一般的です。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: コミット fd01de57 に対してタグ v1.0.0 をつける&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag v1.0.0 fd01de57
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>コミット ID を省略すると、現在チェックアウトしているブランチの最新コミット (&lt;code>HEAD&lt;/code>) に対してタグが付けられます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag v1.0.0
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>タグの一覧は下記のように参照することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">v1.0.0
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">v1.0.1
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">v1.0.2
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="タグを削除する">タグを削除する&lt;/h2>
&lt;p>必要のなくなったタグは &lt;strong>&lt;code>-d&lt;/code>&lt;/strong> オプションを使って削除できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: タグ v1.0.0-draft を削除する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git tag -d v1.0.0-draft
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="タグを中央リポジトリに-push-する">タグを中央リポジトリに push する&lt;/h2>
&lt;p>ローカルで作成したタグ情報を GitHub などの中央リポジトリに反映するには、&lt;strong>&lt;code>git push&lt;/code>&lt;/strong> でタグ情報をプッシュしておく必要があります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: タグ v1.0.0 を push する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin refs/tags/v1.0.0
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: すべてのタグを push する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --tags
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --tags origin
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --tags origin master
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="参考">参考&lt;/h2>
&lt;ul>
&lt;li>&lt;a href="../p/h4dnw6e/">タグをリネームする&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: 別ブランチへの複数のコミットを1つのコミットとしてスカッシュマージする (git merge --squash)</title><link>https://maku77.github.io/p/6naegzu/</link><pubDate>Thu, 12 May 2011 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/6naegzu/</guid><description>&lt;h2 id="複数のコミットを１コミットにしてマージ">複数のコミットを１コミットにしてマージ&lt;/h2>
&lt;p>例えば、mytopic ブランチに複数のコミットが入っているとして、この複数のコミットを１つのコミットとして master ブランチにマージするには以下のようにして &lt;strong>squash merge（スカッシュマージ、圧縮マージ）&lt;/strong> を行います。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout master
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge --squash mytopic
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記のようにすると、mytopic 上で行われたすべてのコミットの修正内容が、現在チェックアウトしているブランチ上でステージングされた状態になります。
あとは &lt;code>git commit&lt;/code> を実行すれば、それらの内容を１つのコミットとして取り込むことができます。
コミット時には、自動的にどのコミットを squash merge したのかを示すコメントが生成されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>Squashed commit of the following:
...コミット１
...コミット２
...コミット３
&lt;/code>&lt;/pre>&lt;p>squash merge したということは、おそらく１つのコミットとして扱いたかった理由があるのでしょうから、このコメントは適切ではないはずです。
適切にコメントを修正してコミットすれば squash merge の完了です。&lt;/p>
&lt;h2 id="squash-merge-の注意点">squash merge の注意点&lt;/h2>
&lt;p>squash merge を実行した場合、Git 内部ではブランチ間のマージは行われていない扱いとなります（全然別物の修正がコミットされたかのように扱われる）。
コミット履歴を辿って調査することが困難になるので、日常的な使用はあまりお勧めできません。&lt;/p>
&lt;p>取り込み元のブランチ自体はマージされずに残ったままの状態になるので、混乱しないように気を付けてください。
例えば、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge --squash mytopic
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>として mytopic 上での修正内容を取り込んだ後で、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge mytopic
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>と実行すると、mytopic ブランチで行われた個々のコミットのマージが実行されてしまうことになります（同じ内容をマージしようとする）。
実際には squash merge した時点で変更内容は取り込み済みなので、コードの変更が発生しないマージ用のコミットが作成されるだけなのですが、&lt;code>git log&lt;/code> の表示が分かりにくくなるという弊害があります。
後々の混乱を防ぐためにも、squash merge したら、マージ元になったブランチは削除してしまった方がよいでしょう。
逆に、ここで削除できないようなブランチであれば、squash merge による修正の取り込みは避けるべきです。&lt;/p></description></item><item><title>Gitメモ: Git / git diff の出力で相対パスを使うようにする (git diff --relative)</title><link>https://maku77.github.io/p/w266n3s/</link><pubDate>Wed, 09 Feb 2011 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/w266n3s/</guid><description>&lt;p>&lt;code>git diff&lt;/code> の出力に含まれるファイルのパスは、デフォルトでは .git ディレクトリのあるディレクトリからのパスになります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">git diff のデフォルトの振る舞い&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff system/core/init/init.c
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">diff --git a/android/system/core/init/init.c b/android/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">index eb5db89..98878f2 100644
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">--- a/android/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+++ b/android/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">@@ -1385,7 +1385,11 @@ int main(int argc, char **argv)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>カレントディレクトリからの相対パスで出力したい場合は、オプションに &lt;strong>&lt;code>--relative&lt;/code>&lt;/strong> を指定します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">相対パスで表示&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --relative system/core/init/init.c
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">diff --git a/system/core/init/init.c b/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">index eb5db89..98878f2 100644
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">--- a/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+++ b/system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">@@ -1385,7 +1385,11 @@ int main(int argc, char **argv)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>さらに、&lt;code>a/&lt;/code> や &lt;code>b/&lt;/code> のプレフィックスを付けないようにするには、オプション &lt;strong>&lt;code>--no-prefix&lt;/code>&lt;/strong> を指定します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">プレフィックスを省略&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --relative --no-prefix system/core/init/init.c
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">diff --git system/core/init/init.c system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">index eb5db89..98878f2 100644
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">--- system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">+++ system/core/init/init.c
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">@@ -1385,7 +1385,11 @@ int main(int argc, char **argv)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: git log によるログを標準出力へ出力する (git log --no-pager)</title><link>https://maku77.github.io/p/ntcpmjj/</link><pubDate>Thu, 09 Dec 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ntcpmjj/</guid><description>&lt;p>&lt;code>git log&lt;/code> によるログ出力は、デフォルトでは &lt;code>less&lt;/code> などの Pager アプリケーションに渡されるようになっています。
ツール連携などをしたいときに、標準出力へ直接出力するには以下のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --no-pager
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: Git でコミットログにある文字列が含まれているコミットを検索する (git log --grep)</title><link>https://maku77.github.io/p/hi5iir5/</link><pubDate>Thu, 09 Dec 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/hi5iir5/</guid><description>&lt;p>&lt;code>git log&lt;/code> コマンドを使用するときに、&lt;code>--grep=&amp;lt;RegularExpression&amp;gt;&lt;/code> オプションを使用すると、指定したパターンがコミットメッセージに含まれているコミットだけを表示することができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: コミット時のコメントに getPrice が含まれているコミットを探す&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --grep&lt;span class="o">=&lt;/span>getPrice
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>&lt;code>--grep&lt;/code> は、あくまでコミット時に記述した「コミットメッセージ」の検索だけ行うことに注意してください。
「ソースコード」の変更内容を検索したい場合は、&lt;code>-G&lt;/code> オプションや &lt;code>-S&lt;/code> オプションを使用します。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/hf7bnyi/">ある単語（文字列）に関する変更履歴を調べる (&lt;code>git log -G/-S&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: 指定したディレクトリ以下の変更に関連するコミットログのみ表示する</title><link>https://maku77.github.io/p/kftbcyc/</link><pubDate>Thu, 09 Dec 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/kftbcyc/</guid><description>&lt;p>&lt;code>git log&lt;/code> コマンドのパラメータとしてディレクトリ名を指定すると、そのディレクトリ以下のファイルの変更に関わるコミットログだけを表示することができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: カレントディレクトリ以下の変更のコミットログを表示&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log .
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: src ディレクトリ以下の変更のコミットログを表示&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log src
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: git-svn を使って Git と Subversion を連携する</title><link>https://maku77.github.io/p/mujnu3f/</link><pubDate>Tue, 07 Dec 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/mujnu3f/</guid><description>&lt;h2 id="git-svn-とは">git-svn とは&lt;/h2>
&lt;p>git-svn を使用すると、Subversion のリポジトリに対して、Git クライアントからチェックアウト、コミットなどを実行できるようになります。
Subversion 上でコミットされた変更履歴などを、そのまま Git クライアントから参照可能なログとしてアクセスすることができます。
Subversion から Git へのリポジトリ移行などに使用することができます。&lt;/p>
&lt;h2 id="subversion-のサーバから-git-にインポートする">Subversion のサーバから Git にインポートする&lt;/h2>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git svn clone -s --prefix svn/ svn://repo/myproject .
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記のようにすることで、Subversion で行われたすべてのコミット履歴を &lt;code>git log&lt;/code> で参照できるようになります。
最後のカレントディレクトリを示す &lt;code>.&lt;/code> を省略すると、URL で指定した &lt;code>myproject&lt;/code> という名前のディレクトリが自動的に作成されます。&lt;/p>
&lt;p>上記のように &lt;code>--prefix&lt;/code> オプションを指定することで、わかりやすいリモートトラッキングブランチ名を付けることができます。
ここでは、&lt;code>svn/&lt;/code> と指定することで、Subversion から clone してきたことを簡単に判別できるようにしています。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -a
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">* master
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> remotes/svn/trunk
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> remotes/svn/branch1
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> remotes/svn/branch2
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> ...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git svn clone&lt;/code> が完了すると、以下のように &lt;code>git log&lt;/code> コマンドを使用して、Subversion で行われてきたコミットの履歴を参照することができるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log svn/trunk
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ただし、&lt;code>git svn clone&lt;/code> した時点ではリモートトラッキングブランチが作成されるだけなので、ローカルファイルとしてコードを参照したい場合は、以下のような感じでローカルブランチを作成し、チェックアウトする必要があります。&lt;/p></description></item><item><title>Gitメモ: Git やり直し: 直前のコミットを取り消す (git reset --soft HEAD~)</title><link>https://maku77.github.io/p/kth4c6j/</link><pubDate>Tue, 30 Nov 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/kth4c6j/</guid><description>&lt;p>ローカル PC 上で行った直前のコミットを取り消すには、&lt;code>git reset&lt;/code> コマンドを下記のように実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git reset --soft HEAD~
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これで最新のコミットが取り消されて、ワーキングツリー上でファイルを編集している状態に戻ります。
追加し忘れたファイルなどがある場合は、この状態から &lt;code>git add&lt;/code> して、&lt;code>git commit&lt;/code> し直せば OK です。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add hoge1
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add hoge2
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>中央サーバなどへ &lt;code>git push&lt;/code> した後は、この作業は絶対に行わないでください。
直前のコミットを他の人がすでにフェッチしていたりすると、コミット履歴がぐちゃぐちゃになり、混乱を招くことになります。&lt;/p></description></item><item><title>Gitメモ: Git でブランチ間の差分を調べる</title><link>https://maku77.github.io/p/fmarnds/</link><pubDate>Tue, 16 Nov 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/fmarnds/</guid><description>&lt;p>下記のようなブランチ構成になっているとします。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="" width="448" height="122" src="../p/fmarnds/img-001.png" alt="/p/fmarnds/img-001.png" />
&lt;/figure>

&lt;p>master ブランチには commit1, commit2, commit3, commit4 がコミットされており、topic ブランチは commit2 から分岐し、commit3' と commit4' がコミットされています。
topic ブランチにだけコミットされているものを調べるには以下のようにします（順番重要）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master..topic
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>図で表すと、下記の部分のログだけを表示することになります。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="" width="448" height="160" src="../p/fmarnds/img-002.png" alt="/p/fmarnds/img-002.png" />
&lt;/figure>

&lt;p>正確には、後ろに指定したコミットからたどれるコミットのログのみを表示しています。
トピックブランチを作って作業するスタイルを取っている場合は、このように差分チェックすれば、自分がトピックブランチに対してコミットした履歴のみ調べることができます。
逆に、master ブランチにだけコミットされているものを調べたいときは、指定順序を逆にします。&lt;/p>


&lt;figure class="xImage">
 &lt;img style="" width="448" height="160" src="../p/fmarnds/img-003.png" alt="/p/fmarnds/img-003.png" />
&lt;/figure>

&lt;p>ブランチ名やコミット ID を省略すると、HEAD を指定したとみなされます。
つまり、現在 topic ブランチをチェックアウトしている場合は、下記のコマンドは同じ意味になります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master..HEAD
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master..topic
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master..
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>下記の 3 コマンドも同じ意味になります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log topic..master
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log HEAD..master
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log ..master
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>master ブランチと topic ブランチのいずれかのブランチに含まれているコミットを調べたい場合は、ドット 2 つ (..) だったところをドット 3 つ (&amp;hellip;) にします。
この場合は、ブランチの指定順序には意味がなくなります。&lt;/p></description></item><item><title>Gitメモ: マージ作業に失敗したときにやり直す (git reset)</title><link>https://maku77.github.io/p/49yjwoe/</link><pubDate>Tue, 16 Nov 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/49yjwoe/</guid><description>&lt;p>例えば、トピックブランチでの変更を master ブランチに以下のように間違えてマージしたとします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge mytopic
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>この場合、十分に古いコミットまで master ブランチを &lt;code>git reset&lt;/code> してから、origin/master にリセットすれば元に戻すことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git reset a60676b ← 十分に古いコミット
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git reset origin ← 中央リポジトリの内容相当にリセット
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>実際に master ブランチの内容が、origin/master と同じ内容に戻ったかを調べるには以下のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff master...origin
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>もっとスマートなやり方がありそう。。。&lt;/p></description></item><item><title>bash のプロンプトに Git のカレントブランチ名を表示する (PS1)</title><link>https://maku77.github.io/p/2mb4a94/</link><pubDate>Fri, 15 Oct 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/2mb4a94/</guid><description>&lt;p>下記のように bash のプロンプト設定 (&lt;code>PS1&lt;/code>) を行っておくと、プロンプト表示に Git のカレントブランチ名を表示することができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">~/.bashrc&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">parse_git_branch&lt;span class="o">()&lt;/span> &lt;span class="o">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> git branch 2&amp;gt; /dev/null &lt;span class="p">|&lt;/span> sed -e &lt;span class="s1">&amp;#39;/^[^*]/d&amp;#39;&lt;/span> -e &lt;span class="s1">&amp;#39;s/* \(.*\)/ (\1)/&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="o">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">PS1&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;\w\$(parse_git_branch) &lt;/span>$&lt;span class="s2"> &amp;#34;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>こんな感じで表示されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>~/gitwork/myapp (master) $
&lt;/code>&lt;/pre>&lt;p>ちなみに、Git で現在チェックアウトしているブランチ名を取得するには、いろんな方法があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git name-rev HEAD 2&amp;gt; /dev/null &lt;span class="p">|&lt;/span> awk &lt;span class="s2">&amp;#34;{ print \$2 }&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch 2&amp;gt; /dev/null &lt;span class="p">|&lt;/span> cut -f2 -d&lt;span class="se">\*&lt;/span> -s
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch 2&amp;gt; /dev/null &lt;span class="p">|&lt;/span> grep -e ^* &lt;span class="p">|&lt;/span> tr -d &lt;span class="se">\*\ &lt;/span>←半角スペース
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch 2&amp;gt; /dev/null &lt;span class="p">|&lt;/span> sed -e &lt;span class="s1">&amp;#39;/^[^*]/d&amp;#39;&lt;/span> -e &lt;span class="s1">&amp;#39;s/* \(.*\)/\1/&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: マージのためのコミットを revert する（取り消す） (git revert -m)</title><link>https://maku77.github.io/p/iz8y69c/</link><pubDate>Fri, 08 Oct 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/iz8y69c/</guid><description>&lt;p>マージのためにコミットされた変更を revert する作業は少々やっかいです。
例えば、master ブランチとトピックブランチ上での修正が、下記のようにコミット G でマージされているとします。&lt;/p>
&lt;pre tabindex="0">&lt;code>A---B---C---D---E---G ← master
 | |
 +-----F-----+ ← topic
&lt;/code>&lt;/pre>&lt;p>このとき、コミット G の変更内容にミスが見つかり、コミット G を revert したくなったとしても、単純に以下のようには revert することはできません。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git revert HEAD
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>なぜなら、この revert を許してしまうと、以下のように、どちらが master なのか分からない状態、つまり意図しない [1] と [2] のブランチができたような状態になってしまうからです。
このような状態が許されると、他の人が変更を fetch しようとした場合に、どちらを master ブランチとして取り込めばよいのか分からなくなってしまいます。&lt;/p>
&lt;pre tabindex="0">&lt;code>A---B---C---D---E [1] master?
 |
 +-----F [2] master?
&lt;/code>&lt;/pre>&lt;p>そこで、マージのためのコミットを revert するときは、revert 後にどちらの修正を master として残すかを指定する必要があります。
どちらを残すかは、&lt;code>-m&lt;/code> オプションに 1 や 2 のような数値を指定します。
ここでは [1] の方を残すことにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git revert -m &lt;span class="m">1&lt;/span> HEAD
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これで、コミット G を打ち消すためのコミット H が作成されます。&lt;/p></description></item><item><title>Gitメモ: Git でパッチファイルを作成する (git diff/apply/format-patch/am)</title><link>https://maku77.github.io/p/3x2viow/</link><pubDate>Fri, 17 Sep 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/3x2viow/</guid><description>&lt;h2 id="git-diff-で変更内容をファイルに保存しておく方法">&lt;code>git diff&lt;/code> で変更内容をファイルに保存しておく方法&lt;/h2>
&lt;h3 id="git-diff--git-apply-の基本">&lt;code>git diff&lt;/code> → &lt;code>git apply&lt;/code> の基本&lt;/h3>
&lt;p>インデックスに追加していない変更をパッチファイルとして残しておきたいときは、以下のように &lt;strong>&lt;code>git diff&lt;/code>&lt;/strong> の出力を保存しておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff file.txt &amp;gt; file.patch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これで、&lt;code>file.txt&lt;/code> に関する変更が &lt;code>file.patch&lt;/code> というファイルに保存されるので、次のように &lt;code>file.txt&lt;/code> の内容を破棄しても大丈夫です。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout file.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>パッチファイルの内容を適用するには、&lt;strong>&lt;code>git apply&lt;/code>&lt;/strong> コマンドを使います。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git apply file.patch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>このコマンドは、単純にワーキングツリー上のファイルにパッチの内容を反映するだけなので、コミットが作成されることはありません。&lt;/p>
&lt;h3 id="linux-標準の-patch-コマンドで適用可能なパッチファイルを作成する">Linux 標準の patch コマンドで適用可能なパッチファイルを作成する&lt;/h3>
&lt;p>Linux に標準で付属している &lt;code>patch&lt;/code> コマンドで適用できるパッチファイルを作成するには、&lt;code>git diff&lt;/code> を実行するときに &lt;strong>&lt;code>--no-prefix&lt;/code>&lt;/strong> オプションを指定します。
これにより、パッチファイル内のパス表記が、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-diff" data-lang="diff">&lt;span class="line">&lt;span class="cl">&lt;span class="gd">--- a/aaa/bbb/ccc/file.cpp
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gd">&lt;/span>&lt;span class="gi">+++ b/aaa/bbb/ccc/file.cpp
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>のように &lt;code>a&lt;/code> や &lt;code>b&lt;/code> のプレフィックスがついていたものを&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-diff" data-lang="diff">&lt;span class="line">&lt;span class="cl">&lt;span class="gd">--- aaa/bbb/ccc/file.cpp
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gd">&lt;/span>&lt;span class="gi">+++ aaa/bbb/ccc/file.cpp
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>のように &lt;code>patch&lt;/code> コマンドで解釈できるものに変更できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: patch ファイルの作成&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --no-prefix file.cpp &amp;gt; file.patch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: patch ファイルの適用&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> （.git ディレクトリのあるルートディレクトリへ移動）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> patch -p0 &amp;lt; file.patch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ ワンポイント&lt;/span>
 &lt;span class="xNote_body">&lt;p>仮にパッチファイルを作るときに &lt;code>--no-prefix&lt;/code> を付けないで作成してしまった場合でも、&lt;code>patch&lt;/code> コマンドの引数で &lt;strong>&lt;code>-p1&lt;/code>&lt;/strong> オプションを付けてパスの階層を調整すれば、そのまま適用することができます。&lt;/p></description></item><item><title>Gitメモ: ファイルの変更を取り消してコミットやインデックスの内容に戻す (git checkout, git checkout HEAD)</title><link>https://maku77.github.io/p/xjixs9v/</link><pubDate>Fri, 17 Sep 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xjixs9v/</guid><description>&lt;p>Git には、ファイルの状態が存在する場所として、大きく次の 3 つがあります。&lt;/p>
&lt;ul>
&lt;li>&lt;strong>ワーキングツリー（作業ディレクトリ）&lt;/strong> … 実際に編集しているファイル&lt;/li>
&lt;li>&lt;strong>インデックス（ステージングエリア）&lt;/strong> … &lt;code>git add&lt;/code> でコミット対象として登録された状態&lt;/li>
&lt;li>&lt;strong>コミット（リポジトリ）&lt;/strong> … &lt;code>git commit&lt;/code> で確定された状態&lt;/li>
&lt;/ul>
&lt;p>&lt;code>git checkout&lt;/code> コマンドを使うと、インデックスやコミットの内容でワーキングツリーのファイルを上書きし、変更を取り消すことができます。&lt;/p>
&lt;h2 id="ファイルの内容を最新コミットの内容に戻す">ファイルの内容を最新コミットの内容に戻す&lt;/h2>
&lt;p>ファイルの変更をすべて破棄して、最後にコミットした状態に戻したい場合は、次のように &lt;code>HEAD&lt;/code>（最新コミット）を指定して実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout HEAD file.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>この方法では、&lt;code>git add&lt;/code> 済みのインデックスの内容も最新コミットの状態にリセットされます。
ファイル名を複数指定したり、ディレクトリ名を指定したりすることも可能です。&lt;/p>
&lt;h2 id="ファイルの内容をインデックスの内容に戻す">ファイルの内容をインデックスの内容に戻す&lt;/h2>
&lt;p>&lt;code>git add&lt;/code> した後にさらにファイルを編集してしまったけれど、&lt;code>git add&lt;/code> した時点の内容に戻したい場合は、次のように &lt;code>git checkout&lt;/code> でファイル名だけを指定します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout file.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>HEAD&lt;/code> を付けた場合との違いに注意してください。&lt;/p>
&lt;ul>
&lt;li>&lt;code>git checkout HEAD file.txt&lt;/code> … &lt;strong>最新コミット&lt;/strong>の内容でワーキングツリーを上書きする&lt;/li>
&lt;li>&lt;code>git checkout file.txt&lt;/code> … &lt;strong>インデックス（&lt;code>git add&lt;/code> した時点の内容）&lt;/strong> でワーキングツリーを上書きする&lt;/li>
&lt;/ul>
&lt;p>まだ一度も &lt;code>git add&lt;/code> していないファイルに対して実行した場合は、インデックスの内容＝最新コミットの内容になるため、結果的にどちらも同じ動作になります。&lt;/p>
&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ git rm した後のチェックアウト&lt;/span>
 &lt;span class="xNote_body">&lt;p>&lt;code>git rm&lt;/code> を実行すると、ワーキングツリーからファイルが削除されるだけでなく、インデックスからも削除されます。
そのため、次のような操作は失敗します。&lt;/p></description></item><item><title>Gitメモ: マージで競合解決が必要なファイルを調べる (git ls-files -u)</title><link>https://maku77.github.io/p/jok7ggh/</link><pubDate>Fri, 17 Sep 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/jok7ggh/</guid><description>&lt;p>マージ作業を行う際に、修正がコンフリクトしているファイルの一覧を表示するには下記のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git ls-files -u
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>.gitignore ファイルで Git にバージョン管理させないファイルを指定する</title><link>https://maku77.github.io/p/ucp6uab/</link><pubDate>Fri, 27 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ucp6uab/</guid><description>&lt;h2 id="gitignore-メカニズムが検索する設定ファイル">gitignore メカニズムが検索する設定ファイル&lt;/h2>
&lt;p>特定のファイルを Git のバージョン管理の対象から外すための仕組みとして、&lt;strong>gitignore&lt;/strong> メカニズムが用意されています。
そのための設定ファイルは、下記のようなパスから検索され、最初に見つかったものが優先的に使用されます。&lt;/p>
&lt;ol>
&lt;li>ワークツリー中のディレクトリに置かれた &lt;strong>&lt;code>.gitignore&lt;/code>&lt;/strong> ファイル&lt;/li>
&lt;li>リポジトリの &lt;strong>&lt;code>.git/info/exclude&lt;/code>&lt;/strong> ファイル&lt;/li>
&lt;li>構成変数 &lt;strong>&lt;code>core.exludesfile&lt;/code>&lt;/strong> でパスを指定したファイル&lt;/li>
&lt;/ol>
&lt;p>ワークツリー内に置く &lt;code>.gitignore&lt;/code> ファイルは、どの階層のディレクトリにも置くことができ、より下位のディレクトリにある &lt;code>.gitignore&lt;/code> ファイルによる設定が優先されます。&lt;/p>
&lt;p>プロジェクト全体で設定を共有したい場合は、&lt;code>.gitignore&lt;/code> に設定を記述し、これをコミットしてチームメンバと共有します。
特定のリポジトリで、自分用に設定を行いたい場合は &lt;code>.git/info/exclude&lt;/code> に設定を記述します。&lt;/p>
&lt;p>どのリポジトリにも共通した、自分専用の設定を用いたい場合は、例えば &lt;code>$HOME/myconf/git-excludes&lt;/code> といったファイルに設定を記述しておき、&lt;code>git config&lt;/code> で以下のように参照設定しておけばよいでしょう。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.excludesfile &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$HOME&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span>/myconf/git-excludes
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記の例では、グローバルな設定値にしていますが、&lt;code>--global&lt;/code> オプションを外せば、特定のリポジトリ用に設定することも可能です。&lt;/p>
&lt;h2 id="gitignore-ファイルの構文">.gitignore ファイルの構文&lt;/h2>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-shell" data-lang="shell">&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># コメント行&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">aaa &lt;span class="c1"># aaa というファイル、あるいはディレクトリを無視。&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">bbb/ &lt;span class="c1"># bbb というディレクトリを無視。&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">/ccc &lt;span class="c1"># .gitignore ファイルのあるディレクトリからの相対パスで&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># ccc というファイル、あるいはディレクトリを無視。&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># つまり、この場合は .gitignore ファイルと同じディレクトリ内にある&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># ccc というファイル、あるいはディレクトリを示す。&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">*.class &lt;span class="c1"># 名前が .class で終わるファイルを無視&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">*.&lt;span class="o">[&lt;/span>ao&lt;span class="o">]&lt;/span> &lt;span class="c1"># 名前が .a あるいは .o で終わるファイルを無視&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">!ddd.o &lt;span class="c1"># ただし、ddd.o というファイルは無視「しない」&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">!/eee.a &lt;span class="c1"># .gitignore ファイルと同じディレクトリの eee.a というファイルは無視「しない」&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="設定例">設定例&lt;/h2>
&lt;p>デフォルトでは、以下のような一時ファイルまで &lt;code>git status&lt;/code> コマンドなどで認識されてしまうので、これらのファイルを版管理の対象からはずしておくとよいです。&lt;/p></description></item><item><title>Gitメモ: git clean で追跡されていないファイルを削除する (git clean -df)</title><link>https://maku77.github.io/p/scqf3sw/</link><pubDate>Fri, 27 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/scqf3sw/</guid><description>&lt;h2 id="git-管轄下にないファイルを削除する">Git 管轄下にないファイルを削除する&lt;/h2>
&lt;p>&lt;code>git status&lt;/code> コマンドを実行したときに &lt;strong>Untracked files&lt;/strong> の欄に表示されるファイルは、Git の管轄下にないファイル（追跡されていないファイル）です。
これらのファイルをすべて削除するには、対象のディレクトリ内で以下のコマンドを実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clean -f
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>空になったディレクトリも同時に削除するには、さらに &lt;code>-d&lt;/code> オプションを指定します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clean -df
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git clean&lt;/code> で削除したファイルは元に戻せないので、このコマンドは慎重に実行する必要があります。&lt;/p>
&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ なぜいちいち -f オプションが必要なのか？&lt;/span>
 &lt;span class="xNote_body">&lt;p>&lt;code>make clean&lt;/code> とタイプしようとして、間違えて &lt;code>git clean&lt;/code> とタイプしてしまうと大変なことになるので、&lt;code>git clean&lt;/code> コマンドは &lt;code>-f&lt;/code> オプションを指定しないとエラーで止まるようになっているらしいです。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clean
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">fatal: clean.requireForce defaults to true and neither -i, -n, nor -f given; refusing to clean
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/span>
&lt;/div>
&lt;h2 id="git-clean-でどのファイルが削除されるか確認する">git clean でどのファイルが削除されるか確認する&lt;/h2>
&lt;p>実際に &lt;code>git clean -f&lt;/code> を実行する前に、どのファイルが消されるのかを &lt;code>git clean -n&lt;/code> で確認することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clean -n
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>同様に、空のディレクトリを消す &lt;code>-d&lt;/code> オプションを同時に指定した場合に、どのファイルとディレクトリが削除されるかは以下のように調べることができます。&lt;/p></description></item><item><title>Gitメモ: ファイルを削除せずに Git によるバージョン管理の対象から外す (git rm --cached)</title><link>https://maku77.github.io/p/qu3d2f9/</link><pubDate>Fri, 27 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/qu3d2f9/</guid><description>&lt;p>&lt;code>git rm&lt;/code> コマンドは、デフォルトでは、指定したファイルをインデックスの登録から解除すると同時に、ローカル PC 上のファイル自体を削除してしまいます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git rm sample.txt &lt;span class="c1"># ローカル PC からも削除されてしまう&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>下記のように &lt;code>git rm&lt;/code> コマンドに &lt;strong>&lt;code>--cached&lt;/code>&lt;/strong> オプションを付けて実行すると、インデックスの登録から解除するだけで、ファイル自体は untracked 状態になってワーキングツリーに残ります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git rm --cached -f sample.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: 特定のユーザのみ git push できるように制限する</title><link>https://maku77.github.io/p/f84ekdq/</link><pubDate>Thu, 26 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/f84ekdq/</guid><description>&lt;h2 id="git-push-できるユーザをホワイトリストで管理する">git push できるユーザをホワイトリストで管理する&lt;/h2>
&lt;p>指定したユーザにだけ &lt;code>git push&lt;/code> を許可するには、&lt;code>git push&lt;/code> 時にサーバサイドで実行される &lt;code>.git/hooks/update&lt;/code> というスクリプトを利用します。
このスクリプトが &lt;code>0&lt;/code> 以外の値を返して終了するとき、ユーザの &lt;code>push&lt;/code> が失敗することを利用してスクリプトを作成します。&lt;/p>
&lt;p>以下のサンプルスクリプトでは、中央リポジトリの &lt;code>.git/hooks/authorized_users&lt;/code> に記述されたユーザにのみ push を許可しています。
&lt;code>authorized_users&lt;/code> ファイルの中身はこのような感じです。
コメントも記述できます。
ユーザ名が 1 つも存在しない場合は、すべてのユーザの push を許可します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">.git/hooks/authorized_users&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl"># Authorized Users
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">user1 # Jack
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">user2 # Smith
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">user3 # Justine&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>中央リポジトリの &lt;code>.git/hooks/update&lt;/code> スクリプトの内容は以下のような感じで記述します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">.git/hooks/update&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="cp">#!/bin/sh
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="cp">&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># Branch name the user is about to push to.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">branch_name&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$1&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># Fullpath of hooks dir.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">self_dir&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="k">$(&lt;/span>&lt;span class="nb">cd&lt;/span> &lt;span class="k">$(&lt;/span>dirname &lt;span class="nv">$0&lt;/span>&lt;span class="k">)&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="nb">pwd&lt;/span>&lt;span class="k">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 1 if there is no entries in the authorized_users.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">no_entry&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="m">1&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># Search user names.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">while&lt;/span> &lt;span class="nb">read&lt;/span> line
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">do&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># Remove comment.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nv">line&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="si">${&lt;/span>&lt;span class="nv">line&lt;/span>&lt;span class="p">%%#*&lt;/span>&lt;span class="si">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># Remove trailing spaces.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nv">line&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="k">$(&lt;/span>&lt;span class="nb">echo&lt;/span> &lt;span class="nv">$line&lt;/span> &lt;span class="p">|&lt;/span> sed -e &lt;span class="s1">&amp;#39;s/ *$//&amp;#39;&lt;/span>&lt;span class="k">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="o">[&lt;/span> -z &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$line&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="o">]&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="k">then&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">continue&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">fi&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="o">[&lt;/span> &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$USER&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$line&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="o">]&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="k">then&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># The user can push to the branch.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nb">exit&lt;/span> &lt;span class="m">0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">fi&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nv">no_entry&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="m">0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">done&lt;/span> &amp;lt; &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$self_dir&lt;/span>&lt;span class="s2">/authorized_users&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">if&lt;/span> &lt;span class="o">[&lt;/span> &lt;span class="nv">$no_entry&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="m">1&lt;/span> &lt;span class="o">]&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="k">then&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># No restriction.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1"># The user can push to the branch.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nb">exit&lt;/span> &lt;span class="m">0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">fi&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;----------------------------------------------&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;[&lt;/span>&lt;span class="nv">$branch_name&lt;/span>&lt;span class="s2">] is in submit control.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;You have no access to the branch.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;Please wait until the submit control finishes.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;----------------------------------------------&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># The user cannot push to the branch.&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">exit&lt;/span> &lt;span class="m">1&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="特定のブランチだけサブミットコントロールをかける">特定のブランチだけサブミットコントロールをかける&lt;/h2>
&lt;p>さらに、ブランチ毎に push 権限をコントロールできるようにする update スクリプトの例です。
このスクリプトでは、ユーザのアクセス制限を以下のように設定ファイル (&lt;code>authorized_users&lt;/code>) でかけることができます。&lt;/p></description></item><item><title>Git でリモートブランチを削除する (git push origin :xxx, git fetch --prune)</title><link>https://maku77.github.io/p/cv5pi7a/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/cv5pi7a/</guid><description>&lt;ul>
&lt;li>参考: &lt;a href="../p/stpfje9/">Git でブランチを削除する (&lt;code>git branch -d/-D&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul>
&lt;h2 id="必要のないリモートブランチを削除リモートサーバー側">必要のないリモートブランチを削除（リモートサーバー側）&lt;/h2>
&lt;p>&lt;code>git branch -d&lt;/code> コマンドを使ってローカルのブランチを削除しても、GitHub などに既に push されているブランチは削除されません（他のメンバーには、まだブランチが存在しているように見えます）。
GitHub 側のブランチを削除するには、ローカルブランチを削除するのとは別のコマンドを実行する必要があります。&lt;/p>
&lt;p>まずは、リモートトラッキングブランチの一覧を確認しておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -r
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/HEAD -&amp;gt; origin/main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/mybranch1
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/mybranch2
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/mybranch3
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>例えば、この中の &lt;code>mybranch2&lt;/code> を削除したい場合は、以下のようにして &lt;code>origin&lt;/code> サーバに削除要求を送ります。
削除したいブランチの名前の前にコロン (&lt;code>:&lt;/code>) を付けるのがポイントです。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin :mybranch2
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git push&lt;/code> の 2 番目のパラメータで指定するリファレンス名は、&lt;code>&amp;lt;プッシュ元のブランチ名&amp;gt;:&amp;lt;プッシュ先のブランチ名&amp;gt;&lt;/code> という形式を取りますが、このコロンの左側の部分を空にすることで、プッシュ先のブランチを削除する（空にする）という意味になります。&lt;/p>
&lt;h2 id="ローカル側のリモートトラッキングブランチを削除する">ローカル側のリモートトラッキングブランチを削除する&lt;/h2>
&lt;p>中央リポジトリ (GitHub) 側のブランチを削除しても、ローカルに作られているリモートトラッキングブランチは削除されません。
以下のように必要のないリモートトラッキングブランチを削除する必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">#&lt;/span> フェッチと同時に必要のないリモートトラッキングブランチを削除する（オススメ）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git fetch --prune
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">#&lt;/span> 必要のないリモートトラッキングブランチを削除する
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git remote prune origin
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>あるいは、以下のように指定したリモートトラッキングブランチを削除することもできます。&lt;/p></description></item><item><title>Git の detached HEAD 状態へのチェックアウトとは</title><link>https://maku77.github.io/p/whv8ues/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/whv8ues/</guid><description>&lt;p>ある Git ブランチのコードをチェックアウトするには、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch &amp;lt;ブランチ名&amp;gt;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>のように実行しますが、そのブランチの最新コードではなく、ある特定のバージョン（コミット ID）のソースコードを少しだけ覗いてみたいということがあります。
そのような場合は、ブランチ名の代わりにコミット ID を指定してチェックアウトすることができます。&lt;/p>
&lt;p>コミット名を指定して &lt;code>git checkout&lt;/code> を実行すると、どのローカルブランチもチェックアウトしていない &lt;strong>detached HEAD&lt;/strong> という状態になります（&lt;code>HEAD&lt;/code> が detached されているということ）。
例えば、以下のように &lt;code>git checkout&lt;/code> すると、この状態になります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout 0d3ce61 &lt;span class="c1"># コミット ID を指定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout HEAD^0 &lt;span class="c1"># 最新のコミットを指定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout HEAD^1 &lt;span class="c1"># １つ前のコミットを指定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout v2.6.18 &lt;span class="c1"># タグで示されるコミットを指定&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout origin/main &lt;span class="c1"># リモートトラッキングブランチを指定&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>HEAD&lt;/code> が detached された状態で &lt;code>git branch&lt;/code> を実行すると、どのブランチにもいないことを示す &lt;code>(no branch)&lt;/code> が表示されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>$ git branch
* (no branch)
 main
 mybranch
&lt;/code>&lt;/pre>&lt;p>detached HEAD 状態でも &lt;code>git commit&lt;/code> などのコマンドを実行できますが、どのブランチにもいない状態なので、どのブランチの head も更新されません。
再びローカルブランチ名を指定してチェックアウトすると、detached HEAD で行っていた変更を破棄してブランチの最新状態に戻ることができます。&lt;/p></description></item><item><title>Gitメモ: git pull の引数省略の仕組み</title><link>https://maku77.github.io/p/xonap3b/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xonap3b/</guid><description>&lt;p>&lt;code>git pull&lt;/code> を実行するときに、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull git://git.example.com/aaa/bbb.git master
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>としないで、リモートリポジトリの URL と、ブランチ名を省略し、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>とした場合に何が行われるかは、&lt;code>.git/config&lt;/code> ファイルの設定によります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> cat .git/config
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">[remote &amp;#34;origin&amp;#34;]
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> url = git://git.example.com/aaa/bbb.git
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> fetch = +refs/heads/*:refs/remotes/origin/*
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">[branch &amp;#34;master&amp;#34;]
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> remote = origin
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> merge = refs/heads/master
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記の &lt;code>[branch &amp;quot;master&amp;quot;]&lt;/code> というセクションは、現在チェックアウトしているブランチが &lt;code>master&lt;/code> の場合に使用する設定を示しています。
例えば、この例では &lt;code>branch.master.remote&lt;/code> 変数が &lt;code>origin&lt;/code> となっているので、&lt;code>git pull&lt;/code> するときにリポジトリの指定を省略すると、&lt;code>origin&lt;/code> というリモートリポジトリがデフォルトで使用されます。
つまり、&lt;code>master&lt;/code> ブランチがカレントのときは、以下の２つが同じ意味になります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull origin
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>さらに、この &lt;code>origin&lt;/code> というリモートリポジトリ名が実際にどの URL を示すのかは、&lt;code>remote.origin.url&lt;/code> 変数に設定されています。
つまり、以下の２つが同じ意味になります。&lt;/p></description></item><item><title>Gitメモ: Git の update フックの基本 (.git/hooks/update)</title><link>https://maku77.github.io/p/gyotfx7/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/gyotfx7/</guid><description>&lt;p>&lt;strong>&lt;code>.git/hooks/update&lt;/code>&lt;/strong> スクリプトは、クライアントが &lt;code>git push&lt;/code> しようとしたときに、サーバ側で実行されるフックスクリプトです。
このスクリプトが &lt;code>0&lt;/code> 以外の値を返すと、&lt;code>git push&lt;/code> は失敗するようになっています。&lt;/p>
&lt;p>例えば、以下のような shell スクリプトを push される側の &lt;code>.git/hooks/update&lt;/code> として置いて実験してみましょう。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: .git/hooks/update スクリプト&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;--------------------------------------------------&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="nv">USER&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nv">$USER&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="nv">refname&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nv">$1&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> old &lt;span class="nv">rev&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nv">$2&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> new &lt;span class="nv">rev&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nv">$3&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;--------------------------------------------------&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">exit&lt;/span> &lt;span class="m">1&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>あくまでスクリプトなので、忘れずに実行権限を付けておきます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> chmod &lt;span class="m">755&lt;/span> update
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>そして、そのリモートリポジトリに対して &lt;code>git push&lt;/code> をすると、上記のスクリプトは、ブランチ名やコミット番号などの情報を表示した後、&lt;code>0&lt;/code> 以外の値で終了しているので &lt;code>git push&lt;/code> がエラーになり失敗します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">git push の実行結果&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl">Counting objects: 4, done.
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">Compressing objects: 100% (2/2), done.
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">Writing objects: 100% (3/3), 292 bytes, done.
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">Total 3 (delta 1), reused 0 (delta 0)
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">--------------------------------------------------
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">USER = joe
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">refname = refs/heads/master
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">old rev = 49af424ee70414b55f232e2761d2dbbbe392a40a
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">new rev = c2ac08c5fceb715c408e62c79ec814388581c1c0
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">--------------------------------------------------
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">error: hooks/update exited with error code 1
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">error: hook declined to update refs/heads/master
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">To /home/maku/git_shared/test
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> ! [remote rejected] master -&amp;gt; master (hook declined)
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">error: failed to push some refs to &amp;#39;/home/maku/git_shared/test&amp;#39;&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: マージベースとなったコミットを見つける（fast-forward の関係になっているか調べる） (git merge-base)</title><link>https://maku77.github.io/p/2u3igu6/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/2u3igu6/</guid><description>&lt;p>ある 2 つのコミットのブランチ元となるコミット（マージベース）を調べるには、&lt;strong>&lt;code>git merge-base&lt;/code>&lt;/strong> コマンドを使用できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge-base master feature
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">fff0c132210d4d1c4517d78802c7fd5c93d4f975
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記の例では、master ブランチと feature ブランチは、共通の &lt;code>fff0c13&lt;/code> というコミットから分岐していることを示しています。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge-base HEAD 0d3ce61
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">0d3ce6138f57590d67f7543435d03972be3b3d4d
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記の例では、2 番目に指定したコミット番号と同じものがマージベースとして表示されています。
このような場合、2 つのコミットは &lt;strong>fast forward&lt;/strong> の関係にあり、コミット &lt;code>0d3ce61&lt;/code> へのリファレンスを前に進めるだけで、HEAD と同じ状態になることを示しています。&lt;/p></description></item><item><title>Gitメモ: ローカルディレクトリをリモートリポジトリとして使用する</title><link>https://maku77.github.io/p/jkbezsr/</link><pubDate>Mon, 23 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/jkbezsr/</guid><description>&lt;p>以下のように、ディレクトリ名指定でリポジトリを &lt;code>git clone&lt;/code> することで、物理的にはローカルにあるリポジトリをリモートリポジトリとして扱うことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone &lt;span class="nv">$HOME&lt;/span>/gitwork/myrep
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Initialized empty Git repository in /home/joe/test/myrep/.git/
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これを利用して、1 つのマシン上で &lt;code>git push&lt;/code> や &lt;code>git pull&lt;/code> などの動作をテストすることができます。&lt;/p>
&lt;p>上記のように &lt;code>clone&lt;/code> してできたリポジトリの &lt;code>.git/config&lt;/code> の内容は以下のようになっています。
&lt;code>remote.origin.url&lt;/code> 変数の値が、&lt;code>clone&lt;/code> 元のローカルディレクトリを示していることが分かります。&lt;/p>
&lt;pre tabindex="0">&lt;code>[core]
 repositoryformatversion = 0
 filemode = true
 bare = false
 logallrefupdates = true
[remote &amp;#34;origin&amp;#34;]
 url = /home/joe/gitwork/myrep
 fetch = +refs/heads/*:refs/remotes/origin/*
[branch &amp;#34;master&amp;#34;]
 remote = origin
 merge = refs/heads/master
&lt;/code>&lt;/pre></description></item><item><title>Git で他のブランチの最新ファイルを現在のブランチにコピーする (git checkout)</title><link>https://maku77.github.io/p/6g8n3jw/</link><pubDate>Fri, 20 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/6g8n3jw/</guid><description>&lt;p>Git では、ファイル単位で他のブランチにコミットされているファイルをコピーしてくることができます。
例えば、現在 &lt;code>main&lt;/code> ブランチをチェックアウトしているときに、別のブランチ &lt;code>mybranch&lt;/code> の &lt;code>sample.txt&lt;/code> の最新をコピーしてきたいときは次のように実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout mybranch sample.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: コミットログにファイル名や変更行数を表示する (git log --name-only, --stat)</title><link>https://maku77.github.io/p/zgr593x/</link><pubDate>Fri, 20 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/zgr593x/</guid><description>&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --name-only &lt;span class="c1"># ファイル名を表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --stat &lt;span class="c1"># ファイル名と変更行数を表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Git コマンドの長いオプション名を省略して入力する小技</title><link>https://maku77.github.io/p/7cf83cy/</link><pubDate>Sun, 15 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/7cf83cy/</guid><description>&lt;p>Git のコマンドで &lt;strong>&lt;code>--&lt;/code>&lt;/strong> で始まるオプションを指定する場合、そのオプション名を省略して指定することができます。
例えば、&lt;code>--dry-run&lt;/code> オプションは通常次のように指定しますが、&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --dry-run
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>次のように &lt;code>--dr&lt;/code> と省略して実行することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push --dr
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>--d&lt;/code> だけだと、&lt;code>--delete&lt;/code> なのか &lt;code>--dry-run&lt;/code> なのか判別できないので、一意に決まる長さまでは入力する必要があります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/a59xkpd/">Git コマンドのエイリアスを作成する (&lt;code>alias.xxx&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: Git でリモートブランチとローカルブランチの差分を調べる</title><link>https://maku77.github.io/p/zyitasn/</link><pubDate>Mon, 02 Aug 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/zyitasn/</guid><description>&lt;p>例えば、クローン元 (origin) から fetch したリモートトラッキングブランチの master と、ローカルの master ブランチの差分を調べるには以下のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git fetch &lt;span class="c1"># まずはサーバ上のデータと同期しておく&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master..origin/master &lt;span class="c1"># リモートにだけコミットされたものを表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log origin/master..master &lt;span class="c1"># ローカルにだけコミットされたものを表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記のように、ドット 2 つでブランチ名を指定すると、右側に指定したブランチにのみ含まれているコミットを調べることができます。
ローカルの &lt;code>master&lt;/code> ブランチと、リモートの &lt;code>origin/master&lt;/code> ブランチの双方で異なるコミットが行われている場合に、それらをすべて表示したいときは以下のようにドット 3 つでブランチ名を繋ぎます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log master...origin/master &lt;span class="c1"># リモート、あるいはローカルにだけコミットされたものを表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>さらに、&lt;code>--left-right&lt;/code> オプションを指定すると、ローカルにコミットしたものは &lt;code>&amp;gt;&lt;/code>、リモートにコミットされたものは &lt;code>&amp;lt;&lt;/code> という記号がコミット名に付くようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --left-right master...origin/master
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: マージ時のコンフリクトを解決する</title><link>https://maku77.github.io/p/ymfxj2s/</link><pubDate>Tue, 27 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ymfxj2s/</guid><description>&lt;h2 id="git-によるコンフリクトの発生と解決">Git によるコンフリクトの発生と解決&lt;/h2>
&lt;p>２つのブランチ上で、同じファイルの同じ個所を修正していると、&lt;code>git merge&lt;/code> や &lt;code>git pull&lt;/code> を実行したときに&lt;strong>コンフリクト&lt;/strong>（修正箇所の衝突）が発生します。
コンフリクトが発生した場合は、コンフリクトの原因となっているファイルを編集して、手動でコンフリクトしている箇所を修正する必要があります。
その後、そのファイルを &lt;code>git add&lt;/code>、&lt;code>git commit&lt;/code> することによって、全体のマージが完了します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add conflicted_file
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="マージ完了前の見え方">マージ完了前の見え方&lt;/h2>
&lt;p>&lt;code>git commit&lt;/code> する前に、&lt;code>git log&lt;/code> でコミット履歴を確認すると、別のブランチで行われていたコミットはまだマージされていないように見えます。
これらのコミットは、&lt;code>git commit&lt;/code> が終了した時点で見える（辿れる）ようになります。&lt;/p>
&lt;p>また、&lt;code>git commit&lt;/code> を実行する前に &lt;code>git status&lt;/code> を見ると、あたかも別のブランチでのコミットで追加されたファイルが、今回の自分の &lt;code>git commit&lt;/code> で追加されるかのようにステージングされているように見えます。
これは、実際には上記のマージ作業が終了すると、別のコミットでちゃんと追加されたように見えるようになりますので安心してください。&lt;/p></description></item><item><title>Gitメモ: リモートリポジトリに略称を付ける (git remote add)</title><link>https://maku77.github.io/p/mhqsei2/</link><pubDate>Thu, 22 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/mhqsei2/</guid><description>&lt;p>&lt;strong>&lt;code>git remote add&lt;/code>&lt;/strong> コマンドを使うと、リモートリポジトリの URL に短い名前（エイリアス）を付けることができます。
長い URL を毎回入力する必要がなくなり、&lt;code>git pull&lt;/code> や &lt;code>git push&lt;/code> の操作が簡単になります。&lt;/p>
&lt;h2 id="リモートリポジトリにエイリアスを設定する">リモートリポジトリにエイリアスを設定する&lt;/h2>
&lt;p>例えば、以下のように長い URL を指定して &lt;code>git pull&lt;/code> するのは面倒です。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull git://example.com/myapp.git main
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;strong>&lt;code>git remote add &amp;lt;エイリアス名&amp;gt; &amp;lt;URL&amp;gt;&lt;/code>&lt;/strong> としてエイリアス名を登録しておくと、URL の代わりに使えるようになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git remote add myapp git://example.com/myapp.git
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>これ以降は、エイリアス名で操作できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git pull myapp main
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push myapp main
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>実は、&lt;code>git clone&lt;/code> でリポジトリを取得すると、クローン元の URL に &lt;strong>&lt;code>origin&lt;/code>&lt;/strong> というエイリアスが自動的に設定されます。
普段 &lt;code>git push origin main&lt;/code> のように使っている &lt;code>origin&lt;/code> は、このエイリアスのことです。&lt;/p>
&lt;h2 id="登録済みのリモートリポジトリを確認する">登録済みのリモートリポジトリを確認する&lt;/h2>
&lt;p>現在登録されているリモートリポジトリの一覧は、&lt;code>git remote -v&lt;/code> で確認できます。&lt;/p></description></item><item><title>Gitメモ: 共有リポジトリとやりとりする (git fetch, git push)</title><link>https://maku77.github.io/p/72jawne/</link><pubDate>Thu, 22 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/72jawne/</guid><description>&lt;h2 id="共有リポジトリから変更を取得する-fetch">共有リポジトリから変更を取得する (fetch)&lt;/h2>
&lt;p>リモートリポジトリ（共有リポジトリ）をクローンしてきた後に、他のユーザによって行われたリモート上での変更を再度取得するには &lt;code>git fetch&lt;/code> コマンドを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git fetch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>リモートリポジトリから fetch してきた内容は、直ちに現在チェックアウトしているブランチに反映されるということはなく、いったん自分の PC 上の&lt;strong>リモートトラッキングブランチ (remote-tracking branch)&lt;/strong> というところに格納されます。&lt;/p>
&lt;p>自分の PC 上のリモートトラッキングブランチを確認するには、&lt;code>git branch&lt;/code> コマンドに &lt;code>-r&lt;/code> オプションを付けて実行します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -r
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/HEAD
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> origin/master
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>origin/&lt;/code> というプレフィックスは、トラック対象となっているリモートリポジトリの名前を示しています。
最初に &lt;code>git clone&lt;/code> したリモートリポジトリには、デフォルトで &lt;code>origin&lt;/code> という略称がつけられており、上記の出力は、この &lt;code>origin&lt;/code> リモートリポジトリをトラックするためのリモートトラッキングブランチであることを示しています。&lt;/p>
&lt;p>&lt;code>origin/HEAD&lt;/code>、&lt;code>origin/master&lt;/code> というブランチは、ローカルブランチと区別して、リモートトラッキングブランチと呼びます。&lt;/p>
&lt;h2 id="fetch-してきた変更をローカルブランチにマージする">fetch してきた変更をローカルブランチにマージする&lt;/h2>
&lt;p>&lt;code>git fetch&lt;/code> によってリモートリポジトリから取得してきた変更内容は、自分の PC 上のリモートトラッキングブランチ（&lt;code>origin/master&lt;/code> など）に格納されます。
つまり、ローカルブランチ（&lt;code>master&lt;/code> など）にはまだコミット内容が反映されていない状態です。&lt;/p>
&lt;p>この変更をローカルブランチに対してコミットされた状態にするには、リモートトラッキングブランチ (&lt;code>origin/master&lt;/code>) 上のコミット内容を、ローカルブランチ (&lt;code>master&lt;/code>) にマージする必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git checkout master &lt;span class="c1"># master ブランチに切り替える（必要があれば）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge origin &lt;span class="c1"># リモートトラッキングブランチをローカルブランチ master にマージ&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git merge&lt;/code> が成功すると、ローカルブランチにリモートブランチの内容がコミットされた状態になります。
&lt;code>git log&lt;/code> でちゃんとマージされていることを確認できます。&lt;/p></description></item><item><title>Git でブランチの作成とチェックアウトを同時に行う (git switch -c, git checkout -b)</title><link>https://maku77.github.io/p/ivbss76/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ivbss76/</guid><description>&lt;p>&lt;strong>&lt;code>git switch -c&lt;/code>&lt;/strong> コマンドを使用すると、ブランチの作成と、そのブランチへの切り替え（チェックアウト）を同時に行うことができます（昔は同様の振る舞いをする &lt;code>git checkout -b&lt;/code> コマンドが使っていました）。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ブランチの作成と切り替えを一度に行う&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch -c mybranch &lt;span class="c1"># ブランチ mybranch の作成＆チェックアウト&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Switched to a new branch &amp;#39;mybranch&amp;#39;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">$&lt;/span> git branch &lt;span class="c1"># 現在チェックアウトしているブランチの確認&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">* mybranch
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>&lt;code>git branch&lt;/code> コマンドでのブランチ作成と同様、ブランチ元のブランチ名を指定することもできます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch -c mybranch main
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Git でブランチ名を変更する (git branch -m)</title><link>https://maku77.github.io/p/9bh2ody/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/9bh2ody/</guid><description>&lt;p>Git のブランチ名を変更するには以下のように &lt;strong>&lt;code>git branch -m&lt;/code>&lt;/strong> コマンドを使用します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ブランチ名を変更する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -m new &lt;span class="c1"># 現在チェックアウトしているブランチの名前を new に変更&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -m old new &lt;span class="c1"># old ブランチの名前を new に変更&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>GitHub などのリモートリポジトリに &lt;code>old&lt;/code> リポジトリがすでに push されている場合は、次のようにして中央リポジトリ側のリポジトリ名も変更する必要があります。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">GitHub 側のブランチ名を変更する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin new &lt;span class="c1"># 新しいブランチ new を push&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin :old &lt;span class="c1"># 古いブランチ old を削除&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;ul>
&lt;li>参考: &lt;a href="../p/cv5pi7a/">Git でリモートブランチを削除する (&lt;code>git push origin :xxx&lt;/code>, &lt;code>git fetch --prune&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Gitメモ: Git で N 個前のコミットを表現する</title><link>https://maku77.github.io/p/npo47yj/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/npo47yj/</guid><description>&lt;p>コミット ID やタグ名の後ろに特殊な記号を付けることで、そのコミットの数個前のコミットを表現することができます。
チルダ (&lt;code>~&lt;/code>) 1 つにつき、1 つ前のコミットを表します。&lt;/p>
&lt;pre tabindex="0">&lt;code>HEAD~ # 最新コミットの 1 つ前
HEAD~~ # 最新コミットの 2 つ前
HEAD~~~ # 最新コミットの 3 つ前
&lt;/code>&lt;/pre>&lt;p>チルダの数が多くなってきた場合は、「チルダ (&lt;code>~&lt;/code>) ＋数値」で同じことを表現できます。&lt;/p>
&lt;pre tabindex="0">&lt;code>HEAD~1 # 最新コミットの 1 つ前
HEAD~2 # 最新コミットの 2 つ前
HEAD~3 # 最新コミットの 3 つ前
&lt;/code>&lt;/pre>
&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">使用例&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-plain" data-lang="plain">&lt;span class="line">&lt;span class="cl"># リビジョン 31df6e7 から、最新の 2 つ前のコミットまでのログ
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">$ git log 31df6e7~..HEAD~2&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>似たような記号にキャレット (&lt;code>^&lt;/code>) があります。
あるコミットの親が複数存在するとき、その親となるコミットを &lt;code>^1&lt;/code>、&lt;code>^2&lt;/code> のように識別できます。
世代としては、どちらも一世代前のコミットを示していることに注意してください。
&lt;code>HEAD^1&lt;/code> と &lt;code>HEAD~1&lt;/code> は結果的に同じコミット（一つ前のコミット）を指します。&lt;/p>
&lt;pre tabindex="0">&lt;code>HEAD^1 # 1 番目の親
HEAD^2 # 2 番目の親
&lt;/code>&lt;/pre>&lt;p>チルダの場合と異なり、&lt;code>HEAD^2&lt;/code> と &lt;code>HEAD^^&lt;/code> は意味が違うことに注意してください。
&lt;code>HEAD^2&lt;/code> は 2 番目の親（一世代前）ですが、&lt;code>HEAD^^&lt;/code> は 1 番目の親 (&lt;code>HEAD^&lt;/code>) のさらに 1 番目の親（二世代前）です。&lt;/p></description></item><item><title>Gitメモ: Git でコミットログの出力形式をカスタマイズする (git log --pretty, --date)</title><link>https://maku77.github.io/p/7ehnuk6/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/7ehnuk6/</guid><description>&lt;h2 id="ログの出力形式をカスタマイズする---pretty">ログの出力形式をカスタマイズする (--pretty)&lt;/h2>
&lt;p>&lt;code>git log&lt;/code> の &lt;code>--pretty&lt;/code> オプションを指定することによってログの出力形式をカスタマイズすることができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">#&lt;/span> 各コミットを一行で表示する
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --pretty&lt;span class="o">=&lt;/span>oneline
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">#&lt;/span> 短い SHA1 ハッシュ値とコミットログの一行目を表示
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --pretty&lt;span class="o">=&lt;/span>format:&lt;span class="s2">&amp;#34;%h %s&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">#&lt;/span> 赤色で commit date、青色で author、緑色で subject を表示
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --pretty&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;format:%Cred%ci %Cblue%an %Cgreen%s&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>フォーマットとして使用できる文字列は &lt;code>git diff --help&lt;/code> の &lt;code>PRETTY FORMATS&lt;/code> セクションで確認できます。&lt;/p>
&lt;h2 id="日時の表示形式をカスタマイズする---date">日時の表示形式をカスタマイズする (--date)&lt;/h2>
&lt;p>&lt;code>git log&lt;/code> コマンドで出力される日時は、デフォルトでは以下のようなフォーマットで表示されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>Tue Apr 14 13:20:45 2015 +0900
&lt;/code>&lt;/pre>&lt;p>&lt;code>git log&lt;/code> の &lt;code>--date&lt;/code> オプションを使用すると、このフォーマットを変更できます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>iso &lt;span class="c1"># 2015-04-14 13:20:45 +0900 （ISO 8601 形式）★オススメ！&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>rfc &lt;span class="c1"># Tue, 14 Apr 2015 13:20:45 +0900（RFC 2822 形式）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>&lt;span class="nb">local&lt;/span> &lt;span class="c1"># Tue Apr 14 13:20:45 2015 （ローカルタイムゾーンにおけるローカルタイム）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>default &lt;span class="c1"># Tue Apr 14 13:20:45 2015 +0900（committer あるいは author のタイムゾーン）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>short &lt;span class="c1"># 2015-04-14 （YYYY-MM-DD 形式の年月日のみ）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>relative &lt;span class="c1"># 3 days ago （現在からの相対時間）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --date&lt;span class="o">=&lt;/span>raw &lt;span class="c1"># 1428985245 +0900 （Git の内部形式）&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Gitメモ: Git でコミットログを絞り込んで表示する</title><link>https://maku77.github.io/p/xsbjsnr/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/xsbjsnr/</guid><description>&lt;h2 id="時間でコミットログを絞り込む">時間でコミットログを絞り込む&lt;/h2>
&lt;p>&lt;code>git log&lt;/code> コマンドでは、時間的な条件を加えてログを絞り込むことができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --since&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;5 hours&amp;#34;&lt;/span> &lt;span class="c1"># ここ 5 時間以内にコミットされたもの&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --since&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;2010-07-15&amp;#34;&lt;/span> &lt;span class="c1"># 2010年7月15日以降にコミットされたもの&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --before&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;5 hours&amp;#34;&lt;/span> &lt;span class="c1"># 5 時間前より前にコミットされたもの&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="コミットの範囲を指定してログを絞り込む">コミットの範囲を指定してログを絞り込む&lt;/h2>
&lt;p>コミット ID や、タグなどを &lt;code>..&lt;/code> でつなげて指定することで、2 つのリビジョン間のコミットログを参照できます。
必ず古いリビジョンから指定します。
1 つ目に指定したリビジョンに関しては検索対象に含まれず、そのリビジョンより後にコミットされたものから表示されます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log 32dfeg..7ab5191 &lt;span class="c1"># 32dfeg 以降 ～ 7ab5191 までのコミット&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log 32dfeg..HEAD &lt;span class="c1"># 32dfeg 以降すべてのコミット&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log 32dfeg.. &lt;span class="c1"># 同上&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log tag1..tag2 &lt;span class="c1"># タグ tag1 以降 ～ tag2 までのコミット&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="コミットした人を指定してログを絞り込む">コミットした人を指定してログを絞り込む&lt;/h2>
&lt;p>Author や Committer で絞り込んで表示することも可能です。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: author あるいは committer に Sean を含むコミットだけを表示する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --author&lt;span class="o">=&lt;/span>Sean
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log --committer&lt;span class="o">=&lt;/span>Sean
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure></description></item><item><title>Gitメモ: ブランチでの変更をマージする（直接マージ、スカッシュマージ、チェリーピック）</title><link>https://maku77.github.io/p/n3vaeh5/</link><pubDate>Mon, 19 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/n3vaeh5/</guid><description>&lt;h2 id="git-の３種類のマージ">Git の３種類のマージ&lt;/h2>
&lt;p>別のブランチで行った変更（コミット）を取り込むことを&lt;strong>マージ&lt;/strong>といいます。
Git のマージには、以下のような種類があります。&lt;/p>
&lt;ul>
&lt;li>&lt;strong>直接マージ&lt;/strong>: 別のブランチでのすべてのコミットをマージ（コミット）する。&lt;/li>
&lt;li>&lt;strong>スカッシュマージ&lt;/strong>: 別のブランチでのすべてのコミットを１つの変更としてステージングする。&lt;/li>
&lt;li>&lt;strong>チェリーピック&lt;/strong>: 別のブランチでの１つのコミットだけを選んでマージ（コミット）する。&lt;/li>
&lt;/ul>
&lt;h2 id="直接マージ">直接マージ&lt;/h2>
&lt;p>直接マージはもっとも一般的なマージで、別のブランチで実行されたコミットを、あたかも現在のブランチに対してコミットされたかのように見えるようにマージします。
通常は、派生先のブランチでの変更を、派生元のブランチにすべて取り込む場合に使用します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: mybranch でのコミットを現在チェックアウトしているブランチにマージする&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge mybranch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Updating e61beb0..dd06978
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Fast-forward
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> aaa.txt | 1 +
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 1 files changed, 1 insertions(+), 0 deletions(-)
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>普通にマージを実行した場合は、すべてのコミットが反映された状態になるので、&lt;code>git commit&lt;/code> を実行する必要はありません。&lt;/p>
&lt;h2 id="スカッシュマージ圧縮マージ">スカッシュマージ（圧縮マージ）&lt;/h2>
&lt;p>&lt;code>git merge&lt;/code> コマンドを実行するときに、&lt;code>--squash&lt;/code> オプションを指定すると、ブランチ先でのすべてのコミットを１つにまとめて、現在チェックアウトしているブランチにステージングすることができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git merge --squash mybranch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Updating dd06978..6d10a56
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Fast-forward
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Squash commit -- not updating HEAD
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> aaa.txt | 3 ++-
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> bbb.txt | 2 ++
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> 2 files changed, 4 insertions(+), 1 deletions(-)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> create mode 100644 bbb.txt
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>通常のマージの場合とは異なり、スカッシュマージした場合は、ただちにコミットが実行されるのではなく、変更がステージングされた状態になります。
実際に変更を反映するにはコミットを実行する必要があります。&lt;/p></description></item><item><title>git config で設定を削除する (config --unset)</title><link>https://maku77.github.io/p/4pad93a/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/4pad93a/</guid><description>&lt;p>&lt;code>git config&lt;/code> で設定した値を削除したい場合は、以下のように &lt;strong>&lt;code>--unset&lt;/code>&lt;/strong> オプションで設定項目を指定します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: user.email のローカル設定を削除する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --local --unset user.email
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>設定のスコープを表すオプション (&lt;code>--local&lt;/code> / &lt;code>--global&lt;/code> / &lt;code>--system&lt;/code>) も同時に指定するようにしてください。
スコープを省略するとデフォルトで local スコープの設定項目の削除になります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/af7q7n3/">Git 設定のスコープ (&lt;code>local&lt;/code>/&lt;code>global&lt;/code>/&lt;code>system&lt;/code>) を理解する&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Git コマンドのエイリアスを作成する (alias.xxx)</title><link>https://maku77.github.io/p/a59xkpd/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/a59xkpd/</guid><description>&lt;p>長くて覚えにくい Git コマンドや、頻繁に使用する Git コマンドには、独自のエイリアスを設定しておくと便利です。&lt;/p>
&lt;p>例えば、&lt;code>git commit&lt;/code> コマンドを &lt;code>git ci&lt;/code> というコマンドとして実行できるようにするには、以下のようにエイリアスを登録します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ci というエイリアスを登録する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.ci &lt;span class="s2">&amp;#34;commit&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>通常、エイリアスはグローバル設定 (&lt;code>--global&lt;/code>) として設定すると思いますが、そのリポジトリ専用のローカル設定 (&lt;code>--local&lt;/code>) として設定するのもありです。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/af7q7n3/">Git 設定のスコープ (&lt;code>local&lt;/code>/&lt;code>global&lt;/code>/&lt;code>system&lt;/code>) を理解する&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>エイリアスの使いすぎは禁物ですが、例えば、Git サブモジュールを扱うためのコマンドは長くなりがちなので、次のような感じでエイリアスを設定しておくと良いかもしれません。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">Git サブモジュール用のエイリアス&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.sclone &lt;span class="s1">&amp;#39;clone --recurse-submodules&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.supdate &lt;span class="s1">&amp;#39;submodule update --remote --recursive --merge&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.sdiff &lt;span class="s1">&amp;#39;!&amp;#39;&lt;/span>&lt;span class="s2">&amp;#34;git diff &amp;amp;&amp;amp; git submodule foreach &amp;#39;git diff&amp;#39;&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global alias.spush &lt;span class="s1">&amp;#39;push --recurse-submodules=on-demand&amp;#39;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の &lt;code>sclone&lt;/code>、&lt;code>supdate&lt;/code>、&lt;code>sdiff&lt;/code>、&lt;code>spush&lt;/code> エイリアスは、サブモジュールまでまとめて &lt;code>clone&lt;/code>、&lt;code>update&lt;/code>、&lt;code>diff&lt;/code>、&lt;code>push&lt;/code> するためのエイリアスです。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/dsctaq7/">Git サブモジュールで別リポジトリの内容を組み込む (&lt;code>git submodule&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Git コマンドの出力をカラフルにする (color.ui)</title><link>https://maku77.github.io/p/odshtcs/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/odshtcs/</guid><description>&lt;h2 id="git-コマンドの出力をカラフルにする">Git コマンドの出力をカラフルにする&lt;/h2>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global color.ui &lt;span class="s2">&amp;#34;auto&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>この設定をしておくと、ターミナルから Git コマンド（&lt;code>git log&lt;/code> や &lt;code>git diff&lt;/code>）を実行したときの出力に色がつくようになります。
特に理由がない限りこの設定をしておくとよいです。&lt;/p>
&lt;h2 id="色の設定をより細かく指定する">色の設定をより細かく指定する&lt;/h2>
&lt;p>以下のような感じで、各パートの色や装飾の方法を設定することができます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global color.diff.meta &lt;span class="s2">&amp;#34;magenta reverse&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;ul>
&lt;li>色: &lt;code>normal&lt;/code>, &lt;code>black&lt;/code>, &lt;code>red&lt;/code>, &lt;code>green&lt;/code>, &lt;code>yellow&lt;/code>, &lt;code>blue&lt;/code>, &lt;code>magenta&lt;/code>, &lt;code>cyan&lt;/code>, &lt;code>white&lt;/code>&lt;/li>
&lt;li>装飾: &lt;code>bold&lt;/code>, &lt;code>dim&lt;/code>, &lt;code>ul&lt;/code>, &lt;code>blink&lt;/code>, &lt;code>reverse&lt;/code>&lt;/li>
&lt;/ul>
&lt;p>どのような項目があるかは、&lt;a href="https://git-scm.com/docs/git-config#Documentation/git-config.txt-coloradvice">&lt;code>git config&lt;/code> のヘルプサイト&lt;/a> で確認できます。&lt;/p></description></item><item><title>Git でコミット時に使用するユーザ名とメールアドレスを設定する (user.name, user.email)</title><link>https://maku77.github.io/p/gr3v7r8/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/gr3v7r8/</guid><description>&lt;p>Git を使い始めるときは、最初に、コミット時に使用されるユーザ名とメールアドレスを設定しておきます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ユーザー名とメールアドレスの設定&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global user.name &lt;span class="s2">&amp;#34;Your Name&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global user.email &lt;span class="s2">&amp;#34;YourName@example.org&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記のようにグローバルな設定値として設定した値は、&lt;strong>&lt;code>~/.gitconfig&lt;/code>&lt;/strong> に保存され、以下のコマンドで設定内容を確認できます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">グローバル設定の確認&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global --list
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">user.name=Your Name
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">user.email=YourName@example.org
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;div class="xNote">
 &lt;span class="xNote_title">☝️ 環境変数による設定&lt;/span>
 &lt;span class="xNote_body">&lt;code>git help config&lt;/code> によると、上記の 2 つの設定は、環境変数 &lt;strong>&lt;code>GIT_AUTHOR_NAME&lt;/code>&lt;/strong>、&lt;strong>&lt;code>GIT_AUTHOR_EMAIL&lt;/code>&lt;/strong> でも設定できるとされており、環境変数の値が優先されるようです。&lt;/span>
&lt;/div></description></item><item><title>Git でファイルの変更をステージする（コミットの印をつける）</title><link>https://maku77.github.io/p/6qp26ez/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/6qp26ez/</guid><description>&lt;h2 id="変更をステージするコミットの印を付けるとは">変更をステージする（コミットの印を付ける）とは&lt;/h2>
&lt;p>Git では、&lt;code>git commit&lt;/code> を実行する前に、ローカルリポジトリにコミットするファイルを指定しておく必要があります。この作業を &lt;strong>「変更をステージする」&lt;/strong> といいます。
変更の内容により、以下のようなコマンドを使い分けます。&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>git add&lt;/code>&lt;/strong>: 新規ファイルを追加、あるいは、既存ファイルを変更する場合。&lt;/li>
&lt;li>&lt;strong>&lt;code>git rm&lt;/code>&lt;/strong>: 既存のファイルを削除する場合。&lt;/li>
&lt;li>&lt;strong>&lt;code>git mv&lt;/code>&lt;/strong>: 既存のファイルの名前を変更、場所を移動する場合。&lt;/li>
&lt;/ul>
&lt;h2 id="git-add-で新規ファイルの追加既存ファイルの変更をステージする">git add で新規ファイルの追加、既存ファイルの変更をステージする&lt;/h2>
&lt;p>Git では、リポジトリに新しいファイルを格納したい場合と、すでにリポジトリに格納されているファイルの変更を行いたい場合に、共通のコマンドとして &lt;strong>&lt;code>git add&lt;/code>&lt;/strong> を使用します。
実際にリポジトリに追加、変更を反映するには、その後で &lt;code>git commit&lt;/code> を実行してコミットを行う必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add aaa.txt
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;Add a sample file&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ちなみに、バージョン管理システムのひとつである Perforce では、編集用の通知には &lt;code>p4 edit&lt;/code>、ファイル追加用の通知には &lt;code>p4 add&lt;/code> と、コマンドが分かれています。
Perforce などの別の VCS に慣れている場合に注意しなければいけないのは、Git の変更管理単位はファイルではなく、もっと小さいハンク (Hunk) と呼ばれる変更部分単位だということです。
そして、それらのハンクがコミット対象として Git に伝わるのは &lt;code>git add&lt;/code> コマンドを実行したタイミングです。
つまり、&lt;code>git add&lt;/code> した後でそのファイルを変更してしまうと、その変更部分はステージされていないということになります。
結果として、そのファイルにはコミット対象の変更部分と、コミット対象ではない変更部分の両方が含まれる少しややこしい状態になります。
慣れないうちは、&lt;code>git add&lt;/code> による変更のステージングは、&lt;code>git commit&lt;/code> の直前に行うようにしてコミット漏れを防ぐとよいでしょう。&lt;/p></description></item><item><title>Git でブランチをチェックアウトする（作業対象のブランチを切り替える）(git switch/checkout)</title><link>https://maku77.github.io/p/8cwbp3e/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/8cwbp3e/</guid><description>&lt;p>作業対象のブランチを切り替えるには、&lt;strong>&lt;code>git switch&lt;/code>&lt;/strong> コマンドで切り替え先のブランチ名を指定します（昔は &lt;code>git checkout&lt;/code> コマンドが使われていましたが、現在は役割を明確にした &lt;code>git switch&lt;/code> コマンドが使われています）。&lt;/p>
&lt;p>ブランチを切り替える前に、 &lt;code>git branch&lt;/code> コマンドを使用して、現在チェックアウトしているブランチと、切り替え先のブランチ名を確認しておくとよいです。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">ブランチの一覧を確認&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">* main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> my-branch
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の出力で、現在の作業対象が &lt;code>main&lt;/code> であることが分かります。
次のようにすると、作業対象を &lt;code>my-branch&lt;/code> ブランチに切り替えることができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">my-branch ブランチに切り替える&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch my-branch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Switched to branch &amp;#34;my-branch&amp;#34;
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">切り替わっていることを確認する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">* my-branch
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>これで、作業対象のブランチが &lt;code>my-branch&lt;/code> に切り替わったので、これ以降の変更作業 (&lt;code>git commit&lt;/code>) は、&lt;code>my-branch&lt;/code> ブランチだけに反映され、&lt;code>main&lt;/code> ブランチには反映されなくなります。&lt;/p></description></item><item><title>Git でブランチを作成する (git branch)</title><link>https://maku77.github.io/p/x9s8k2e/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/x9s8k2e/</guid><description>&lt;p>現在チェックアウトしているブランチから、新しいブランチを作成するには次のコマンドを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch &amp;lt;新しいブランチ名&amp;gt;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ブランチ元のブランチを指定して、新しいブランチを作成するには次のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch &amp;lt;新しいブランチ名&amp;gt; &amp;lt;既存のブランチ名&amp;gt;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>
&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: main ブランチから REL-1.0 ブランチを作成する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch REL-1.0 main
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>&lt;code>git branch&lt;/code> コマンドを引数なしで実行すると、ブランチの一覧を確認できます（&lt;code>git branch --list&lt;/code> と同等）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> REL-1.0
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">* master
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記の例では、今回作成された &lt;code>REL-1.0&lt;/code> と、デフォルトの &lt;code>master&lt;/code> ブランチが存在することが分かります。&lt;code>master&lt;/code> の前の &lt;code>*&lt;/code> は、現在の作業対象（チェックアウトされているブランチ）が &lt;code>master&lt;/code> ブランチであることを示しています。
作業対象を新しいブランチに切り替えるには、&lt;strong>&lt;code>git switch&lt;/code>&lt;/strong> コマンドを使用する必要があります。&lt;/p>
&lt;ul>
&lt;li>参考: &lt;a href="../p/8cwbp3e/">Git でブランチをチェックアウトする（作業対象のブランチを切り替える）(&lt;code>git switch/checkout&lt;/code>)&lt;/a>&lt;/li>
&lt;li>参考: &lt;a href="../p/ivbss76/">Git でブランチの作成とチェックアウトを同時に行う (&lt;code>git switch -c&lt;/code>, &lt;code>git checkout -b&lt;/code>)&lt;/a>&lt;/li>
&lt;li>参考: &lt;a href="../p/ewvaoe3/">Git で他の人が作成したブランチ上で作業する&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>ちなみに、ローカルで新しく作成したブランチの内容を GitHub などの中央リポジトリに &lt;code>git push&lt;/code> するには、次のように実行します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">origin リポジトリへ新しいブランチ REL-1.0 を push&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git push origin REL-1.0
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>このとき、&lt;strong>&lt;code>-u (--set-upstream)&lt;/code>&lt;/strong> オプションを付けて実行すると、リモートブランチ名とローカルブランチ名が対応付けられて、次回からはパラメーターなしで &lt;code>git push&lt;/code> できるようになります。&lt;/p></description></item><item><title>Git でブランチを削除する (git branch -d/-D)</title><link>https://maku77.github.io/p/stpfje9/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/stpfje9/</guid><description>&lt;p>不要になったブランチを削除するには、&lt;strong>&lt;code>git branch -d&lt;/code>&lt;/strong> コマンドでブランチ名を指定します。
削除対象のブランチが作業対象になっていると削除できないので、先に &lt;code>git switch&lt;/code> コマンドで別のブランチに切り替えてから削除を実行します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">mybranch ブランチを削除する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git switch main
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -d mybranch
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Deleted branch mybranch (was 1cc229d3)
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>削除しようとしているブランチの変更内容が、ブランチ元にマージされていない場合、以下のようなエラーが表示されます。&lt;/p>
&lt;pre tabindex="0">&lt;code>error: The branch &amp;#39;mybranch&amp;#39; is not an ancestor of your current HEAD.
If you are sure you want to delete it, run &amp;#39;git branch -D mybranch&amp;#39;
&lt;/code>&lt;/pre>&lt;p>この場合は、先にブランチ元にマージを実行するか、あるいは、変更内容が完全に消えても構わない場合は &lt;code>-d&lt;/code> オプションの代わりに &lt;strong>&lt;code>-D&lt;/code>&lt;/strong> オプションを指定して実行します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">マージされていないブランチを強制的に削除する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git branch -D mybranch
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;ul>
&lt;li>参考: &lt;a href="../p/cv5pi7a/">Git でリモートブランチを削除する (&lt;code>git push origin :xxx&lt;/code>, &lt;code>git fetch --prune&lt;/code>)&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Git で変更をローカルリポジトリにコミットする</title><link>https://maku77.github.io/p/ez3knei/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/ez3knei/</guid><description>&lt;h2 id="git-commit-で変更をローカルリポジトリにコミットする">git commit で変更をローカルリポジトリにコミットする&lt;/h2>
&lt;p>&lt;code>git add&lt;/code>、&lt;code>git rm&lt;/code>、&lt;code>git mv&lt;/code> などのコマンドによってステージされたファイル（の変更箇所）は、&lt;code>git commit&lt;/code> でローカルリポジトリにコミットされる対象になります。
コミットを行うときは、必ずコミットコメントを記述する必要があります。
コメントは、下記のように &lt;strong>&lt;code>-m&lt;/code>&lt;/strong> オプションで指定することができます。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">コメントを付けてコミット&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;Correct typos&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>複数の段落にわたるコメントを付けたい場合は、&lt;code>-m&lt;/code> オプションを複数指定します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">複数段落のコメントを付ける&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;First paragraph&amp;#34;&lt;/span> -m &lt;span class="s2">&amp;#34;Long long description.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">commit c7617b4d92edd946ae2a3358c321a2c6c81240b8
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Author: test &amp;lt;test@example.org&amp;gt;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Date: Sat Jul 17 17:39:57 2010 +0900
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go"> First paragraph
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go"> Long long description.
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>&lt;code>git commit&lt;/code> コマンドに &lt;code>-m&lt;/code> オプションを指定しなかった場合は、コミットコメントを入力するためにエディタが起動します。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">エディタを開いてコミットコメントを入力する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>このとき &lt;strong>&lt;code>-v&lt;/code>&lt;/strong> オプションを指定すると、コミットされるファイルの変更差分 (diff) も同時にエディタ上に表示されます。
ただ、一般的には &lt;code>git commit&lt;/code> の実行前に &lt;code>git diff --staged&lt;/code> で差分確認します。&lt;/p></description></item><item><title>Git のコミット時にコメント記述に使用するエディタを設定する (core.editor)</title><link>https://maku77.github.io/p/cqjv7wv/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/cqjv7wv/</guid><description>&lt;p>&lt;code>git commit&lt;/code> コマンド実行時に、&lt;code>-m&lt;/code> オプションでコメントを入力しなかった場合は、コメント入力のためにエディタが起動します。
ここで起動するエディタは &lt;strong>&lt;code>core.editor&lt;/code>&lt;/strong> 設定で自由に変更できます。
操作に慣れた Vim エディタなどを指定しておけば、コミット前の編集画面で操作ミスしてしまうこともなくなるでしょう。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: エディタを Neovim (nvim) に設定する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git config --global core.editor nvim
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;p>上記の例では、&lt;strong>&lt;code>core.editor&lt;/code>&lt;/strong> 設定を使ってエディタの指定を行っていますが、エディタの指定方法はたくさんあり、Git は次の順番で使用するエディタを判断します。&lt;/p>
&lt;ol>
&lt;li>環境変数 &lt;strong>&lt;code>GIT_EDITOR&lt;/code>&lt;/strong>&lt;/li>
&lt;li>Git の &lt;strong>&lt;code>core.editor&lt;/code>&lt;/strong> 設定（&lt;code>git config&lt;/code> コマンド）&lt;/li>
&lt;li>環境変数 &lt;strong>&lt;code>VISUAL&lt;/code>&lt;/strong>&lt;/li>
&lt;li>環境変数 &lt;strong>&lt;code>EDITOR&lt;/code>&lt;/strong>&lt;/li>
&lt;li>&lt;code>vi&lt;/code>（Debian、Ubuntu の場合は &lt;code>editor&lt;/code>）&lt;/li>
&lt;/ol>
&lt;p>どれで設定しても良いですけど、基本的には他の設定項目と一緒に &lt;code>git config&lt;/code> コマンドで設定してしまうのがシンプルです。&lt;/p></description></item><item><title>Git の作業ツリーを作成する (git init, git clone)</title><link>https://maku77.github.io/p/muhmimi/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/muhmimi/</guid><description>&lt;h2 id="git-の作業ツリーの作成方法は２つ">Git の作業ツリーの作成方法は２つ&lt;/h2>
&lt;p>Git の作業ツリーを作成するには、以下の 2 種類の方法があります。&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>git init&lt;/code>&lt;/strong> &amp;hellip; 新しくローカルリポジトリを作成します。&lt;/li>
&lt;li>&lt;strong>&lt;code>git clone&lt;/code>&lt;/strong> &amp;hellip; リモートの Git リポジトリをコピーしてきて、作業ディレクトリとローカルリポジトリを作成します。&lt;/li>
&lt;/ul>
&lt;h2 id="git-init-で新しく作業ツリーを作成する">git init で新しく作業ツリーを作成する&lt;/h2>
&lt;p>完全に新規の作業ツリーを作成する場合と、既存のファイル群を Git で管理する場合は、&lt;strong>&lt;code>git init&lt;/code>&lt;/strong> コマンドを使用して、あるディレクトリ以下を Git の管理下に置きます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> mkdir myproject
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> myproject
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git init
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Initialized empty Git repository in /home/john/myproject/.git
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>この &lt;code>git init&lt;/code> コマンドにより、カレントディレクトリに &lt;strong>&lt;code>.git&lt;/code>&lt;/strong> という Git の管理ディレクトリが作成され、カレントディレクトリ以下が Git の管理対象に入ります。
この時点ではローカルリポジトリの中は空っぽなので、このディレクトリ（作業ツリー）内でファイルを作成し、&lt;code>git add&lt;/code> や &lt;code>git commit&lt;/code> コマンドなどを使用してリポジトリにファイルをコミットしていきます。&lt;/p>
&lt;p>既にソースコードの入っているディレクトリを Git で管理する場合も、まずは上記と同様の手順でリポジトリ (&lt;code>.git&lt;/code>) を作成します。
ただし、&lt;code>git init&lt;/code> しただけでは、既存のファイルはリポジトリの管理下に入らないので、以下のように既存のファイル群をコミットしてやる必要があります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> myproject
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git init
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add .
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;First commit.&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">```console
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">コミットの履歴は `git log` で確認できます。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">git clone で別のマシンのリポジトリから作業ツリーを作成する（コードをコピーしてくる）
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">----
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">
&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: Android のコードを取得してみる&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> &lt;span class="nb">cd&lt;/span> ~/gitwork
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git clone git://android.git.kernel.org/platform/development.git
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Initialized empty Git repository in /home/john/gitwork/development/.git
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">got 36a38df99cd7485db6a83c4f8c7b329c24b9b6e8
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">...
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">上記のようにすると、カレントディレクトリに `development` ディレクトリがローカルにコピーされます。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">`development` ディレクトリは作業ツリーであり、そのルートに更新履歴を示すリポジトリのコアとなる `.git` ディレクトリも入っています。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">`git clone` を実行しようとして以下のようなエラーが出る場合、git プロトコルの代わりに `http` プロトコルを指定するとうまく取得できたりします。
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Initialized empty Git repository in /home/john/gitwork/development/.git
fatal: Unable to look up android.git.kernel.org (port 9418) (Name or service not known)&lt;/p></description></item><item><title>Git 用語集</title><link>https://maku77.github.io/p/f844tkg/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/f844tkg/</guid><description>&lt;h2 id="作業ツリーワーキングツリー-working-tree">作業ツリー、ワーキングツリー (working tree)&lt;/h2>
&lt;p>Git の管理下に入ったローカルのディレクトリツリーを &lt;strong>作業ツリー (working tree)&lt;/strong> と呼びます。
実際にファイルを編集する場所であり、ユーザーが直接操作するのはこの作業ツリーです。
リポジトリに記録された内容を作業ツリーに展開する操作を &lt;strong>チェックアウト (checkout)&lt;/strong> と呼びます。
&lt;code>git init&lt;/code> コマンドなどで作成される &lt;code>.git/&lt;/code> ディレクトリが存在するディレクトリが、作業ツリーのルートになります。&lt;/p>
&lt;h2 id="リポジトリ-repository">リポジトリ (repository)&lt;/h2>
&lt;p>変更履歴などを管理するデータベースで、Git ではローカルディレクトリ内にリポジトリの実体が存在します。
リポジトリの実体は、作業ツリーのルートにある &lt;code>.git/&lt;/code> ディレクトリです。
Git は分散型バージョン管理システムなので、各開発者がローカルにリポジトリの完全なコピーを持ちます。
それぞれのリポジトリは対等の関係にあり、コミットされた変更内容を互いにやりとり (&lt;code>push&lt;/code> / &lt;code>pull&lt;/code>) することでプロジェクト全体の一貫性を保ちます。
一般的には、GitHub などにある一つのリポジトリを中央リポジトリ（リモートリポジトリ）とし、開発のベースとして使用します。&lt;/p>
&lt;h2 id="インデックスステージングエリア-index--staging-area">インデックス、ステージングエリア (index / staging area)&lt;/h2>
&lt;p>&lt;strong>インデックス&lt;/strong> は、作業ツリーとリポジトリ（コミット履歴）の間にある中間領域です。
次回のコミットに含めるファイルの変更内容が登録される場所で、&lt;strong>ステージングエリア&lt;/strong> とも呼ばれます。
&lt;code>git add&lt;/code> でファイルをインデックスに登録し、&lt;code>git commit&lt;/code> でインデックスの内容をリポジトリに記録する、という 2 段階の仕組みになっています。
インデックスの実体は &lt;code>.git/index&lt;/code> ファイルです。&lt;/p>
&lt;h2 id="ステージする-stage">ステージする (stage)&lt;/h2>
&lt;p>あるファイルの変更を次回のコミット対象に含めることを、ファイルを &lt;strong>ステージする (stage)&lt;/strong> といいます。
&lt;code>git add&lt;/code> や &lt;code>git rm&lt;/code> コマンドでファイルを指定すると、その変更がインデックスに登録され、次回のコミット対象となります。&lt;/p>
&lt;h2 id="ハンク-hunk">ハンク (hunk)&lt;/h2>
&lt;p>ファイル内の連続した変更箇所のこと。
1 つのファイル内に連続した変更箇所が複数あれば、ハンクも複数存在することになります。
Git ではファイル単位で変更を追跡するのではなく、もっと細かいハンク単位で追跡します。
つまり、ファイル内の変更の一部だけをコミットするという作業が行えます。&lt;/p></description></item><item><title>Gitメモ: Git でのログ表示の基本</title><link>https://maku77.github.io/p/hsavjib/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/hsavjib/</guid><description>&lt;p>ローカルリポジトリにおけるコミットログを参照するには、&lt;code>git log&lt;/code> コマンドを使用します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log &lt;span class="c1"># すべてのコミットログを表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -5 &lt;span class="c1"># 最近の 5 つのコミットログを表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log -p &lt;span class="c1"># パッチ作成用の diff（変更内容）も表示&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git log 613e3de &lt;span class="c1"># 指定したリビジョンのコミットログを表示&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;code>git log&lt;/code> コマンドは、現在チェックアウトしているブランチのログを表示します。&lt;/p></description></item><item><title>Gitメモ: ソースコードのアーカイブファイル (ZIP/TAR) を作成する (git archive)</title><link>https://maku77.github.io/p/dmpa5vw/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/dmpa5vw/</guid><description>&lt;h2 id="最新のソースコードのアーカイブを作成する">最新のソースコードのアーカイブを作成する&lt;/h2>
&lt;p>&lt;strong>&lt;code>git archive&lt;/code>&lt;/strong> コマンドを使用すると、現在チェックアウトしているブランチの最新コミット (HEAD) のコードを ZIP や TAR アーカイブファイルとしてバックアップできます。
バックアップには &lt;code>.git&lt;/code> ディレクトリ（リポジトリ本体）は含まれないため、あくまでソースコードを配布するときなどに使用できるコマンドです。&lt;/p>

&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: ZIP ファイルを作成する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git archive -o backup.zip HEAD
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">#&lt;/span> あるいは
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="gp">$&lt;/span> git archive --format&lt;span class="o">=&lt;/span>zip HEAD &amp;gt; backup.zip
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>


&lt;figure class="xCodeBlock">
&lt;figcaption class="xCodeBlock_title">例: tar.gz ファイルを作成する&lt;/figcaption>
&lt;div class="xCodeBlock_code">&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git archive --format&lt;span class="o">=&lt;/span>tar HEAD &lt;span class="p">|&lt;/span> gzip &amp;gt; backup.tar.gz
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>
&lt;/figure>

&lt;h2 id="特定のバージョンのソースコードのアーカイブを作成する">特定のバージョンのソースコードのアーカイブを作成する&lt;/h2>
&lt;p>例えば、タグ &lt;code>REL-1.2.34&lt;/code> のついたバージョンのソースコードのアーカイブを作成するには、下記のようにします（最新を表す &lt;code>HEAD&lt;/code> の指定をタグ名に置き換えるだけです）。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git archive -o backup.zip REL-1.2.34
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="アーカイブを展開したときにディレクトリ内に展開されるようにする">アーカイブを展開したときにディレクトリ内に展開されるようにする&lt;/h2>
&lt;p>&lt;code>git archive&lt;/code> コマンドによって作成されたアーカイブファイルを展開すると、デフォルトではフラットにファイル群が展開されてしまいます。
展開時にディレクトリを作成して、その中にファイル群を展開するようにするには、下記のようにディレクトリ名を示す &lt;code>--prefix&lt;/code> オプションを指定します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git archive --format&lt;span class="o">=&lt;/span>zip --prefix&lt;span class="o">=&lt;/span>myapp/ HEAD &amp;gt; myapp-1.0.zip
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>上記のようにすると、&lt;code>myapp-1.0.zip&lt;/code> を展開したときに、&lt;code>myapp&lt;/code> というディレクトリの下にファイル群が展開されるようになります。
&lt;code>--prefix&lt;/code> オプションで指定するディレクトリ名は、&lt;strong>最後にスラッシュを付ける&lt;/strong>ことに注意してください。
これを忘れると、各ファイルのプレフィックスとしてその名前が付けられてしまいます。&lt;/p></description></item><item><title>Gitメモ: 変更の一部だけをコミットする (git add -p)</title><link>https://maku77.github.io/p/a6776js/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/a6776js/</guid><description>&lt;p>Git では、変更をファイル単位ではなく、ひとまとまりの変更**「ハンク (Hunk)」**の単位で認識するので、ファイルの一部分の変更だけをコミットすることができます。&lt;/p>
&lt;p>ハンクごとにコミットする対象を指定（ステージング）したい場合は、&lt;code>git add&lt;/code> に &lt;code>-p&lt;/code> オプションを付けて実行し、&lt;strong>パッチモード&lt;/strong>という対話モードを起動します。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git add -p Hoge.txt
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>すると、変更部分ごとに &lt;code>diff&lt;/code> が順番に表示されるので、実際にそのハンクをステージするかを &lt;code>y/n&lt;/code> で選んでいきます。
選択し終わったら、あとは通常通りコミットするだけです。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git commit -m &lt;span class="s2">&amp;#34;Your comment&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>ファイルの一部だけステージされている状況で &lt;code>git status&lt;/code> コマンドを実行すると、そのファイルは &lt;strong>"Changes to be committed"&lt;/strong> にも属しているし、&lt;strong>"Changed but not updated"&lt;/strong> にも属しているというように表示されます。&lt;/p></description></item><item><title>コミット対象になっているファイルを確認する (git status)</title><link>https://maku77.github.io/p/z26xvc9/</link><pubDate>Sat, 17 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/z26xvc9/</guid><description>&lt;p>&lt;strong>&lt;code>git status&lt;/code>&lt;/strong> コマンドを使用すると、次回のコミットの対象になっているファイルや、コミットの対象にはなっていないけれど編集されているファイルなどを確認することができます。
出力は大体以下のような感じになります。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git status
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">On branch main
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">Your branch is up to date with &amp;#39;origin/main&amp;#39;.
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">Changes to be committed:
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> (use &amp;#34;git restore --staged &amp;lt;file&amp;gt;...&amp;#34; to unstage)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	new file: aaa.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	new file: bbb.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	modified: ccc.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">Changes not staged for commit:
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> (use &amp;#34;git add &amp;lt;file&amp;gt;...&amp;#34; to update what will be committed)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> (use &amp;#34;git restore &amp;lt;file&amp;gt;...&amp;#34; to discard changes in working directory)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	modified: ddd.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	midified: eee.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	deleted: fff.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">&lt;/span>&lt;span class="err">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="err">&lt;/span>&lt;span class="go">Untracked files:
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go"> (use &amp;#34;git add &amp;lt;file&amp;gt;...&amp;#34; to include in what will be committed)
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	ggg.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	hhh.txt
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="go">	iii.txt
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>3 つのセクションに分けてファイルが表示されますが、それぞれ下記のような意味を持っています。&lt;/p></description></item><item><title>Gitメモ: Git / git diff でファイルの差分を調べる</title><link>https://maku77.github.io/p/y9v4isr/</link><pubDate>Wed, 14 Jul 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/y9v4isr/</guid><description>&lt;h2 id="さまざまな差分を理解する">さまざまな差分を理解する&lt;/h2>
&lt;p>Git の diff を理解するには、まず以下の３か所の、どの部分間での差分を取りたいのかを意識する必要があります。&lt;/p>
&lt;ul>
&lt;li>リポジトリにコミットされた内容&lt;/li>
&lt;li>ステージングエリア内のコミット待ちの変更内容（&lt;code>git add&lt;/code> で index に追加されたハンク）&lt;/li>
&lt;li>作業ツリー内の変更内容（&lt;code>git add&lt;/code> されていない変更）&lt;/li>
&lt;/ul>
&lt;pre tabindex="0">&lt;code> +------------+ +--------------+ +--------------+
 | Repository |&amp;lt;---------------------&amp;gt;| Staging Area |&amp;lt;------------&amp;gt;| Working Tree |
 +------------+ git diff --cached +--------------+ git diff +------+-------+
 | (--staged) |
 +------------------------------------------------------------------+
 git diff HEAD
&lt;/code>&lt;/pre>&lt;h2 id="リポジトリとステージングエリアの変更差分">リポジトリとステージングエリアの変更差分&lt;/h2>
&lt;p>ステージングエリアに入った変更、つまり次回のコミットの対象となっている変更部分を調べたい場合は以下のようにします。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --cached
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff --staged（新しいバージョンで追加された方法）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="ステージングエリアと作業ツリー内の変更差分">ステージングエリアと作業ツリー内の変更差分&lt;/h2>
&lt;p>ステージングエリアに登録されていない変更、つまり、まだ &lt;code>git add&lt;/code> されていない作業ツリー上での変更を調べたい場合は以下のようにします。
ただし、新規に作成されたファイルに関しては表示されません。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="リポジトリとステージングエリア及び作業ツリー内の変更差分">リポジトリとステージングエリア、及び、作業ツリー内の変更差分&lt;/h2>
&lt;p>上記の２つの差分表示を両方合わせたものです。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff HEAD
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>リポジトリ内の任意のリビジョンを指定することもできます。&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> git diff b374ef1
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="変更された行数の統計を取る">変更された行数の統計を取る&lt;/h2>
&lt;p>&lt;code>git diff&lt;/code> を &lt;code>--stat&lt;/code> オプションを指定して実行すると、2 つのリビジョン間で、どのファイルが何行変更されたかのグラフを表示してくれます。&lt;/p></description></item><item><title>Gitメモ: .svn ディレクトリをまとめて削除する</title><link>https://maku77.github.io/p/3thorrh/</link><pubDate>Sun, 25 Apr 2010 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/3thorrh/</guid><description>&lt;h2 id="linux-の場合">Linux の場合&lt;/h2>
&lt;div class="highlight">&lt;pre tabindex="0" class="chroma">&lt;code class="language-console" data-lang="console">&lt;span class="line">&lt;span class="cl">&lt;span class="gp">$&lt;/span> find . -name &lt;span class="s1">&amp;#39;.svn&amp;#39;&lt;/span> -exec rm -rf &lt;span class="o">{}&lt;/span> &lt;span class="se">\;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>コマンドの実行順序の関係で "No such file or directory" といったエラーが大量に出ますが、&lt;code>.svn&lt;/code> ディレクトリはちゃんと削除されています。&lt;/p>
&lt;h2 id="windows-xp-の場合">Windows XP の場合&lt;/h2>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; for /d /r %x in (*.svn) do rmdir /q /s &amp;#34;%x&amp;#34;
&lt;/code>&lt;/pre>&lt;p>&lt;code>.svn&lt;/code> ディレクトリが隠しディレクトリになっている場合は、Windows では &lt;code>FOR&lt;/code> コマンドで列挙されないので、&lt;code>DIR&lt;/code> コマンドと組み合わせて以下のようにする必要があります。
ただし、この方法は &lt;code>DIR&lt;/code> コマンドが終わってから &lt;code>FOR&lt;/code> コマンドに結果が渡されるので、かなり効率が悪くなります。&lt;/p>
&lt;pre tabindex="0">&lt;code>C:\&amp;gt; for /f &amp;#34;tokens=*&amp;#34; %x in (&amp;#39;dir /b /s /a:dh *.svn&amp;#39;) do rmdir /q /s &amp;#34;%x&amp;#34;
&lt;/code>&lt;/pre>&lt;p>バッチファイルにするときは、&lt;code>%x&lt;/code> の部分を &lt;code>%%x&lt;/code> にするのを忘れないようにしてください。&lt;/p></description></item><item><title>Gitメモ: コミットログの書き方</title><link>https://maku77.github.io/p/svg6x8f/</link><pubDate>Tue, 20 May 2008 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/svg6x8f/</guid><description>&lt;p>&lt;a href="http://tbaggery.com/2008/04/19/a-note-about-git-commit-messages.html">Tim Pope さんのブログ&lt;/a>に Git のコミットログの書き方の要約が書いてあります。
わかりやすいので、抜粋してご紹介します。
下記は、コミットログの書き方（フォーマット）の説明ですが、この説明自体がコミットログの書き方に従って書かれています。&lt;/p>
&lt;pre tabindex="0">&lt;code>Capitalized, short (50 chars or less) summary

More detailed explanatory text, if necessary. Wrap it to about 72
characters or so. In some contexts, the first line is treated as the
subject of an email and the rest of the text as the body. The blank
line separating the summary from the body is critical (unless you omit
the body entirely); tools like rebase can get confused if you run the
two together.

Write your commit message in the imperative: &amp;#34;Fix bug&amp;#34; and not &amp;#34;Fixed bug&amp;#34;
or &amp;#34;Fixes bug.&amp;#34; This convention matches up with commit messages generated
by commands like git merge and git revert.

Further paragraphs come after blank lines.

- Bullet points are okay, too

- Typically a hyphen or asterisk is used for the bullet, followed by a
 single space, with blank lines in between, but conventions vary here

- Use a hanging indent
&lt;/code>&lt;/pre>&lt;p>ポイントをざっとまとめるとこんな感じです。&lt;/p></description></item><item><title>Gitメモ: CVS のファイルのパーミッションの扱いについて</title><link>https://maku77.github.io/p/mt89s5z/</link><pubDate>Sat, 08 Jan 2005 00:00:00 +0900</pubDate><guid>https://maku77.github.io/p/mt89s5z/</guid><description>&lt;p>CVS のリポジトリはファイルの実行属性だけは保存してくれます。
この属性は &lt;code>cvs add&lt;/code> する時点で設定されていなければいけません。&lt;/p>
&lt;p>&lt;code>cvs update&lt;/code> した場合は、リポジトリに置いてあるファイルのパーミッションの方が優先されます。
パーミッションを変更したい場合は、リポジトリ内の RCS ファイルのパーミッションを直接変更しなければいけません。&lt;/p></description></item></channel></rss>